---
phase: 04-inicio-recargas
plan: 06
type: execute
wave: 2
depends_on: ["04-03", "04-04"]
files_modified:
  - src/components/recargas/RecargasKPICards.tsx
  - src/components/recargas/RecargasTrendChart.tsx
  - src/components/recargas/RecargasTable.tsx
  - src/components/recargas/HechosCuradosRecargas.tsx
autonomous: true

must_haves:
  truths:
    - "RecargasKPICards renders 2 cards (Total $ recargado, # transacciones), both with deltas, no presenter-hide (no internal-only KPI in Recargas)"
    - "RecargasTrendChart is a 320px-tall bar chart, mirroring GMVTrendChart structure but for monto-by-day"
    - "RecargasTable renders top 10 empresas, mirroring SalesTable from bonos (sortable visual hierarchy by descending monto)"
    - "HechosCuradosRecargas container carries data-presenter-empresa-hide"
    - "DeltaBadge from inicio/ is reused (no duplication)"
  artifacts:
    - path: "src/components/recargas/RecargasKPICards.tsx"
      provides: "2-KPI grid with delta (Server Component)"
      exports: ["RecargasKPICards"]
      min_lines: 50
    - path: "src/components/recargas/RecargasTrendChart.tsx"
      provides: "Bar chart of monto recargado by date (Client Component)"
      exports: ["RecargasTrendChart"]
      contains: "\"use client\""
      min_lines: 40
    - path: "src/components/recargas/RecargasTable.tsx"
      provides: "Top 10 empresas table (Server Component)"
      exports: ["RecargasTable"]
      min_lines: 50
    - path: "src/components/recargas/HechosCuradosRecargas.tsx"
      provides: "2-card narrative section, container marked data-presenter-empresa-hide"
      exports: ["HechosCuradosRecargas"]
      min_lines: 60
  key_links:
    - from: "src/components/recargas/RecargasKPICards.tsx"
      to: "src/components/inicio/DeltaBadge"
      via: "Cross-feature reuse of DeltaBadge atom"
      pattern: "from \"@/components/inicio/DeltaBadge\""
    - from: "src/components/recargas/RecargasTable.tsx"
      to: "@/lib/domain/recargas"
      via: "RecargaByEmpresa type-only import"
      pattern: "type .* RecargaByEmpresa"
---

<objective>
Build the 4 Recargas visual leaves: KPICards (2 cards with delta), TrendChart (bar chart by date), Table (top 10), and HechosCuradosRecargas. Recargas is NOT the hero pestaña per CONTEXT.md, but stays consistent with Inicio's editorial voice — same component primitives, same currentColor + tabular-nums conventions.

Purpose: REC-01/02/03 are roadmap-mandated. Hechos curados (top empresa recargadora, recarga más grande) are the cliente-foco-hidden narrative layer. CONTEXT.md cut-priority allows trimming hechos here first if scope tightens — but Plan 04-03 already built the domain primitives, so the marginal cost of rendering them is low.

Output: 4 new component files in `src/components/recargas/`. DeltaBadge is reused from `inicio/` (Plan 04-05) — do NOT duplicate.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-inicio-recargas/04-CONTEXT.md
@.planning/phases/04-inicio-recargas/04-RESEARCH.md
@.planning/phases/04-inicio-recargas/04-03-SUMMARY.md
@.planning/phases/04-inicio-recargas/04-05-SUMMARY.md

# Reference components to mirror:
@src/components/bonos/SalesTable.tsx
@src/components/bonos/KPICards.tsx
@src/components/bonos/BonosChart.tsx
@src/components/inicio/DeltaBadge.tsx
@src/components/ui/card.tsx
@src/lib/format.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RecargasKPICards + RecargasTrendChart</name>
  <files>src/components/recargas/RecargasKPICards.tsx, src/components/recargas/RecargasTrendChart.tsx</files>
  <action>
    **RecargasKPICards.tsx** — Server Component. Mirror `KPICardsInicio.tsx` (Plan 04-05 Task 1) but with 2 cards in a `grid grid-cols-1 sm:grid-cols-2 gap-4` (no lg:grid-cols-5).

    Props:
    ```ts
    type Props = {
      summary: { current: RecargaSummary; prior: RecargaSummary | null };
    };
    ```

    Imports: `Card, CardContent, CardDescription, CardHeader, CardTitle` from `@/components/ui/card`; `formatCOP, formatInteger` from `@/lib/format`; `DeltaBadge` from `@/components/inicio/DeltaBadge` (cross-feature reuse — do NOT duplicate); type-only `RecargaSummary` from `@/lib/domain/recargas`.

    Cards (in this order):
    1. **Total $ recargado** — `<CardDescription>Total recargado</CardDescription>` + `<CardTitle className="font-heading text-3xl tabular-nums">{formatCOP(current.montoTotal)}</CardTitle>` + `<DeltaBadge current={current.montoTotal} prior={prior?.montoTotal ?? null} />`
    2. **# transacciones** — `<CardDescription># de recargas</CardDescription>` + `formatInteger(current.count)` + delta on count.

    NO `data-presenter-hide` on either card (Recargas has no internal-only KPI per CONTEXT.md — Comisión/Take rate are Inicio's concern; Recargas just shows volume).

    **RecargasTrendChart.tsx** — `"use client"`. Mirror `GMVTrendChart.tsx` (Plan 04-05 Task 2) shape exactly. Difference:
    - Props: `{ data: RecargaByDate[] }` (no granularity prop — Recargas trend is daily-only per CONTEXT.md "Recargas no es héroe" — keep it simple).
    - dataKey for Bar: `"monto"`.
    - tickFormatter for X-axis: `(d: string) => d.slice(8) + "/" + d.slice(5, 7)` (just daily compaction; no week branch).
    - Tooltip formatter: `(v: number) => [formatCOP(v), "Recargado"]`.
    - Same `h-[320px] w-full text-foreground` wrapper, same currentColor strokes, same minPointSize=2 + radius=[4,4,0,0].

    Type imports: type-only `RecargaByDate` from `@/lib/domain/recargas`.
  </action>
  <verify>
    1. `grep -c "data-presenter-hide" src/components/recargas/RecargasKPICards.tsx` === 0.
    2. `grep "use client" src/components/recargas/RecargasKPICards.tsx` returns NOTHING.
    3. `grep "use client" src/components/recargas/RecargasTrendChart.tsx` returns the directive.
    4. `grep "@/components/inicio/DeltaBadge" src/components/recargas/RecargasKPICards.tsx` ≥ 1 (cross-feature reuse confirmed).
    5. `grep -c "DeltaBadge\\|Bar\\|Card" src/components/recargas/RecargasTrendChart.tsx` returns Bar (no Card import — page provides chrome).
    6. `npx tsc --noEmit` passes.
  </verify>
  <done>
    Both components compile. KPICards reuses DeltaBadge from inicio/. TrendChart is daily-only.
  </done>
</task>

<task type="auto">
  <name>Task 2: RecargasTable + HechosCuradosRecargas</name>
  <files>src/components/recargas/RecargasTable.tsx, src/components/recargas/HechosCuradosRecargas.tsx</files>
  <action>
    **RecargasTable.tsx** — Server Component. Mirror `bonos/SalesTable.tsx` shape line-for-line, adapted for `RecargaByEmpresa` shape.

    Props:
    ```ts
    type Props = { rows: RecargaByEmpresa[] };
    ```

    Layout: A `<Card>` containing `<CardHeader><CardTitle>Top 10 empresas por recargas</CardTitle></CardHeader><CardContent>...</CardContent>`.

    Inside CardContent: an HTML `<table className="w-full text-sm">` with:
    - `<thead className="border-b text-xs uppercase text-muted-foreground">`:
      - Empresa (left-aligned)
      - # recargas (right-aligned, tabular-nums)
      - $ recargado (right-aligned, tabular-nums)
      - % del total (right-aligned, tabular-nums)
    - `<tbody>` rows: each `<tr className="border-b last:border-0">`:
      - empresa_nombre (truncate to ~30 chars with title attribute)
      - formatInteger(count)
      - formatCOP(monto)
      - formatPercent(pctDelTotal)

    Empty state: if `rows.length === 0`, render `<p className="text-sm text-muted-foreground py-8 text-center">Sin recargas en el período</p>` inside CardContent (no table).

    NO `data-presenter-hide` on any column (per CONTEXT.md — Recargas has no internal-only column. The full table is fine even in cliente-foco for the cliente's own data).

    Imports: `Card, CardContent, CardHeader, CardTitle` from `@/components/ui/card`; `formatCOP, formatInteger, formatPercent` from `@/lib/format`; type-only `RecargaByEmpresa` from `@/lib/domain/recargas`.

    **HechosCuradosRecargas.tsx** — Server Component. Mirror `inicio/HechosCurados.tsx` (Plan 04-05 Task 3) but with 2 cards instead of 3.

    Props:
    ```ts
    type Props = {
      topEmpresa: RecargaByEmpresa | null;
      recargaMasGrande: Transaction | null;
    };
    ```

    Imports: `Card, CardHeader, CardTitle, CardContent, CardDescription` from `@/components/ui/card`; `formatCOP, formatBogotaDate` from `@/lib/format`; type-only `RecargaByEmpresa` from `@/lib/domain/recargas`; type-only `Transaction` from `@/lib/domain/types`.

    Layout:
    - Outer wrapper: `<div className="space-y-4" data-presenter-empresa-hide>` (cliente-foco hides whole section).
    - Section title: `<h2 className="text-sm font-medium text-muted-foreground uppercase tracking-wide">Lo destacado del período</h2>`.
    - 2-card grid: `<div className="grid gap-4 md:grid-cols-2">`
      1. **Top empresa recargadora** Card:
         - Title: `topEmpresa?.empresa_nombre ?? "—"`
         - Description: `Top empresa recargadora`
         - Body: `formatCOP(topEmpresa?.monto ?? 0)` + small muted line `{formatInteger(topEmpresa?.count ?? 0)} recargas` — or `"Sin recargas en el período"` if null.
      2. **Recarga más grande** Card:
         - Title: `formatCOP(recargaMasGrande?.monto ?? 0)` (font-heading text-3xl tabular-nums) — or "—".
         - Description: `Recarga más grande`
         - Body: `recargaMasGrande` ? `<p>{empresa_nombre} · {formatBogotaDate(fecha)}</p>` : `"Sin recargas en el período"`.

    DO NOT use any client hook.
  </action>
  <verify>
    1. `grep "use client" src/components/recargas/RecargasTable.tsx src/components/recargas/HechosCuradosRecargas.tsx` returns NOTHING.
    2. `grep -c "data-presenter-hide" src/components/recargas/RecargasTable.tsx` === 0.
    3. `grep -c "data-presenter-empresa-hide" src/components/recargas/HechosCuradosRecargas.tsx` === 1 (outer wrapper only).
    4. `npx tsc --noEmit` passes.
    5. Mental render with `rows: []` → friendly empty-state copy in table area.
    6. Mental render with `topEmpresa: null + recargaMasGrande: null` → 2 empty-state cards.
  </verify>
  <done>
    RecargasTable renders top 10 with 4 columns + empty state. HechosCuradosRecargas renders 2-card narrative section with cliente-foco wrapper.
  </done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run build` succeeds
- [ ] All 4 files exist under src/components/recargas/
- [ ] DeltaBadge cross-feature reuse confirmed (no duplicate atom)
- [ ] HechosCuradosRecargas wrapper carries data-presenter-empresa-hide
- [ ] Zero React conditionals on presenter / empresa state
- [ ] No regression on other tabs
</verification>

<success_criteria>
- All 5 must_have truths verified
- Plan 04-08 (page composition) can compose all 4 components without refactor
- DeltaBadge is the SINGLE delta atom for the project (used by both Inicio and Recargas KPI cards)
</success_criteria>

<output>
After completion, create `.planning/phases/04-inicio-recargas/04-06-SUMMARY.md` documenting:
- Component shapes (props, line counts)
- The cross-feature DeltaBadge reuse pattern
- Empty-state copy variants
- Stable contracts for Plan 04-08 (page passes summary + chart data + table rows + 2 hechos)
</output>
