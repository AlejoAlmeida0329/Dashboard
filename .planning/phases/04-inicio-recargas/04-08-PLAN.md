---
phase: 04-inicio-recargas
plan: 08
type: execute
wave: 3
depends_on: ["04-03", "04-04", "04-06"]
files_modified:
  - src/app/(protected)/recargas/page.tsx
autonomous: false

must_haves:
  truths:
    - "/recargas renders live BD_Plataforma data filtered to RECHARGE_TIPOS: 2 KPIs with deltas, 1 trend chart, top-10 table, 2 hechos curados"
    - "Default view: 2 KPIs + chart + table + 2 hechos curados visible (no presenter-hide on KPIs — Recargas has no internal-only metric)"
    - "?presenter=1: same as default (Recargas has no Comisión/Take rate equivalent to hide)"
    - "?empresa=$X: 2 KPIs filtered, chart filtered, table shows just that empresa, 2 hechos curados visible"
    - "?presenter=1&empresa=$X (cliente-foco): 2 KPIs visible, chart visible, table visible, 2 hechos curados HIDDEN"
    - "Sheets fetch failure: inline Card with err.message"
    - "Empty state: friendly Spanish copy when no recargas in period"
  artifacts:
    - path: "src/app/(protected)/recargas/page.tsx"
      provides: "Recargas page composition (replaces placeholder)"
      contains: "filterRecargas"
      min_lines: 100
  key_links:
    - from: "src/app/(protected)/recargas/page.tsx"
      to: "@/lib/domain/recargas"
      via: "filterRecargas + summarizeRecargas + aggregations + 2 hechos"
      pattern: "from \"@/lib/domain/recargas\""
    - from: "src/app/(protected)/recargas/page.tsx"
      to: "@/lib/domain/period"
      via: "computePriorPeriod for delta computation"
      pattern: "computePriorPeriod"
---

<objective>
Compose `/recargas` page replacing the Phase 1 placeholder. Mirror `/inicio` page (Plan 04-07) but simpler — Recargas is not the hero pestaña, has only 2 KPIs (no Comisión/Take rate equivalent), 1 chart, 1 table, 2 hechos curados.

Purpose: REC-01/02/03 are roadmap-mandated. Phase 5 doesn't strictly depend on Recargas — but having Recargas done unblocks the "highlight reel consistency" CONTEXT.md asks for, and lets Phase 5's CLI-07 (mini-resumen 3 cards Bonos/Recargas/Payouts) point to a populated page.

Output: Single replaced page.tsx file. Plan ends with checkpoint:human-verify.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-inicio-recargas/04-CONTEXT.md
@.planning/phases/04-inicio-recargas/04-RESEARCH.md
@.planning/phases/04-inicio-recargas/04-03-SUMMARY.md
@.planning/phases/04-inicio-recargas/04-04-SUMMARY.md
@.planning/phases/04-inicio-recargas/04-06-SUMMARY.md

# The reference page composition shapes:
@src/app/(protected)/bonos/page.tsx
@src/app/(protected)/inicio/page.tsx
@src/lib/url-state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace recargas/page.tsx with full composition</name>
  <files>src/app/(protected)/recargas/page.tsx</files>
  <action>
    REPLACE the entire contents (current is a Phase 1 placeholder Card). Mirror `bonos/page.tsx` line-for-line as the structural skeleton; differences are imports + the dual-period filter for delta + 2 hechos curados rendering.

    1. **`export const dynamic = "force-dynamic";`** — required for live Sheet reads.

    2. **`export const metadata = { title: "Recargas · Tikin Dashboard" };`**

    3. **Imports** — exactly:
       - From `@/components/ui/card`: Card, CardContent, CardHeader, CardTitle.
       - From `@/components/recargas/RecargasKPICards`: RecargasKPICards.
       - From `@/components/recargas/RecargasTrendChart`: RecargasTrendChart.
       - From `@/components/recargas/RecargasTable`: RecargasTable.
       - From `@/components/recargas/HechosCuradosRecargas`: HechosCuradosRecargas.
       - From `@/lib/domain/recargas`: filterRecargas, summarizeRecargas, aggregateRecargasByDate, aggregateRecargasByEmpresa, top10RecargasEmpresas, findTopEmpresaRecargadora, findRecargaMasGrande.
       - From `@/lib/domain/period`: computePriorPeriod.
       - From `@/lib/sheets/transactions`: getCachedTransactions.
       - From `@/lib/url-state`: parseFilters.

    4. **PageProps & default async function** signature: same shape as bonos/page.tsx.

    5. **Composition flow**:
       ```ts
       const params = await searchParams;
       const filters = parseFilters(params);
       const priorWindow = computePriorPeriod(filters);

       let txResult;
       try {
         txResult = await getCachedTransactions();
       } catch (err) {
         return <Card>...err.message inline fallback...</Card>;  // mirror bonos/page.tsx
       }

       const allTx = txResult.rows;

       // Current period
       const currentRecargas = filterRecargas(allTx, filters);

       // Prior period (same data, different window)
       const priorRecargas = priorWindow
         ? filterRecargas(allTx, { ...filters, from: priorWindow.from, to: priorWindow.to })
         : null;

       const summary = {
         current: summarizeRecargas(currentRecargas),
         prior: priorRecargas ? summarizeRecargas(priorRecargas) : null,
       };

       const trendData = aggregateRecargasByDate(currentRecargas);
       const byEmpresa = aggregateRecargasByEmpresa(currentRecargas);
       const top10 = top10RecargasEmpresas(byEmpresa);

       // Hechos curados
       const topEmpresa = findTopEmpresaRecargadora(byEmpresa);
       const recargaMasGrande = findRecargaMasGrande(currentRecargas);
       ```

    6. **JSX render**:
       - Outer: `<div className="space-y-6">`
       - `<RecargasKPICards summary={summary} />`
       - Trend chart Card: `<Card><CardHeader><CardTitle>Recargas en el tiempo</CardTitle></CardHeader><CardContent>{trendData.length < 2 ? <EmptyMessage /> : <RecargasTrendChart data={trendData} />}</CardContent></Card>`
       - `<RecargasTable rows={top10} />` (already wrapped in Card internally per Plan 04-06).
       - `<HechosCuradosRecargas topEmpresa={topEmpresa} recargaMasGrande={recargaMasGrande} />` (wrapper already carries data-presenter-empresa-hide per Plan 04-06).

    7. **Empty-state behavior** (mirror bonos/page.tsx:103-117):
       - When `currentRecargas.length === 0`, KPICards still render zero values, chart Card shows empty message, table shows empty message, HechosCurados show their own empty-state copies. NO short-circuit with a single empty Card.

    8. **DO NOT call `verifySession()`** — layout handles it.

    9. Add top-level JSDoc (~12 lines) explaining: (a) Recargas filters by RECHARGE_TIPOS in domain, not at the URL level; (b) cliente-foco hides hechos curados via CSS (no React conditional); (c) why this is simpler than /inicio (no take rate, no presenter-only KPIs).
  </action>
  <verify>
    1. `npm run build` succeeds.
    2. `npx tsc --noEmit` passes.
    3. `grep -c "filterRecargas" src/app/\\(protected\\)/recargas/page.tsx` === 2 (current + prior).
    4. `dynamic = "force-dynamic"` present.
    5. Page renders locally: `npm run dev` → visit /recargas after login.
    6. `grep -c "data-presenter-hide\\|data-presenter-empresa-hide" src/app/\\(protected\\)/recargas/page.tsx` === 0 (page itself adds NO visibility attributes — leaves handle their own).
  </verify>
  <done>
    Page renders live recargas end-to-end. Build clean. No regression on /bonos, /payouts, /inicio.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    /recargas page composed with live RECHARGE_TIPOS-filtered data: 2 KPIs with deltas, 1 trend chart, top-10 table, 2 hechos curados. Cliente-foco hides only the HechosCuradosRecargas wrapper.
  </what-built>
  <how-to-verify>
    Run: `npm run dev` and log in. Verify each URL state:

    1. **`http://localhost:3000/recargas`** (no filters)
       - 2 KPI cards visible (Total $ recargado, # transacciones), each with DeltaBadge
       - Trend chart visible
       - Top 10 table visible (empresas with most $ recargado)
       - 2 hechos curados visible (top empresa recargadora, recarga más grande)
       - No Infinity%, NaN%, malformed strings

    2. **`http://localhost:3000/recargas?presenter=1`** (presenter, no empresa)
       - Same as default — Recargas has no Comisión/Take rate to hide.
       - Chrome (header, tabs) hidden via existing presenter-hide CSS.

    3. **`http://localhost:3000/recargas?empresa=$mario`** (empresa filter)
       - 2 KPIs filtered to Mario's recargas
       - Trend chart shows Mario's recargas only
       - Table shows Mario's row only (or empty if no recargas)
       - 2 hechos curados show Mario-related (top empresa = Mario; recarga más grande from Mario's recargas)

    4. **`http://localhost:3000/recargas?presenter=1&empresa=$mario`** (CLIENTE-FOCO)
       - 2 KPIs visible
       - Trend chart visible
       - Table visible (Mario's row, fine for cliente to see their own data)
       - **2 hechos curados HIDDEN** (HechosCuradosRecargas wrapper has data-presenter-empresa-hide)

    5. **Empty state:** `?from=2026-12-01&to=2026-12-31` → KPIs show $ 0 / 0, chart "Sin datos…", table "Sin recargas en el período", hechos show "Sin recargas en el período". Page renders without crash.

    6. **No regression:** /bonos, /payouts, /inicio, /clientes all render as before.

    Resume with "approved" or describe issues precisely.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Deploy to Vercel production after approval</name>
  <files>(none — deployment artifact)</files>
  <action>
    Deploy to production:
    ```bash
    vercel --prod
    ```
    Capture deployment URL + ID. Verify with `curl -I` against /login (expect 200). Spot-check production /recargas with at least 1 cliente-foco URL.

    Phase 4 SHIPS at this point: /inicio (Plan 04-07) + /recargas (this plan) both live. Phase 5 (Clientes + Domain) is the next milestone.
  </action>
  <verify>
    1. `vercel ls | head -3` shows the new deployment.
    2. Production /recargas renders correctly (with valid auth cookie).
    3. /inicio still renders correctly (no regression from this deploy).
  </verify>
  <done>
    Production deploy URL captured. Phase 4 ships.
  </done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds
- [ ] `npx tsc --noEmit` passes
- [ ] All 4 cliente-foco URL states verified (checkpoint approval)
- [ ] Empty state renders sanely
- [ ] No regression on /bonos, /payouts, /inicio
- [ ] Production deploy succeeds
- [ ] Phase 4 SHIPS (both /inicio and /recargas live)
</verification>

<success_criteria>
- All 7 must_have truths verified
- REC-01, REC-02, REC-03 covered
- Cliente-foco hides only hechos (table is fine — cliente sees their own data)
- Phase 5 has both /inicio and /recargas to point CLI-08 / CLI-07 at
</success_criteria>

<output>
After completion, create `.planning/phases/04-inicio-recargas/04-08-SUMMARY.md` documenting:
- Final page.tsx line count + structural mirror to bonos/page.tsx
- Production deployment URL + ID
- Live data observations (count of recargas in default view, top empresa recargadora, biggest recarga)
- Cliente-foco verification results
- Any deviations from the plan
- Phase 4 ships status: confirm both /inicio and /recargas live in production, and update ROADMAP.md Phase 4 to ✅ Complete
</output>
