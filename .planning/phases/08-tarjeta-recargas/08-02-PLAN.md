---
phase: 08-tarjeta-recargas
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/(protected)/uso-tarjeta/page.tsx
  - src/components/uso-tarjeta/KPICardsCardUsage.tsx
  - src/components/uso-tarjeta/AdoptionCard.tsx
  - src/components/uso-tarjeta/PurchaseTrendChart.tsx
  - src/components/uso-tarjeta/TopCardUsers.tsx
  - src/components/layout/tab-nav.tsx
autonomous: false

must_haves:
  truths:
    - "TabNav muestra 6 entries en orden: Inicio, Bonos, Payouts, Uso Tarjeta, Clientes, Recargas"
    - "Navigating to /uso-tarjeta renders KPI header + adoption + trend + top users — no 404, no hydration error"
    - "KPI header shows totalCompras, volumenCOP, ticketPromedio with section accent text-section-tarjeta on the primary"
    - "AdoptionCard renders adoptionRate as percent + numerator/denominator subtext"
    - "PurchaseTrendChart renders Recharts time series (count + amount dual-axis OR amount-only — implementer's choice within plan boundaries)"
    - "TopCardUsers table renders ≤10 rows ranked by volumenCOP desc, with tikintag + empresa + count + volumen + ticketPromedio columns"
  artifacts:
    - path: src/app/(protected)/uso-tarjeta/page.tsx
      provides: "Uso Tarjeta tab page composition (Server Component)"
      min_lines: 100
      contains: "filterPurchases"
    - path: src/components/uso-tarjeta/KPICardsCardUsage.tsx
      provides: "Top KPI strip — 3 cards (compras / volumen / ticket promedio)"
      min_lines: 40
    - path: src/components/uso-tarjeta/AdoptionCard.tsx
      provides: "Adoption rate card — % + numerator/denominator"
      min_lines: 30
    - path: src/components/uso-tarjeta/PurchaseTrendChart.tsx
      provides: "Recharts time-series chart"
      min_lines: 60
    - path: src/components/uso-tarjeta/TopCardUsers.tsx
      provides: "Top 10 card users table"
      min_lines: 50
    - path: src/components/layout/tab-nav.tsx
      provides: "TabNav with 6 entries (Uso Tarjeta inserted, Recargas reordered last)"
      contains: '"/uso-tarjeta"'
  key_links:
    - from: src/app/(protected)/uso-tarjeta/page.tsx
      to: src/lib/domain/cardUsage.ts
      via: "imports filterPurchases + summarizePurchases + 3 aggregations from Plan 08-01"
      pattern: 'from "@/lib/domain/cardUsage"'
    - from: src/app/(protected)/uso-tarjeta/page.tsx
      to: src/lib/sheets/transactions.ts
      via: "getCachedTransactions per-request fetch"
      pattern: "getCachedTransactions"
    - from: src/components/layout/tab-nav.tsx
      to: src/app/(protected)/uso-tarjeta/page.tsx
      via: "TabNav Link href=/uso-tarjeta routes to the new page"
      pattern: '"/uso-tarjeta"'
---

<objective>
Build the `/uso-tarjeta` page composition + 4 leaf components + TabNav update so users can navigate to and consume Uso Tarjeta in v2.0.

Purpose: Closes CARD-V2-01..06 (entire Uso Tarjeta requirement set). First protected route added in v2.0 (every other tab is a v1 carryforward — Uso Tarjeta is greenfield).

Output:
- New page `src/app/(protected)/uso-tarjeta/page.tsx`
- 4 new leaf components in `src/components/uso-tarjeta/`
- 1 surgical edit to `src/components/layout/tab-nav.tsx` (TABS array reorder + add)
- Visual checkpoint approval

Layout pattern: ADOPTION-FIRST cockpit — KPI strip (3 numeric KPIs) + AdoptionCard (the headline metric per CARD-V2-04) prominently above the fold; trend chart full-width below; top users table at bottom. Mirror's Plan 07-04's time-first cockpit shape (PRIMARY KPI + supporting metrics + diagnostic layer) — adapted for adoption being the conversation-driver in customer meetings.

Section accent: `text-section-tarjeta` (Amber) on the primary headline metric. Status accents reused from Phase 6 paleta where applicable (none expected — Uso Tarjeta has no semáforo per CROSS-V2-05).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-tarjeta-recargas/08-01-SUMMARY.md

@src/app/(protected)/recargas/page.tsx
@src/app/(protected)/bonos/page.tsx
@src/components/layout/tab-nav.tsx
@src/components/bonos/KPICardsV2.tsx
@src/components/payouts/PayoutsKPICardsV2.tsx
@src/lib/format.ts
@src/lib/url-state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 4 leaf components</name>
  <files>
    src/components/uso-tarjeta/KPICardsCardUsage.tsx,
    src/components/uso-tarjeta/AdoptionCard.tsx,
    src/components/uso-tarjeta/PurchaseTrendChart.tsx,
    src/components/uso-tarjeta/TopCardUsers.tsx
  </files>
  <action>
    Create the directory `src/components/uso-tarjeta/` and 4 leaves. Visual conventions:
    - Server Components by default (only mark "use client" if Recharts requires it — it does
      for the trend chart; the other 3 stay server)
    - shadcn `Card` / `CardHeader` / `CardTitle` / `CardContent` primitives
    - Format helpers: `formatCOP`, `formatInteger`, `formatPercent` from `@/lib/format`
    - Section accent: `text-section-tarjeta` for the headline number on the page's most
      prominent metric (KPICards primary OR AdoptionCard — implementer chooses ONE, not both)

    **KPICardsCardUsage.tsx** (Server Component):
    - Props: `{ summary: PurchaseSummary }` (type from `@/lib/domain/cardUsage`)
    - 3 cards in `grid grid-cols-1 md:grid-cols-3 gap-4`:
      1. "Compras totales" → `formatInteger(summary.totalCompras)`
      2. "Volumen" → `formatCOP(summary.volumenCOP)`
      3. "Ticket promedio" → `formatCOP(summary.ticketPromedio)`
    - Apply `text-section-tarjeta` on the primary card's number — pick "Compras totales" as
      primary (first card, eye-magnet position).
    - All numbers `text-3xl` or `text-4xl font-bold`.

    **AdoptionCard.tsx** (Server Component):
    - Props: `{ adoption: PurchaseAdoption }`
    - Single Card with title "Adopción de tarjeta"
    - Big percentage: `formatPercent(adoption.adoptionRate)` (text-4xl, can use `text-section-tarjeta`
      if NOT used in KPICards — pick exactly ONE place for the section accent on this page).
    - Subtext: `${adoption.usersWithPurchase} de ${adoption.totalUsers} usuarios`
    - When `adoption.totalUsers === 0` → render "Sin datos" placeholder (zero-safe).
    - JSDoc cites CARD-V2-04 + PRD baseline.

    **PurchaseTrendChart.tsx** (Client Component — `"use client"`):
    - Props: `{ data: PurchaseByDate[] }`
    - Recharts `LineChart` or `BarChart` (implementer's choice — line is recommended for trend
      readability) with X = `date`, Y = `compras` (count). Single-series — keep simple.
    - Optional: secondary line for `volumenCOP` if dual-axis is straightforward; if it adds
      hydration/layout complexity, omit.
    - Empty `data` → render `"Sin datos suficientes"` Card body, NOT a broken chart.
    - X-axis tick formatter: short date (DD/MM or DD-Mon).
    - Tooltip formatter: locale-aware date + integer count + COP volume.
    - No granularity switcher in this plan — domain emits daily; switching is a future enhancement.

    **TopCardUsers.tsx** (Server Component):
    - Props: `{ rows: TopCardUser[] }`
    - Raw `<table>` markup (per Plan 07-02 deviation note — repo has no shadcn `ui/table`
      primitive; matches v1/v2 idiom across the codebase). 5 columns:
      `Tikintag | Empresa | Compras | Volumen | Ticket promedio`.
    - Empty rows → `"Sin compras en el período seleccionado"` placeholder.
    - Numeric columns right-aligned. `tabular-nums` on numeric `<td>`.

    **No extra components**: no purple-cow visual flourishes. Keep parity with the Phase 7
    cockpit composition density.

    NO eager imports of unused types. NO `any`. NO defensive null-checks where types already
    guarantee shape.
  </action>
  <verify>
    - `tsc --noEmit` passes
    - `next lint` passes 0 errors
    - All 4 files exist and export the named component
    - File sizes meet `min_lines` in must_haves
    - Exactly ONE card carries `text-section-tarjeta` on its primary number across the page
      (not all four — pick one focal point)
  </verify>
  <done>4 leaves build, lint, tsc clean. Section accent applied surgically. Recharts client component does not crash on empty data.</done>
</task>

<task type="auto">
  <name>Task 2: Compose /uso-tarjeta page + update TabNav</name>
  <files>
    src/app/(protected)/uso-tarjeta/page.tsx,
    src/components/layout/tab-nav.tsx
  </files>
  <action>
    **Create `src/app/(protected)/uso-tarjeta/page.tsx`** (Server Component):

    Imports:
    ```ts
    import {
      filterPurchases,
      summarizePurchases,
      aggregatePurchaseAdoption,
      aggregatePurchasesByDate,
      aggregateTopCardUsers,
    } from "@/lib/domain/cardUsage";
    import { getCachedTransactions } from "@/lib/sheets/transactions";
    import { parseFilters } from "@/lib/url-state";
    import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
    // 4 leaves
    ```

    Mirror the `recargas/page.tsx` v1 server-side composition shape:
    - `export const dynamic = "force-dynamic"` (live-read per request, project standard)
    - `searchParams` via Next 15 async props convention (await + parseFilters)
    - Single `getCachedTransactions()` call → returns Result-shape: handle the `error` branch
      with the existing v1 error Card pattern from `recargas/page.tsx`
    - `const allTx = txResult.data` (post-period scoping happens via `filterPurchases`
      internally; for adoption denominator we want period-filtered allTx — see note below)
    - `const purchaseRows = filterPurchases(allTx, filters)`
    - `const summary = summarizePurchases(purchaseRows)`
    - `const adoption = aggregatePurchaseAdoption(allTx, purchaseRows)`
    - `const trendData = aggregatePurchasesByDate(purchaseRows)`
    - `const topRows = aggregateTopCardUsers(purchaseRows, 10)`

    **Adoption denominator note**: Plan 08-01 defined the API as
    `aggregatePurchaseAdoption(allTx, purchaseRows)` and noted the page is the budget-keeper.
    For Phase 8 we keep `allTx` = the FULL `getCachedTransactions().data` (NOT period-pre-filtered).
    Rationale: PRD baseline 40/235 is a global-pool reading, and the page-level filters in
    practice scope to recent windows — period-filtering the denominator would inflate adoption
    artificially during short windows. Document this decision in the page-level JSDoc;
    Phase 9 Vista Cliente may revisit if a per-period denominator becomes desirable.

    **Page layout** (return JSX):
    ```
    <main className="space-y-6 p-6">
      <header>
        <h1>Uso Tarjeta</h1>
        <p className="text-muted-foreground">Compras con tarjeta en el período</p>
      </header>

      <KPICardsCardUsage summary={summary} />

      <AdoptionCard adoption={adoption} />

      <Card>
        <CardHeader><CardTitle>Tendencia de uso</CardTitle></CardHeader>
        <CardContent>
          <PurchaseTrendChart data={trendData} />
        </CardContent>
      </Card>

      <Card>
        <CardHeader><CardTitle>Top usuarios por uso de tarjeta</CardTitle></CardHeader>
        <CardContent>
          <TopCardUsers rows={topRows} />
        </CardContent>
      </Card>
    </main>
    ```

    Empty-state: when `purchaseRows.length === 0`, the leaves render their own zero-state
    placeholders — page does NOT short-circuit with a single empty Card. Same pattern as
    bonos/recargas v1.

    Cliente-foco visibility: NO `data-presenter-*` attributes on this page in this plan.
    Uso Tarjeta is presenter-friendly by default (CROSS-V2-07 conservative-default policy).

    **Update `src/components/layout/tab-nav.tsx`**:
    Edit the TABS array — single change:

    ```ts
    const TABS: { href: string; label: string }[] = [
      { href: "/inicio", label: "Inicio" },
      { href: "/bonos", label: "Bonos" },
      { href: "/payouts", label: "Payouts" },
      { href: "/uso-tarjeta", label: "Uso Tarjeta" },  // NEW (CARD-V2-*)
      { href: "/clientes", label: "Clientes" },
      { href: "/recargas", label: "Recargas" },
    ];
    ```

    Order matches PRD v2 reading order (Inicio · Bonos · Payouts · Uso Tarjeta · Vista Cliente · Recargas).
    No other changes to tab-nav.tsx — buildUrl preserves filters automatically (the existing
    pattern handles the new tab without code change).

    Run end-to-end checks before checkpoint:
    - `npm run build` succeeds
    - `next lint` 0 errors
    - `tsc --noEmit` 0 errors
    - `git diff --stat` shows the 6 files in `files_modified` (4 new leaves + 1 new page +
      1 tab-nav edit) and ZERO others.
  </action>
  <verify>
    - All builds green
    - `curl -fsS http://localhost:3000/uso-tarjeta -o /dev/null` returns 2xx after `npm run dev` starts
      (deferred to checkpoint — Claude doesn't run dev server in CI; user verifies)
    - TabNav shows 6 entries in order; clicking "Uso Tarjeta" routes to /uso-tarjeta with
      filters preserved.
  </verify>
  <done>Page composes, TabNav updated, build/lint/tsc all green. Ready for visual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    /uso-tarjeta tab — first new section in v2.0. KPI header (compras + volumen + ticket promedio)
    + Adoption card + Recharts trend chart + Top 10 card users table. TabNav updated to 6 entries
    in PRD order.
  </what-built>
  <how-to-verify>
    1. Run: `npm run dev` (Next dev server)
    2. Visit: http://localhost:3000/uso-tarjeta (auth via shared password if not already in)
    3. Confirm TabNav shows: Inicio · Bonos · Payouts · **Uso Tarjeta** · Clientes · Recargas
       in that order. Clicking each tab preserves filters in the URL.
    4. With default filters (e.g. last 30 days), confirm KPI header shows non-zero numbers
       OR the empty-state placeholders. Total compras + volumen + ticket promedio all visible.
    5. Confirm Adoption card shows a percentage with `usersWithPurchase / totalUsers` subtext.
    6. Confirm trend chart renders without console errors (open DevTools → Console). When
       hovering, tooltip shows date + count.
    7. Confirm Top users table shows ≤ 10 rows, sorted by volumen DESC, with tikintag + empresa
       + counts + COP figures. Numeric columns right-aligned with tabular-nums.
    8. Toggle dark mode (next-themes ThemeToggle in header) → confirm `text-section-tarjeta`
       still reads correctly in both modes (Amber CSS var has dark-mode lift per Plan 06-04).
    9. Toggle Modo Presentación (presenter=1) → TabNav should HIDE (existing
       `data-presenter-hide` rule from v1). Page content stays visible.
    10. Try a `?empresa=$X` URL (cliente-foco) → page renders the same metrics filtered to
        that empresa (filterPurchases honors empresa filter). No `presenter-empresa-hide`
        on this page in Phase 8 (Phase 9 may revisit if dual-purpose visibility per metric
        becomes a CLI-V2 requirement for Uso Tarjeta — out of scope here).
    11. Visual: section accent appears on EXACTLY ONE focal metric (not all). Layout reads
        clean at desktop (≥1024px) and stacks responsively at mobile (375px) with no
        horizontal overflow.
  </how-to-verify>
  <resume-signal>Type "approved" to continue (commits + SUMMARY), or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` returns 0 errors
- [ ] `npx next lint` returns 0 errors
- [ ] `npm run build` succeeds
- [ ] User-approved visual checkpoint (live `/uso-tarjeta` rendering verified in browser)
- [ ] `git diff --name-only` shows EXACTLY the 6 files in files_modified, no others
- [ ] TabNav contains 6 entries in PRD-prescribed order
</verification>

<success_criteria>
- /uso-tarjeta route LIVE and rendering KPI + adoption + trend + top users
- TabNav updated to 6 tabs in PRD order
- All CARD-V2-01..06 requirements demonstrably satisfied via UI
- Build/lint/tsc green
- User approves visual checkpoint
- Commits atomic via explicit pathspec (sibling 08-04 may be racing on recargas page)
</success_criteria>

<output>
After completion, create `.planning/phases/08-tarjeta-recargas/08-02-SUMMARY.md` covering:
- 4 leaves built (file paths + LOC each)
- Page composition layout chosen (where the section accent landed)
- TabNav update (1-line semantic change)
- Adoption denominator decision (full pool, NOT period-filtered) and rationale
- Any deviations (deviation rules from execute-plan.md if applied)
- Parallel-wave race observations (sibling 08-04 may have edits to recargas page in worktree)
- Visual checkpoint feedback summary
- Lines added / files added / files modified
</output>
