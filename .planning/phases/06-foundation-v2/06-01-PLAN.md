---
phase: 06-foundation-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/domain/parsers.ts
  - src/lib/domain/schemas.ts
autonomous: true

must_haves:
  truths:
    - "parseAging(s) returns minutes for valid PostgreSQL interval strings; NaN for unparseable input; 0 for empty string"
    - "parseTotalTime(s) returns minutes for valid PostgreSQL interval strings; NaN for unparseable input; 0 for empty string"
    - "parseCOPAmount(s) returns a finite number for 'COP X,XXX.XX' strings, including negatives, zeros and pre-trimmed empties"
    - "Existing PayoutRowSchema continues to validate 798 production rows after refactor (no regression)"
  artifacts:
    - path: "src/lib/domain/parsers.ts"
      provides: "Public parsing utilities for v2.0 domain libraries (parseAging, parseTotalTime, parseCOPAmount)"
      min_lines: 80
      exports: ["parseAging", "parseTotalTime", "parseCOPAmount"]
    - path: "src/lib/domain/schemas.ts"
      provides: "Refactored to delegate to parsers.ts; keeps internal seconds-based parsing for Payout.latencySeconds backward-compat"
      contains: "from \"./parsers\""
  key_links:
    - from: "src/lib/domain/schemas.ts"
      to: "src/lib/domain/parsers.ts"
      via: "import parseCOPAmountRaw / parsePgIntervalSeconds (internal helpers used by MoneyFromCOP and parsePgInterval Zod transforms)"
      pattern: "from\\s+\"\\./parsers\""
---

<objective>
Expose parsing primitives for v2.0 domain libraries as a public API, in the units the PRD v2 expects (minutes for time, number for COP).

Purpose: CROSS-V2-03 declara que `parseAging()`, `parseTotalTime()` y `parseCOPAmount()` deben existir como utilidades reusables. Hoy esas funciones viven enterradas dentro de Zod schemas como helpers privados (`parsePgInterval` retorna seconds; `MoneyFromCOP` es un Zod transform). Phase 7+ van a llamar estas funciones directamente — necesitan API pública en MINUTOS para tiempos (no seconds) sin romper consumers existentes que esperan seconds via `Payout.latencySeconds`.

Output: `src/lib/domain/parsers.ts` con tres funciones puras documentadas + JSDoc. `schemas.ts` refactorizado para delegar a parsers.ts manteniendo el contrato seconds-based para `latencySeconds`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-foundation-v2/06-CONTEXT.md

# Existing code to reference (parsing logic already proven against 798 prod rows):
@src/lib/domain/schemas.ts
@src/lib/domain/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create parsers.ts with public parsing API</name>
  <files>src/lib/domain/parsers.ts</files>
  <action>
Create `src/lib/domain/parsers.ts` exporting three pure functions:

1. **`parsePgIntervalSeconds(s: unknown): number`** (internal — kept seconds-based for backward-compat)
   - Move logic verbatim from `schemas.ts:parsePgInterval` (lines ~317-333).
   - Same regex: `/(-?\d+)\s+years?\s+(-?\d+)\s+mons?\s+(-?\d+)\s+days?\s+(-?\d+)\s+hours?\s+(-?\d+)\s+mins?\s+(-?\d+(?:\.\d+)?)\s+secs?/i`
   - Same factors: 365.25 d/y, 30.44 d/mo (defensive — real data has y=mo=0).
   - Empty string → 0. Non-string → NaN. Format mismatch → NaN.

2. **`parseAging(s: unknown): number`** (public — returns MINUTES per PRD v2)
   - Wraps `parsePgIntervalSeconds(s)`.
   - Returns `seconds / 60` when finite; preserves NaN.
   - JSDoc: cite CROSS-V2-03; document that `Aging` is always present in BD_Payouts (row's age = now - created_at) and is the fallback for `latencySeconds` when `Total Time` is empty.

3. **`parseTotalTime(s: unknown): number`** (public — returns MINUTES per PRD v2)
   - Wraps `parsePgIntervalSeconds(s)`.
   - Returns `seconds / 60` when finite; preserves NaN; empty string → 0.
   - JSDoc: cite CROSS-V2-03; document that `Total Time` is empty for in_progress/failed payouts and is the canonical end-to-end latency for completed payouts.

4. **`parseCOPAmount(s: unknown): number | null`** (public — returns number)
   - Strip non-digit/decimal/sign chars (`/[^0-9.\-]/g`).
   - Empty cleaned string → `null` (not NaN — explicit "no value" signal for v2.0 callers).
   - `Number(cleaned)` non-finite → `null`.
   - Otherwise return the number.
   - Accepts: `"COP 200,000.00"` → `200000`, `"COP -5,229.46"` → `-5229.46`, `"COP 0.00"` → `0`, `""` → `null`, number input → echo if finite else `null`.
   - JSDoc: cite CROSS-V2-03; explicitly enumerate the four edge cases (negativos, ceros, vacíos, NaN/Infinity).

5. Add module-level JSDoc explaining: this module is the public parsing API for v2.0 domain libraries (Phases 7-10 import from here). The Zod schemas in `schemas.ts` continue to use the internal seconds-based helpers for backward compatibility with `Payout.latencySeconds`.

Do NOT add tests in this task — verify via `tsc --noEmit` and inline diagnostic in Task 2 verify.
Do NOT change function signatures over time without bumping the import path; v2.0 phases import these by name.
  </action>
  <verify>
- File exists at `src/lib/domain/parsers.ts` with three exports: `parseAging`, `parseTotalTime`, `parseCOPAmount`
- `npx tsc --noEmit` succeeds (no TypeScript errors)
- `grep -c "^export function" src/lib/domain/parsers.ts` returns 3 (or 4 if `parsePgIntervalSeconds` is exported for schemas.ts; both are acceptable)
  </verify>
  <done>parsers.ts exists with public API that returns minutes for time parsers and number/null for COP. tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor schemas.ts to delegate to parsers.ts</name>
  <files>src/lib/domain/schemas.ts</files>
  <action>
Refactor `src/lib/domain/schemas.ts` so the existing private `parsePgInterval` and `MoneyFromCOP` helpers delegate to the new public API in `parsers.ts`. Goal: NO behavior change for existing consumers (PayoutRowSchema must continue producing the same `Payout.latencySeconds` in seconds for the 798 production rows).

Steps:
1. Add at the top: `import { parseCOPAmount, parsePgIntervalSeconds } from "./parsers";`
   - If you exported `parsePgIntervalSeconds` from parsers.ts, import it. If not, keep the private function in schemas.ts as a thin wrapper around what's in parsers.ts (re-export-like). Prefer the import path.

2. Replace the body of the local `parsePgInterval(s: unknown): number` function with a one-liner: `return parsePgIntervalSeconds(s);` OR delete the local function entirely and replace its 2 call sites in `PayoutRowSchema` (the `aging` and `total time` field transforms) with `parsePgIntervalSeconds(v)`.

3. Replace `MoneyFromCOP`'s transform body with a thin wrapper around `parseCOPAmount`:
   - If parseCOPAmount returns `null`, addIssue + return z.NEVER (preserves the existing "skip row on bad money" semantics).
   - If parseCOPAmount returns a finite number, return it directly.
   - Number input fast-path stays (existing `if (typeof v === "number")` branch).

4. Verify the contract is preserved: PayoutRowSchema's `latencySeconds` still receives SECONDS (not minutes). The `total_time > 0 ? total_time : aging` fallback in the outer transform must keep working with the same numeric magnitude.

5. Do NOT touch `TransactionRowSchema` (BD_Plataforma). It doesn't use these helpers — only PayoutRowSchema does.
6. Do NOT change exported types or symbols from schemas.ts. Phase 3 consumers (`PayoutRowSchema`, `ExpectedPayoutHeaders`) keep the same surface.
  </action>
  <verify>
- `npx tsc --noEmit` succeeds
- `npm run build` succeeds (Next.js compile)
- `npm run lint` succeeds (no new ESLint errors)
- `grep "parsePgInterval\b" src/lib/domain/schemas.ts | wc -l` should drop (or the function becomes a one-line delegate)
- Manual trace: read `src/lib/domain/payouts.ts` line ~118 (`p50Seconds: number`); confirm the JSDoc still says "seconds" — backward-compat must not silently flip units
  </verify>
  <done>schemas.ts refactored to delegate to parsers.ts. No behavior change for PayoutRowSchema. tsc + build + lint pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes (zero errors)
- [ ] `npm run build` succeeds
- [ ] `npm run lint` passes (no new errors)
- [ ] `src/lib/domain/parsers.ts` exists and exports `parseAging`, `parseTotalTime`, `parseCOPAmount`
- [ ] Inline trace check: parsers in MINUTES (CROSS-V2-03) vs schemas in SECONDS (Payout.latencySeconds backward-compat) — confirm by reading 3-5 lines of each
</verification>

<success_criteria>
- All tasks completed with atomic commits
- Public parsing API exposed for Phase 7+ to consume
- Zero regression in PayoutRowSchema (existing 798-row contract preserved)
- Module-level JSDoc clearly distinguishes the public-minutes API from the internal-seconds backward-compat path
</success_criteria>

<output>
After completion, create `.planning/phases/06-foundation-v2/06-01-SUMMARY.md`. Required frontmatter fields: phase, subsystem (= "parsing"), provides (= ["parseAging", "parseTotalTime", "parseCOPAmount"]), affects (= ["07-bonos-payouts", "08-tarjeta-recargas", "09-vista-cliente", "10-inicio-infra"]), key-files, key-decisions, tech-stack.added (= []), patterns-established.
</output>
