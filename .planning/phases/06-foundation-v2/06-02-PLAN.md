---
phase: 06-foundation-v2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/domain/join.ts
autonomous: true

must_haves:
  truths:
    - "joinPayouts(transactions, payouts) returns a Map<transaction_id, Transaction> where every Payout's transactionId that exists in BD_Plataforma is mapped"
    - "Match rate against full prod data: 773/798 (96.9%) — verified via diagnostic script or page run"
    - "Empty inputs return an empty Map (no NaN, no throw)"
    - "Function is pure — no side effects, no Date.now, no env reads"
  artifacts:
    - path: "src/lib/domain/join.ts"
      provides: "Canonical JOIN helper between BD_Plataforma transactions and BD_Payouts payouts via transaction_id ↔ Transaction ID"
      min_lines: 60
      exports: ["joinPayouts", "JoinedPayout"]
  key_links:
    - from: "src/lib/domain/join.ts"
      to: "src/lib/domain/types.ts"
      via: "imports Transaction and Payout types"
      pattern: "from\\s+\"\\./types\""
---

<objective>
Formalize the canonical JOIN between BD_Plataforma transactions and BD_Payouts payouts as a reusable, well-documented helper.

Purpose: CROSS-V2-04 declara JOIN canónico `BD_Plataforma.transaction_id ↔ BD_Payouts.Transaction ID`. Hoy ese join vive ad-hoc dentro de `payouts/page.tsx` (líneas ~125-138) como un `Map<transactionId, empresa_id>` improvisado. Phases 7-9 lo necesitan para: enriquecer payouts con `empresa_id` (Vista Cliente CLI-V2-03), construir tablas de pagos a terceros (PAY-V2-08 — `Holder ≠ tikintag`), y cualquier sección que cruce las dos fuentes.

Verificación documentada en STATE.md: 773/798 (96.9%) match. La columna `reference` en BD_Plataforma contiene hashes blockchain (NO sirve como JOIN key) — el código usa `transaction_id`, JSDoc cita el nombre semántico `reference` del PRD por convención.

Output: `src/lib/domain/join.ts` con `joinPayouts()` puro y un tipo `JoinedPayout` que extiende `Payout` con `transaction?: Transaction` opcional.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-foundation-v2/06-CONTEXT.md

# Existing ad-hoc join site (the pattern this plan formalizes):
@src/app/(protected)/payouts/page.tsx

# Domain types:
@src/lib/domain/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement joinPayouts() canonical helper</name>
  <files>src/lib/domain/join.ts</files>
  <action>
Create `src/lib/domain/join.ts` exporting:

1. **Type `JoinedPayout`**:
   ```ts
   export interface JoinedPayout extends Payout {
     transaction?: Transaction; // The matched BD_Plataforma row, undefined when no match
   }
   ```

2. **Function `joinPayouts(transactions: Transaction[], payouts: Payout[]): JoinedPayout[]`**:
   - Build a `Map<string, Transaction>` keyed by `t.id` (which the schema sources from `BD_Plataforma.transaction_id`).
   - Iterate `payouts` once; for each `p`, look up `byId.get(p.transactionId)`.
   - Return new array `[...payouts.map(p => ({ ...p, transaction }))]`.
   - PURE — never mutate inputs.
   - Empty inputs: empty `transactions` returns `[...payouts]` with `transaction` always undefined; empty `payouts` returns `[]`.

3. **Helper `joinIndex(transactions: Transaction[]): Map<string, Transaction>`** (exported):
   - The `Map` builder factored out so callers that already own a transactions list can build the index once and reuse for many lookups (e.g., row-by-row UI rendering).

4. **Match-rate counter `joinMatchStats(transactions: Transaction[], payouts: Payout[]): { matched: number; unmatched: number; total: number; rate: number }`**:
   - Returns counts for diagnostics. The verify step uses this against real Sheets data.
   - `rate = total > 0 ? matched/total : 0`.

5. **Module-level JSDoc**:
   - Explain the convention: code uses `transaction_id` (the technical column name), PRD uses semantic name `reference`.
   - Document the historic verification: 773/798 (96.9%) match against production data captured 2026-05-04. Cite `BD_Plataforma.reference` is hex hashes (blockchain), NOT the JOIN key.
   - Cite use-cases: empresa enrichment (CLI-V2-03), pagos a terceros (PAY-V2-08, `Holder ≠ tikintag`), Vista Cliente JOIN (CLI-V2-03..07).

6. **Replace ad-hoc usage in `payouts/page.tsx`** (OPTIONAL — do this in this plan if the diff stays small):
   - Lines ~125-138 currently build `txEmpresaByTransactionId` inline. Refactor to call `joinPayouts(txResult.rows, payoutsResult.rows)` and read `enriched[i].transaction?.empresa_id`.
   - If the refactor introduces ANY behavior change, SKIP this — leave a comment in `join.ts` JSDoc noting the migration is pending and Phase 7+ will adopt it. The plan's primary deliverable is the helper module; the refactor is a nice-to-have.

Do NOT add a test framework in this plan — Phase 6 has no tests yet. Verification is via `tsc --noEmit`, `npm run build`, and the diagnostic command in verify.
  </action>
  <verify>
- File exists at `src/lib/domain/join.ts` with named exports `joinPayouts`, `joinIndex`, `joinMatchStats`, type `JoinedPayout`
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- Match-rate verification: in dev (`npm run dev`), open the Payouts page (which loads both Sheets) and read browser console — OR add a temporary diagnostic route at `/api/diagnose/join` that calls `joinMatchStats` and returns JSON. Either path: confirm `matched >= 770` and `rate >= 0.96` against production data. Delete the diagnostic route immediately after if you created one.
  </verify>
  <done>join.ts exists with joinPayouts/joinIndex/joinMatchStats. tsc + build pass. Match rate verified ≥96% against prod data.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run build` succeeds
- [ ] `npm run lint` passes
- [ ] Diagnostic check confirms 773/798 (or close) match against production
- [ ] No diagnostic routes left in `src/app/api/` after verification
</verification>

<success_criteria>
- joinPayouts() is the single canonical helper for v2.0 cross-source joins
- JSDoc cites the 96.9% baseline and the technical-vs-semantic naming convention
- Build clean; no regressions
- (Optional) payouts/page.tsx refactored to use the helper, OR migration path documented in JSDoc
</success_criteria>

<output>
After completion, create `.planning/phases/06-foundation-v2/06-02-SUMMARY.md`. Required frontmatter: phase, subsystem (= "join"), provides (= ["joinPayouts", "joinIndex", "joinMatchStats", "JoinedPayout"]), affects (= ["07-bonos-payouts", "09-vista-cliente"]), key-files, key-decisions (mention the technical-vs-semantic naming convention), tech-stack.added (= []), patterns-established.
</output>
