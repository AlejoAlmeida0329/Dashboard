---
phase: 10-inicio-infra
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/app/(protected)/inicio/page.tsx
  - src/components/inicio/InicioKPIStripV2.tsx
  - src/components/inicio/TransactionTypeDonut.tsx
  - src/components/inicio/ActivityTimelineV2.tsx
  - src/components/inicio/TopUsersByVolume.tsx
  - src/lib/domain/inicio.ts
  - src/lib/domain/payouts.ts
  - src/lib/domain/inicio-hechos.ts
  - src/components/inicio/HechosCurados.tsx
  - src/components/inicio/KPICardsInicio.tsx
  - src/components/inicio/GMVTrendChart.tsx
  - src/components/inicio/EmpresasActivasChart.tsx
  - src/components/inicio/DeltaBadge.tsx
autonomous: false
must_haves:
  truths:
    - "/inicio renders 6-section operative cockpit: usuarios activos · volumen IN/OUT · tasa éxito · donut tipos · actividad temporal · top 10 usuarios"
    - "successRate KPI carries semáforo color (verde ≥95%, amber ≥85%, else rojo) per CROSS-V2-05 status palette"
    - "donut renders top 6 transaction types + Otros rollup (PRD INI-V2-04)"
    - "activity timeline supports day/week granularity selectable via URL searchParam (mirror v1 60-day threshold default OR explicit toggle)"
    - "top 10 users table grouped by tikintag (NOT empresa) — v1→v2 user-lens shift held"
    - "Indigo section accent (text-section-inicio) lands on EXACTLY ONE focal metric (one-section-accent-per-page rule)"
    - "v1 inicio.ts surface (filterCompletedIn + summarizeInicio + 4 GMV/empresa aggregations + types) deleted; v2 surface preserved byte-identical"
    - "v1 payouts.ts surface (filterPayouts + summarizePayouts + PayoutSummary + COMPLETED_PAYOUT_STATES) deleted — closes Phase 7-04 deferral docket"
    - "inicio-hechos.ts file deleted entirely (orphaned after page rewrite)"
    - "5 v1 inicio leaves deleted (HechosCurados + KPICardsInicio + GMVTrendChart + EmpresasActivasChart + DeltaBadge)"
  artifacts:
    - path: "src/app/(protected)/inicio/page.tsx"
      provides: "Inicio v2 operative-lens cockpit composition"
      contains: "summarizeInicioV2"
      min_lines: 120
    - path: "src/components/inicio/InicioKPIStripV2.tsx"
      provides: "3-card KPI strip: usuarios activos · volumen IN/OUT · tasa de éxito"
    - path: "src/components/inicio/TransactionTypeDonut.tsx"
      provides: "Donut chart for INI-V2-04 (top 6 + Otros)"
    - path: "src/components/inicio/ActivityTimelineV2.tsx"
      provides: "Distinct-tikintag-per-bucket line chart for INI-V2-05"
    - path: "src/components/inicio/TopUsersByVolume.tsx"
      provides: "Top-10 users by volumenNeto table for INI-V2-06"
  key_links:
    - from: "src/app/(protected)/inicio/page.tsx"
      to: "filterInicioV2 + summarizeInicioV2 + 3 v2 aggregations"
      via: "import from @/lib/domain/inicio (no v1 import survives)"
      pattern: "summarizeInicioV2"
    - from: "InicioKPIStripV2"
      to: "InicioSummaryV2"
      via: "props.summary"
      pattern: "InicioSummaryV2"
    - from: "src/lib/domain/inicio.ts after prune"
      to: "v2-only surface"
      via: "v1 block removed in cohesive refactor commit"
      pattern: "summarizeInicio[^V]"
---

<objective>
Rewrite `/inicio` as the v2 operative-lens cockpit and close the v2.0 milestone's last v1 prune debt in one cohesive refactor commit. Page composition consumes Plan 10-01's domain surface; all v1 components are deleted; the 4 deferred v1 payouts.ts symbols (Plan 07-04 docket) finally die.

Purpose: INI-V2-01..06 deliver the operative-lens home page (usuarios activos · volumen IN/OUT · tasa éxito semáforo · tipo donut · actividad temporal · top 10 users). After this plan ships, EVERY v2.0 page ships against the v2 surface and the 4-symbol payouts.ts coexistence layer (the only remaining v1→v2 debt) closes — milestone completion hinges on this plan.

Output:
- 1 page rewrite (`/inicio`)
- 4 new v2 leaf components in `src/components/inicio/`
- 5 v1 leaf component file deletions (HechosCurados, KPICardsInicio, GMVTrendChart, EmpresasActivasChart, DeltaBadge)
- 1 file deletion (`src/lib/domain/inicio-hechos.ts`)
- 2 module prunes (`inicio.ts` v1 block, `payouts.ts` 4 v1 symbols)
- ONE visual checkpoint at the end (gate to merge)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Plan 10-01 produces the domain surface this plan composes:
@.planning/phases/10-inicio-infra/10-01-SUMMARY.md

# Authoritative type contracts:
@src/lib/domain/types.ts
@src/lib/url-state.ts
@src/lib/format.ts

# Domain surface consumed:
@src/lib/domain/inicio.ts
@src/lib/domain/payouts.ts

# v1 page being replaced — read to understand what to migrate vs delete:
@src/app/(protected)/inicio/page.tsx

# v2 page composition pattern reference (mirror the structure):
@src/app/(protected)/recargas/page.tsx
@src/app/(protected)/uso-tarjeta/page.tsx

# v2 leaf composition reference (one-section-accent-per-page; raw <table>; OKLCH chart fills):
@src/components/recargas/RecargasKPICardsV2.tsx
@src/components/recargas/RecargasTrendChartV2.tsx
@src/components/recargas/TopRechargers.tsx
@src/components/uso-tarjeta/PurchaseTrendChart.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 4 new v2 leaf components (InicioKPIStripV2, TransactionTypeDonut, ActivityTimelineV2, TopUsersByVolume)</name>
  <files>
    src/components/inicio/InicioKPIStripV2.tsx,
    src/components/inicio/TransactionTypeDonut.tsx,
    src/components/inicio/ActivityTimelineV2.tsx,
    src/components/inicio/TopUsersByVolume.tsx
  </files>
  <action>
    All 4 leaves: pure presentational; consume types from `@/lib/domain/inicio` (Plan 10-01 v2 surface); use `@/lib/format` helpers; no Sheets/Next/router imports. Server Components by default; client only where Recharts requires DOM.

    **1. `InicioKPIStripV2.tsx` (Server Component, ~110-130 LOC):**
    - Props: `{ summary: InicioSummaryV2 }`
    - 3-card grid `grid grid-cols-1 gap-4 sm:grid-cols-3` (NOT 5-card like v1; PRD pivots to 3 protagonist KPIs):
      1. **Usuarios activos** (the conversation-driver): `formatInteger(summary.usuariosActivos)`. **Section accent here**: `text-section-inicio` headline + `text-4xl` (one-section-accent-per-page rule held — same pattern as `RecargasKPICardsV2` "Total recargas" first card; verify with `grep -rE "text-section-inicio" src/components/inicio/` after build → exactly 1 hit).
      2. **Volumen IN vs OUT** (single Card with two stacked stats): top line `formatCOP(summary.volumenIn)` labeled "Entradas"; below in muted `formatCOP(summary.volumenOut)` labeled "Salidas". `text-foreground` (NOT section accent — only one accent per page).
      3. **Tasa de éxito** (semáforo): `formatPercent(summary.successRate)`. Color via inline helper `successRateAccent(rate: number): string` returning a Tailwind class:
         - `>= 0.95` → `text-status-success`
         - `>= 0.85` → `text-status-pending` (amber)
         - else → `text-status-fail`
         (Mirror the helper convention from Phase 7-04 PayoutsKPICardsV2 — that file already defines an analogous `successRateAccent`; if the helper exists in a shared module, import; otherwise inline a 4-line copy here. STATE.md "successRateAccent" key term — search `src/components/payouts/` first, reuse if found.)
         Subtitle line: counts breakdown `formatInteger(completed) + " / " + formatInteger(failed) + " / " + formatInteger(inProgress)` with `text-xs text-muted-foreground`.
    - All values `tabular-nums` for column-stable rendering.

    **2. `TransactionTypeDonut.tsx` (Client Component, `"use client"`, ~120-150 LOC):**
    - Props: `{ buckets: TransactionTypeBucket[] }`
    - Recharts `PieChart` + `Pie` + `Cell` + `Tooltip` + `Legend`. `ResponsiveContainer width="100%" height={280}`.
    - 7 stable OKLCH fills (Indigo family with hue rotation for contrast — section accent's hue is `~250`, so use `oklch(0.55 0.18 250)`, `oklch(0.62 0.16 230)`, `oklch(0.55 0.16 200)`, `oklch(0.62 0.14 175)`, `oklch(0.55 0.14 150)`, `oklch(0.62 0.12 90)`, and the LAST color (for "Otros") fixed to `oklch(0.6 0.05 250)` — desaturated muted-Indigo so it visually reads as a tail). Mirror the `BonosFlowChart` / `RecargasTrendChartV2` OKLCH-pinned-fills convention so dark mode renders correctly without theme classes.
    - Legend below the pie; tooltip shows `{tipo}: {formatInteger(count)} ({formatPercent(share)})`.
    - Empty input → render `<p className="text-sm text-muted-foreground">Sin transacciones en el período.</p>` instead of an empty pie.
    - Localization label map (Spanish UX strings for Recharts tooltip): `BONUS → "Bonos"`, `P2P → "P2P"`, `PAYIN_PSE → "Recarga PSE"`, `PAYIN_TRANSFER → "Recarga Transfer"`, `PAYOUT_BANK → "Retiro a banco"`, `PURCHASE → "Compra tarjeta"`, `FEE → "Comisión"`, `REFUND → "Reverso"`, `CREDIT_ADJUSTMENT → "Ajuste"`, `TREASURY → "Tesorería"`, `UKNOWN → "Desconocido"`, `OTRO → "Otro"`, `Otros → "Otros"`. Apply via `dataKey` mapping or a Tooltip `formatter`; choose whatever Recharts shape is cleanest.

    **3. `ActivityTimelineV2.tsx` (Client Component, `"use client"`, ~140-170 LOC):**
    - Props: `{ data: ActivityPointV2[]; granularity: "day" | "week" }`
    - Recharts `LineChart` with TWO `<Line>` series sharing the X-axis:
      - Series 1: `dataKey="usuariosActivos"` — `stroke="oklch(0.55 0.18 250)"` (Indigo, section accent hue), `strokeWidth={2}`, `dot={false}`, name "Usuarios activos"
      - Series 2: `dataKey="volumen"` — `stroke="oklch(0.55 0.05 250)"` (muted Indigo), `strokeWidth={1.5}`, `strokeDasharray="4 4"`, `dot={false}`, `yAxisId="right"`, name "Volumen"
    - Dual `<YAxis>`: left = users count, right = volumen formatted via `formatCOP`. `<XAxis dataKey="bucket" />`.
    - Tooltip: per-row `Usuarios: N · Volumen: $X · Bucket: YYYY-MM-DD or YYYY-Www`. Use `formatInteger` + `formatCOP`.
    - `<2 data points` → render `<p className="text-sm text-muted-foreground">Sin datos suficientes para tendencia. Ampliá el período.</p>` instead of the chart.
    - Granularity is a DISPLAY hint only (currently affects only the bucket label format — no aggregation here; the page does the byDate-vs-byWeek choice upstream and passes the appropriate series). Caption text inside the chart wrapper Card is the page's responsibility (NOT this leaf's).

    **4. `TopUsersByVolume.tsx` (Server Component, ~100-130 LOC):**
    - Props: `{ rows: TopUserVolumeRow[] }`
    - Raw `<table className="w-full text-sm">` markup over `<thead>` / `<tbody>` (Plan 07-02 deviation pattern reaffirmed — repo has no shadcn `ui/table` primitive). Mirror `TopRechargers.tsx` shape from `src/components/recargas/`.
    - Columns: `#` (1..N rank), `Tikintag` (`font-mono` for `$`-prefix legibility), `Empresa` (denormalized; muted when undefined → `<span className="text-muted-foreground">—</span>`), `Tx` (right-aligned `tabular-nums`), `Volumen IN` (right-aligned `formatCOP`), `Volumen OUT` (right-aligned `formatCOP`), `Neto` (right-aligned `formatCOP`; positive → default; negative → wrap in `<span className="text-status-fail">` to flag net spenders).
    - Empty input → render `<p className="text-sm text-muted-foreground">Sin transacciones en el período.</p>` instead of an empty table.
    - NO `data-presenter-*` attributes — table is operator-and-cliente friendly (top 10 by volume is the kind of metric the cliente wants to SEE).
  </action>
  <verify>
    - `npx tsc --noEmit` 0 errors after writing all 4 files
    - `npm run lint` 0 errors, 3 baseline warnings unchanged
    - `grep -rE "text-section-inicio" src/components/inicio/InicioKPIStripV2.tsx` → exactly 1 line (one-section-accent rule scoped to the focal Usuarios activos card)
    - `grep -rE "use client" src/components/inicio/{InicioKPIStripV2,TransactionTypeDonut,ActivityTimelineV2,TopUsersByVolume}.tsx` → ONLY `TransactionTypeDonut` and `ActivityTimelineV2` carry the directive
    - `npm run build` succeeds (still 13 routes — no consumer changes yet; the new leaves are unreferenced until Task 2)
  </verify>
  <done>
    4 new v2 leaf files compile, lint, build clean. Atomic commit using HEREDOC: `feat(10-02): add 4 inicio v2 leaves (KPIStrip + TipoDonut + ActivityTimeline + TopUsersByVolume)`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite /inicio page to v2 cockpit</name>
  <files>src/app/(protected)/inicio/page.tsx</files>
  <action>
    Replace the v1 page (255 LOC, GMV/comisión/take-rate/empresas-activas/bonos-vendidos lens + 3 hechos curados) with a v2 operative-lens cockpit (~120-150 LOC). Preserve top-of-file conventions: `dynamic = "force-dynamic"`, `metadata.title`, Next 16 `searchParams: Promise<...>` signature, try/catch with inline error Card.

    **Structure (mirror `recargas/page.tsx` shape):**

    1. **Imports** — replace ALL v1 imports:
       - Domain: `filterInicioV2`, `summarizeInicioV2`, `aggregateTransactionTypeDistribution`, `aggregateActivityByDateV2`, `aggregateActivityByWeekV2`, `aggregateTopUsersByVolume` from `@/lib/domain/inicio`
       - Components: the 4 new v2 leaves from Task 1
       - Sheets: `getCachedTransactions` ONLY (drop `getCachedPayouts` — the v2 page no longer renders payouts data; tasa de éxito is over the transaction set's `status` field, NOT payout state)
       - URL: `parseFilters` from `@/lib/url-state`
       - UI: `Card`, `CardContent`, `CardHeader`, `CardTitle`
       - Drop: `formatInTimeZone`, `differenceInCalendarDays`, `computePriorPeriod`, all `inicio-hechos` imports, all `payouts` imports, `EmpresasActivasChart`, `GMVTrendChart`, `HechosCurados`, `KPICardsInicio`, `DeltaBadge`.

    2. **Pipeline:**
       - `params = await searchParams; filters = parseFilters(params);`
       - try/catch single fetch `txResult = await getCachedTransactions()` (NO payouts fetch needed). On throw: same inline error Card pattern as `recargas/page.tsx:115-128`.
       - `const allTx = txResult.rows;`
       - `const inicioRows = filterInicioV2(allTx, filters);` ONE filter pass.
       - **Granularity decision** — preserve v1's 60-day threshold (`inicio/page.tsx:163-170` shape):
         ```
         const length = filters.from && filters.to
           ? differenceInCalendarDays(new Date(`${filters.to}T00:00:00-05:00`), new Date(`${filters.from}T00:00:00-05:00`)) + 1
           : 30;
         const granularity: "day" | "week" = length > 60 ? "week" : "day";
         ```
         Re-import `differenceInCalendarDays` from `date-fns`. (This is the ONE v1 utility that survives because the threshold rule is identical.)
       - Run 4 v2 aggregations:
         - `summary = summarizeInicioV2(inicioRows)`
         - `tipoDistribution = aggregateTransactionTypeDistribution(inicioRows, 6)`
         - `activitySeries = granularity === "week" ? aggregateActivityByWeekV2(inicioRows) : aggregateActivityByDateV2(inicioRows)`
         - `topUsers = aggregateTopUsersByVolume(inicioRows, 10)`

    3. **Layout (`<div className="space-y-6">`):**
       - `<header>` with `<h1 className="font-heading text-2xl">Inicio</h1>` + `<p className="text-sm text-muted-foreground">Lente operativa · {granularity === "week" ? "vista semanal" : "vista diaria"}</p>`
       - `<InicioKPIStripV2 summary={summary} />`
       - `<div className="grid gap-6 lg:grid-cols-2">` containing:
         - `<Card>` with `CardTitle="Distribución por tipo de transacción"` wrapping `<TransactionTypeDonut buckets={tipoDistribution} />`
         - `<Card>` with `CardTitle="Actividad en el tiempo"` (inline `<span className="text-sm font-normal text-muted-foreground">({granularity === "week" ? "semanal" : "diario"})</span>`) wrapping `<ActivityTimelineV2 data={activitySeries} granularity={granularity} />`
       - `<Card>` with `CardTitle="Top 10 usuarios por volumen"` wrapping `<TopUsersByVolume rows={topUsers} />`

    **Cliente-foco contract:** NO `data-presenter-*` attributes — v2 Inicio is operator+cliente friendly (no internal-only metrics; tasa de éxito is the value-prop, not a secret). Conservative-default per CROSS-V2-07 (mirror `recargas/page.tsx` and `uso-tarjeta/page.tsx` decision). The empresa filter still works (filterInicioV2 honors `filters.empresa`); cliente-foco share URL was retargeted to `/clientes/{id}` in Plan 09-03, so /inicio is no longer the cliente landing page.

    **Decommissioned features (intentionally NOT carried to v2):**
    - Prior-period KPI badges (`computePriorPeriod` + `InicioDeltaSummary` + `DeltaBadge` deltas) — PRD lens shift; INI-V2 requirements describe absolute numbers, not deltas (mirror Plan 08-04 `recargas` page decision documented in STATE.md).
    - Hechos curados (`HechosCurados.tsx` + `inicio-hechos.ts` `findTopEmpresaByGMV` / `findEmpresasNuevasActivadas` / latencia destacada) — PRD pivots to operative lens; the editorial highlight reel does not appear in INI-V2-01..06.

    Update top-of-file JSDoc to reflect the v2 pipeline (replace the 70-line v1 JSDoc explaining dual-period delta pattern + cliente-foco CSS contract — both gone — with a 25-40 line v2 JSDoc citing INI-V2-01..06 + the operative-lens vision + the deferred-prune docket closure).
  </action>
  <verify>
    - `npx tsc --noEmit` 0 errors
    - `npm run lint` 0 errors, 3 baseline warnings unchanged
    - `npm run build` succeeds, still 13 routes (`/inicio` rebuilt in place)
    - `grep -nE "(filterCompletedIn|summarizeInicio[^V]|computePriorPeriod|inicio-hechos|HechosCurados|KPICardsInicio|GMVTrendChart|EmpresasActivasChart|filterPayouts|summarizePayouts)" src/app/\(protected\)/inicio/page.tsx` returns NO matches (all v1 surface gone)
    - Visual sanity check: `npm run dev` then visit `http://localhost:3000/inicio` — page renders without 500/404; section accent visible on Usuarios activos card; donut renders; activity chart renders; top users table renders.
  </verify>
  <done>
    /inicio v2 cockpit live; v1 page imports fully replaced; build green. Atomic commit using HEREDOC: `refactor(10-02): rewrite /inicio as v2 operative-lens cockpit`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cohesive multi-module v1 prune (delete 5 leaves + inicio-hechos.ts + 9 v1 symbols across inicio.ts/payouts.ts)</name>
  <files>
    src/components/inicio/HechosCurados.tsx,
    src/components/inicio/KPICardsInicio.tsx,
    src/components/inicio/GMVTrendChart.tsx,
    src/components/inicio/EmpresasActivasChart.tsx,
    src/components/inicio/DeltaBadge.tsx,
    src/lib/domain/inicio-hechos.ts,
    src/lib/domain/inicio.ts,
    src/lib/domain/payouts.ts
  </files>
  <action>
    **Audit gate FIRST** (mirror Plan 09-03 cohesive-prune protocol). For EACH symbol below, run `grep -rE "<symbol>" src/` and confirm only the about-to-be-deleted file references it (JSDoc / comment refs in unrelated files are acceptable — they don't break compilation). If ANY surprise consumer surfaces, STOP and surface — the prune cannot proceed without breaking that file.

    **Symbols to verify orphaned (16 total — should all confirm orphan after Task 2 lands):**

    v1 inicio leaves (5 components):
    - `HechosCurados`, `KPICardsInicio`, `GMVTrendChart`, `EmpresasActivasChart`, `DeltaBadge`

    inicio-hechos.ts file exports (5 symbols + the file itself):
    - `findTopEmpresaByGMV`, `findEmpresasNuevasActivadas`, `TopEmpresaResult`, `EmpresaNueva`, `EmpresasNuevasResult`

    inicio.ts v1 surface (8 symbols at the original line numbers):
    - `filterCompletedIn`, `summarizeInicio`, `aggregateGMVByDate`, `aggregateGMVByWeek`, `aggregateActiveEmpresasByDate`, `aggregateActiveEmpresasByWeek`
    - Types: `InicioSummary`, `InicioDeltaSummary`, `GMVPoint`, `ActiveEmpresaPoint`
    - Internal: `BONUS_TIPO` constant, `BOGOTA_TZ` constant if no longer needed by the v2 surface (verify against Plan 10-01 outputs — if v2 uses `formatInTimeZone(t.fecha, BOGOTA_TZ, "yyyy-MM-dd")` calls, KEEP the constant; if v2 inlines the literal, delete it)

    payouts.ts v1 surface (4 symbols — closes Plan 07-04 deferral docket):
    - `filterPayouts`, `summarizePayouts`, `PayoutSummary`, `COMPLETED_PAYOUT_STATES`
    - Internal: also delete `startOfDayBogotaTimestamp` / `endOfDayBogotaTimestamp` ONLY if `filterPayoutsV2` doesn't use them (verify — `filterPayoutsV2:512-513` DOES use them; KEEP).

    **After audit passes, perform deletions IN ONE COMMIT:**

    1. Delete the 5 leaf component files outright (`git rm`).
    2. Delete `src/lib/domain/inicio-hechos.ts` outright (`git rm`).
    3. Edit `src/lib/domain/inicio.ts`: remove the v1 block (lines 1-361 from BEFORE Plan 10-01's append, NOW lines 1-361 still). The v2 block Plan 10-01 appended below stays. Update the file header JSDoc to remove v1 references and document the v2-only surface.
    4. Edit `src/lib/domain/payouts.ts`: remove `COMPLETED_PAYOUT_STATES` constant + `filterPayouts` function + `summarizePayouts` function + `PayoutSummary` interface + their JSDoc. Top-of-file JSDoc updates to remove the v1 default-filter contract section (lines 24-42 of the current file) — replace with a brief 2-3 line "v2-only surface; v1 deprecated 2026-05-08" note.

    **Final file shape after prune:**
    - `src/components/inicio/` contains ONLY the 4 new v2 leaves from Task 1.
    - `src/lib/domain/inicio.ts` contains ONLY v2 surface (the previously-appended block from Plan 10-01).
    - `src/lib/domain/payouts.ts` contains ONLY v2 surface (filterPayoutsV2, summarizePayoutsByState, aggregateAverageProcessingMinutes, aggregateAgingAlertPending, aggregateFailureReasons, aggregateThirdPartyPayouts, aggregateTopBancos, quantileSorted + types).

    **Critical:** v2 surface preservation is byte-identical (no opportunistic edits — discipline matches Plan 09-03's 22-symbol prune commit).
  </action>
  <verify>
    - `git ls-files src/components/inicio/` shows ONLY 4 new v2 leaves (no DeltaBadge / HechosCurados / KPICardsInicio / GMVTrendChart / EmpresasActivasChart)
    - `git ls-files src/lib/domain/inicio-hechos.ts` returns nothing (file deleted)
    - `grep -nE "^export (interface|function|const)" src/lib/domain/inicio.ts` shows ONLY v2 exports
    - `grep -nE "^export (interface|function|const)" src/lib/domain/payouts.ts` does NOT show `filterPayouts` / `summarizePayouts` / `PayoutSummary` / `COMPLETED_PAYOUT_STATES`
    - `npx tsc --noEmit` 0 errors
    - `npm run lint` 0 errors, 3 baseline warnings unchanged
    - `npm run build` succeeds, 13 routes
    - `grep -rE "filterCompletedIn|summarizeInicio[^V]|filterPayouts[^V]|summarizePayouts[^B]|PayoutSummary|COMPLETED_PAYOUT_STATES|HechosCurados|KPICardsInicio|GMVTrendChart|EmpresasActivasChart|DeltaBadge|findTopEmpresaByGMV|findEmpresasNuevasActivadas|inicio-hechos" src/` returns ONLY documentation/JSDoc references (zero import or component-use survivors)
  </verify>
  <done>
    All 16 symbols pruned in one cohesive `refactor(10-02):` commit using HEREDOC. v2 surface preserved byte-identical. Phase 7-04 deferred-prune docket CLOSED. Atomic commit message: `refactor(10-02): cohesive v1 prune — 5 leaves + inicio-hechos + 9 symbols across inicio.ts/payouts.ts`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Inicio v2 operative-lens cockpit at `/inicio` + cohesive cleanup. The home page now shows: 3-card KPI strip (Usuarios activos with Indigo accent · Volumen IN/OUT · Tasa éxito with semáforo color); 2-col grid (donut por tipo · línea de actividad temporal); top 10 users table by volumen neto. All v1 leaves + inicio-hechos.ts + 13 v1 domain symbols deleted in one cohesive refactor commit. Phase 9 deferred-prune docket fully closed.
  </what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. Visit: `http://localhost:3000/inicio`
    3. Verify page renders without console errors:
       - Header reads "Inicio" with subtitle "Lente operativa · vista diaria" (or "semanal" with >60-day filter)
       - **3-card KPI strip:** Usuarios activos card has Indigo accent (`text-section-inicio`) on the headline; Volumen IN/OUT card shows two stacked stats; Tasa de éxito card color reflects semáforo (verde / amber / rojo) per the rate
       - **Donut chart:** "Distribución por tipo de transacción" — top 6 types + Otros bucket; Spanish localized labels in tooltip ("Bonos", "P2P", "Recarga PSE", etc.)
       - **Activity timeline:** "Actividad en el tiempo (diario)" — line chart of distinct active users per day; secondary muted dashed line for volumen on right axis
       - **Top users table:** "Top 10 usuarios por volumen" — 7-column table grouped by tikintag (NOT empresa); negative Neto values render with `text-status-fail` color
    4. Test filters:
       - Set `?from=2026-04-01&to=2026-04-30` — KPIs should narrow; chart should show only that range; granularity stays daily (≤60 days)
       - Set `?from=2026-01-01&to=2026-04-30` — granularity should switch to weekly (caption "(semanal)" appears)
       - Set `?status=completed` — only completed transactions counted; success rate should be 100%
       - Set `?empresa=$<sometikintag>` — KPIs / charts narrow to that empresa
    5. Dark mode toggle — Indigo accent preserved; donut OKLCH fills render without theme-conflict; semáforo colors flip-readable
    6. Visit other tabs (`/bonos`, `/payouts`, `/uso-tarjeta`, `/clientes`, `/recargas`) — confirm none broke (the prune touched payouts.ts; verify `/payouts` and `/clientes/[id]` still render fine)
    7. Visit a per-cliente dossier `/clientes/$<sometikintag>` — verify Vista Cliente (Plan 09-03) still renders cleanly
    8. Open browser console — zero errors
  </how-to-verify>
  <resume-signal>Type "approved" to commit a docs(10) closeout commit, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 6 INI-V2 requirements visibly satisfied (usuarios activos · volumen IN/OUT · tasa éxito semáforo · donut tipos · actividad temporal · top 10 users)
- [ ] One-section-accent-per-page rule held: exactly 1 occurrence of `text-section-inicio` on the focal Usuarios activos card
- [ ] All 5 v1 leaf files deleted (`git ls-files src/components/inicio/` returns 4 names)
- [ ] `inicio-hechos.ts` deleted (`git ls-files src/lib/domain/inicio-hechos.ts` returns empty)
- [ ] inicio.ts v1 block (filterCompletedIn + summarizeInicio + 4 GMV/empresa fns + 4 types) removed
- [ ] payouts.ts v1 surface (4 symbols) removed — Phase 7-04 docket CLOSED
- [ ] Other v2 pages still build and render (no regressions in /bonos, /payouts, /clientes, /uso-tarjeta, /recargas)
- [ ] Dark mode + URL filters + multi-select status/tipo all functional
- [ ] User typed "approved" at the visual checkpoint
</verification>

<success_criteria>

- All tasks completed with atomic commits
- Visual checkpoint approved by user
- 13 routes still build (no route addition/removal — `/inicio` rebuilt in place)
- v2 surface byte-identical preservation across the prune (audit grep proves it)
- Net LOC: expect ~−500 to −800 LOC removed despite delivering richer v2 lens (mirror Phase 9 closeout's ~−1500 LOC pattern at smaller scope; v1 block in inicio.ts ~360 LOC + inicio-hechos.ts 279 LOC + 5 v1 leaves ~554 LOC + payouts.ts v1 surface ~120 LOC ≈ −1300 LOC removed; v2 page rewrite ~−100 LOC delta + 4 new leaves ~+450 LOC delta = ~−950 LOC net)

</success_criteria>

<output>
After completion, create `.planning/phases/10-inicio-infra/10-02-SUMMARY.md` documenting:
- Final inventory of /inicio v2 surface (4 leaves + page composition + 5 v2 domain fns)
- Audit gate output for all 16 deleted symbols (orphan confirmation per symbol)
- LOC totals (added vs deleted; net per file)
- Any field-name reconciliation deviations encountered
- Forward note: v2.0 milestone v1→v2 migration end-to-end COMPLETE — only Plan 10-03 (INFRA-04) remains for Phase 10 closeout. Phase 9 deferred-prune docket fully closed (was: 22 symbols + 4 payouts symbols deferred; final tally: 26 symbols pruned across Phase 9-03 + Phase 10-02).
</output>
