# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-05-07 al iniciar milestone v2.0 Analytics)

**Core value:** Una sola URL donde el equipo de Tikin ve métricas frescas del negocio sin abrir Sheets, presentable cuando se proyecta a clientes.
**Current focus:** Phase 10 — Inicio v2 + Infrastructure 🚧 **IN PROGRESS** (1/3 plans landed: Plan 10-01 v2 domain surface ✅). Plan 10-02 (page rewrite + cohesive prune of v1 inicio + 4 v1 payouts.ts symbols + inicio-hechos.ts) is the next milestone. Plan 10-03 (INFRA-04 custom domain) is independent and may run in parallel.

## Current Position

Phase: 10 of 10 (Inicio + Infrastructure) — **IN PROGRESS** 🚧
Plan: 1/3 landed — **10-01** (inicio.ts v2 operative-lens domain surface; 6 fns + 4 types; +568 LOC; v1 byte-identical, lines 1-361 preserved).
Status: Plan 10-01 SHIPPED — `src/lib/domain/inicio.ts` extended with `filterInicioV2` + `summarizeInicioV2` + `aggregateTransactionTypeDistribution` + `aggregateActivityByDateV2` + `aggregateActivityByWeekV2` + `aggregateTopUsersByVolume` (and 4 v2 types `InicioSummaryV2`, `TransactionTypeBucket`, `ActivityPointV2`, `TopUserVolumeRow`). Fifth instance of v2-alongside-v1 coexistence pattern. v1 surface still consumed by `inicio/page.tsx` + `HechosCurados.tsx` + `inicio-hechos.ts`. tsc + lint (0 errors, 3 baseline warnings unchanged) + `npm run build` (13 routes) all green. **Field-name reconciliation: NONE** — fifth clean-break plan in a row after 09-01/09-02/09-03 + Plan 10-01.
Last activity: 2026-05-08 — Completed 10-01-PLAN.md (inicio.ts v2 surface appended; 2 atomic commits `8ea04b6` + `12d0710`; tsc + lint + build clean).

Progress: v1.0 ✅ SHIPPED (Phases 1-5) · v2.0 🚧 4/5 phases shipped (80%) · **Phase 10: 1/3 plans █░░ (33%) 🚧**

## Performance Metrics

**Velocity (v1.0 baseline):**
- Total plans completed in v1.0: 24 (1 deferred — INFRA-04 carry-forward)
- Total commits: 88 commits in 10 días
- Total LOC produced: ~9150 TypeScript LOC en 76 archivos
- Total execution time: 2026-04-27 → 2026-05-06 (10 días calendario)

**By Phase (v1.0):**

| Phase | Plans | Status |
|-------|-------|--------|
| 1. Foundation | TBD | ✅ |
| 2. Bonos | TBD | ✅ |
| 3. Payouts | TBD | ✅ |
| 4. Inicio + Recargas | TBD | ✅ |
| 5. Clientes + Domain | TBD | ✅ (1 plan deferred) |

**Recent Trend:**
- Last 5 plans (v1.0 closing): 12-plan zero-deviation streak (plan-spec literal-block fidelity demonstrated)
- Trend: improving — domain libraries + UI leaves + page composition demonstrated deterministic parity in late v1.0

*Updated after each plan completion in v2.0*

## Accumulated Context

### Decisions (recent, affecting current work)

Decisions están loggeadas en PROJECT.md Key Decisions table. Recent ones que afectan Phase 6 (Foundation v2):

- **JOIN canónico = `transaction_id` (técnico) / `reference` (semántico via PRD)** — código usa `transaction_id`, JSDoc cita PRD por convención. Verificación histórica 773/798 (96.9%) match; live re-verificación Plan 06-02 = 768/797 (96.36%, satisface ≥96%). CROSS-V2-04. **Helper canónico `joinPayouts()` en `src/lib/domain/join.ts` (Plan 06-02)** — `joinIndex` + `joinMatchStats` + tipo `JoinedPayout` exportados. Migración del inline-join de `payouts/page.tsx` deferida a Phase 7+.
- **Modo Presentación + cliente-foco share-URL preservados en v2.0** — PRD declara dual-purpose explícito en Vista Cliente; visibility por métrica refinada en CLI-V2-* requirements y CROSS-V2-07.
- **Reutilizar componentes/diseño/auth de v1.0** donde apliquen — design system aprobado; refactorizar solo data + nuevas vistas.
- **6 secciones (no 5)** — Recargas vuelve en v2 con scope expandido (PSE + Transfer); Uso Tarjeta es una sección distinta para PURCHASE.
- **`parseCOPAmount` returns `number | null`** (Plan 06-01) — null = explicit "no value" signal; v2.0 callers branch on `=== null` to distinguish missing vs zero. Zod schemas wrap and translate null → `addIssue + z.NEVER`.
- **`parsers.ts` public API in MINUTES, `schemas.ts` internal path in SECONDS** (Plan 06-01) — `parseAging`/`parseTotalTime` return minutes (PRD v2 expectation); `parsePgIntervalSeconds` kept exported and used by `schemas.ts` for `Payout.latencySeconds` backward-compat (Phase 3 percentiles).
- **CSV multi-select URL serialization** (Plan 06-03) — `?status=completed,failed&tipo=BONUS,P2P` chosen over repeated keys. Empty array → param omitted (parity with absent key). Stable URL ordering: from → to → empresa → status → tipo → presenter.
- **Filter option lists hardcoded in components** (Plan 06-03) — `StatusFilter` + `TypeFilter` declare their own `value/label` arrays; NOT auto-derived from `TransactionType` union. UI labels (e.g. "Compra (tarjeta)") are UI concerns; defensive fallbacks (UKNOWN, OTRO) excluded from filter options. Schema additions don't auto-pollute the dropdown.
- **Phase 6 ships filter UI + URL contract only** (Plan 06-03) — domain functions (`filterBonos`, `filterPayouts`, etc.) NOT touched. Each Phase 7+ section decides which filters to honor in its data layer.
- **Diagnostic-route lifecycle pattern reaffirmed** (Plan 06-02) — temp `/api/diagnose-*` routes for live verification get created, exercised via temporary proxy.ts PUBLIC_PATHS extension, then deleted (route) + reverted (proxy) BEFORE the task commit. Source never lands in git history. Mirrors Plans 02-01 and 03-01.
- **next-themes wired with `attribute="class"` + `defaultTheme="system"`** (Plan 06-04) — aligns with the existing `@custom-variant dark (&:is(.dark *))` selector in `globals.css` (zero v1.0 dark CSS rewritten). First visit respects OS preference; user override persists in localStorage. `<html suppressHydrationWarning>` required because next-themes flips `.dark` before React hydrates. ThemeToggle stays VISIBLE in presenter mode (operator control during meetings).
- **v2.0 paleta = OKLCH, lifted +0.10 lightness for dark** (Plan 06-04) — 6 section vars (`--section-{inicio|bonos|payouts|tarjeta|clientes|recargas}`) + 3 status vars (`--status-{success|fail|pending}`) declared in `:root`, mirrored brighter in `.dark`. Exposed to Tailwind via `@theme inline { --color-section-*: var(--section-*) }` so `text-section-bonos`, `bg-status-success`, etc. resolve as utilities. Status palette intentionally aliases section hues (verde ≡ emerald, amarillo ≡ amber) — single semantic source per hue family.
- **Conservative-default per-metric visibility** (Plan 06-04, CROSS-V2-07) — `[data-presenter="on"] [data-presenter-metric-hide] { display: none !important }`. Default policy: ALL metrics visible in presenter; opt-out by tagging individual elements. Distinct from chrome `data-presenter-hide` and cliente-foco `data-presenter-empresa-hide`. Phase 9 (Vista Cliente CLI-V2-08) tags the cronological timeline.
- **React 19 `react-hooks/set-state-in-effect` requires per-call eslint-disable for next-themes mount gate** (Plan 06-04 deviation) — canonical `useEffect → setMounted(true)` pattern collides with the React 19 lint rule. Resolution: keep the documented next-themes pattern + focused single-line `eslint-disable-next-line` + JSDoc explaining the why. Future plans adopting "subscribe → setState" patterns may need similar exceptions; if more than 2-3 cases appear, consider project-level config exception.
- **v2-alongside-v1 coexistence pattern in domain modules** (Plan 07-01) — when a Wave-1 plan extends a domain library that's still consumed by a live page, append v2 exports BELOW v1 in the same file and keep v1 byte-identical. The Wave-2 plan that rewrites the consuming page does the import swap + v1 prune in ONE cohesive diff. Result: the live build never goes red mid-wave. Applied to `bonos.ts` (5 v2 functions added; v1 untouched until Plan 07-02 swaps `/bonos/page.tsx`).
- **`Transaction.sourceTransferTikintag` / `destinationTransferTikintag` first-class fields** (Plan 07-01) — surfaced from BD_Plataforma `source_transfer_tikintag` / `destination_transfer_tikintag` columns that the schema parsed but discarded since Phase 2. Pure-add to the `Transaction` interface (typed `string | undefined`); 2-edit surgical schema add (types.ts JSDoc + schemas.ts transform write). v1 callers unaffected (undefined-by-default). v2 Bonos rankings (BON-V2-05/06) read these directly: top emisores ranks by `sourceTransferTikintag`, top receptores by `destinationTransferTikintag`, irrespective of `direction`.
- **v2 Bonos filter does NOT pre-filter `direction`** (Plan 07-01) — v1 `filterBonos` collapsed to `direction=in`; `filterBonosV2` lets BOTH directions through and downstream aggregations (`summarizeBonosV2` splits in/out, `aggregateTopEmisores` ranks the sender column, `aggregateTopReceptores` ranks the receiver column) partition at aggregation time. One filter pass → many aggregation paths. `filters.tipo` INTENTIONALLY ignored by `filterBonosV2` (Bonos tab is BONUS-by-definition); `filters.status` CSV honored verbatim with default `['completed']` when absent or empty.
- **Parallel-wave git race RECONFIRMED in v2.0** (Plan 07-01 Wave 1, sibling 07-03 racing on `payouts.ts`) — sibling agent's uncommitted Task-2 working-tree changes appeared in my `git status` as `M src/lib/domain/payouts.ts` despite Plan 07-01 never touching that file. Recovery: explicit pathspec stage (`git add -- src/lib/domain/bonos.ts`) — leaves sibling's pending file untouched, commit contains exactly the plan's intended diff. Confirms STATE.md's prior guidance pattern (also used in v1.0). Lint may transiently report new warnings in sibling-owned files (e.g. unused imports awaiting the sibling's consumer commit) — these are NOT plan-owned and disappear once the sibling's Task 2 lands.
- **`JoinedPayout[]` as aggregation function input** (Plan 07-03) — `aggregateThirdPartyPayouts(joined: JoinedPayout[])` accepts the joined shape directly, NOT `(transactions, payouts)`. Page composition runs `joinPayouts()` ONCE per request and chains the result into multiple downstream aggregations (this plan: third-party detection; future plans: any cross-source enrichment). One JOIN per request budget contract. Composability: downstream consumers all read the same `Payout & { transaction?: Transaction }` shape. First production consumer of Plan 06-02's canonical helper.
- **Type-only import fallback for v2 aggregation modules** (Plan 07-03) — when a domain aggregation file needs only the SHAPE of `JoinedPayout` (page composition layer owns the runtime `joinPayouts` call), use `import type { JoinedPayout } from "./join";` instead of value-import. Plan 07-03 attempted value-import first (per plan), got `no-unused-vars`, fell back to type-only — this branch is the canonical resolution for any v2 aggregation that consumes the joined shape without invoking the JOIN.
- **`$`-prefix-stripped tikintag normalization in third-party comparison** (Plan 07-03 PAY-V2-08) — `aggregateThirdPartyPayouts` lowercases + trims both sides; when `tikintag` starts with `$` (the on-Sheet convention per Plan 02-01 SUMMARY), strips the leading `$` before comparison. Catches the rare `$mario ↔ "mario"` first-party case that would otherwise misclassify a self-payout as third-party. Unmatched payouts (~3.1% historic per Plan 06-02) SKIPPED, not counted either way.
- **Defensive completed-only inside time aggregations** (Plan 07-03) — `aggregateAverageProcessingMinutes` and `aggregateFailureReasons` self-filter to the relevant state inside the function, even though callers typically pre-filter. Prevents Aging-fallback contamination of a completed-mean (the silent semantic drift v1 03-CONTEXT.md essentials warned about). `latencySeconds` carries `Total Time` for completed rows and `Aging` for non-completed; these aggregations refuse to mix the two.
- **`summarizePayoutsByState.total` uses `payouts.length`** (Plan 07-03) — not `completed + failed + inProgress`. If upstream Sheet ever introduces a 4th state (e.g. `cancelled`, `pending_review`), `successRate` denominator stays correct (numerator = completed, denominator = total-attempted including the unrecognized state); the 3 named counters quietly underrepresent until the schema is updated. Defensive-by-default.
- **Wave-1 v2-suffix coexistence convention** (Plans 07-01 + 07-03 in parallel) — both wave-1 plans independently chose `<fn>V2` suffix for their parallel additions (`filterBonosV2`, `summarizeBonosV2`, `filterPayoutsV2`, `summarizePayoutsByState`). Convention emerged organically without explicit cross-plan coordination — Plans 07-02 + 07-04 (Wave 2) will swap imports and prune v1 fns in a single cohesive diff each.
- **Partial v1 payouts prune — 4 symbols KEPT alive deferred to Phase 9 + Phase 10** (Plan 07-04 deviation, Rule 4 architectural / scope). Plan 07-04 was supposed to delete the full v1 set per Plan 07-03's pruning list, but a `grep -rE` audit of `src/` BEFORE deletion surfaced 4 symbols still consumed outside `/payouts/page.tsx`: **`filterPayouts`** (used by `inicio/page.tsx` Latencia destacada hecho + `clientes/[empresaId]/page.tsx` mini cards), **`summarizePayouts`** (same two consumers), **`PayoutSummary`** interface (used by `HechosCurados.tsx` + `EmpresaMiniCards.tsx` as prop type), and **`COMPLETED_PAYOUT_STATES`** constant (still consumed internally by the kept-alive `filterPayouts`). Plan 07-03 SUMMARY's pruning list assumed `/payouts/page.tsx` was the sole consumer — incorrect. Plan 07-04's verify section explicitly anticipates this gate ("STOP and surface — that file needs a swap before the prune can land") and the output spec sanctions a flag-for-future-cleanup branch. **DELETED in Plan 07-04** (only consumed by the rewritten page + deleted v1 leaves): `filterPayoutsByPeriodOnly`, `aggregateLatencyHistogram`, `LatencyBucket`, `LatencyBucketLabel`, `HISTOGRAM_BUCKET_ORDER`, `aggregateSuccessRate`, `SuccessRate`. **Migration path:** Phase 10 (Inicio v2 rewrite) replaces `filterPayouts + summarizePayouts` for the Latencia destacada hecho with v2 helpers (`summarizePayoutsByState` + `aggregateAverageProcessingMinutes`); Phase 9 (Vista Cliente v2 rewrite of `clientes/[empresaId]/page.tsx`) does the same for empresa mini-cards. After BOTH phases land, the 4 kept-alive symbols become orphans — final prune lands as a 1-task cleanup in whichever phase touches `payouts.ts` last. **No behavior change to Inicio or Clientes pages today** — they keep building and rendering identically against the kept-alive surface.
- **Time-first cockpit layout pattern established** (Plan 07-04 PAY-V2-01..08) — PRIMARY KPI text-4xl with section accent (`text-section-payouts`); semáforo-bound color on the second protagonist via threshold helper (`successRateAccent`: ≥95% verde, ≥85% amber, else rojo); conditional null-render alert between protagonists and quality semáforo (AgingAlert returns `null` when `rows.length === 0` — health = absence of card, not a positive placeholder); diagnostic layer at bottom in `lg:grid-cols-2`. Reusable shape for any future operational/quality dashboards (Phase 9 Vista Cliente expected to inherit). Conditional null-render pattern reusable for any future alert/queue widget.
- **JoinedPayout pipeline at page-composition** (Plan 07-04 first production wiring) — page composition runs `joinPayouts(transactions, completed)` ONCE per request, then chains the result into `aggregateThirdPartyPayouts`. Confirms the Plan 06-02 + Plan 07-03 design contract (one-JOIN-per-request budget). React `cache()` dedupes the BD_Plataforma fetch with DashboardHeader's empresa-registry call, so the always-on JOIN doesn't double-pay quota. Phase 9 Vista Cliente will reuse the same pipeline shape (run JOIN once, chain into multiple aggregations for empresa enrichment + tikintag-based metrics).
- **`PayoutState` type lives in `types.ts`, not re-exported from `payouts.ts`** (Plan 07-04 import-fix) — `ThirdPartyPayouts.tsx` initially imported `PayoutState` from `@/lib/domain/payouts` (where the plan implicitly suggested), tsc surfaced `TS2459: declares locally but not exported`. Resolution: `import type { PayoutState } from "@/lib/domain/types";`. Future v2 components rendering payout state badges should import directly from `types.ts`. (Note: Phase 9+ may consider whether to re-export `PayoutState` from `payouts.ts` for ergonomics — for now, direct-from-types-ts is the canonical path.)
- **Partial v1 bonos prune — 8 symbols KEPT alive deferred to Phase 9 / `07-02b` follow-up plan** (Plan 07-02 deviation, Rule 4 architectural / scope; mirrors the Plan 07-04 deferral pattern). Plan 07-02 was supposed to prune ALL v1 bonos exports per Task 2(b), but the plan's own STOP gate ("If any other file still imports a v1 symbol, STOP and surface — the prune cannot proceed without breaking that file") fired on a `grep -r` audit. **KEPT alive** (still consumed outside `/bonos/page.tsx`): `filterBonos` + `summarizeBonos` (used by `clientes/[empresaId]/page.tsx` lines 84/180/193 to render mini-cards for one empresa), `BonoSummary` interface (used by `EmpresaMiniCards.tsx` as a `bonos` prop type), plus the unused-but-coupled trio `aggregateBonosByDate` / `aggregateBonosByEmpresa` / `top10Empresas` + interfaces `BonoByDate` / `BonoByEmpresa` (KEPT for symmetry — pruning the latter 5 while keeping the former 3 would leave bonos.ts at a partial v1 surface; safer to defer all 8 in one cohesive future diff). **DELETED in Plan 07-02** (sole consumer was the rewritten page): 4 v1 component files (`KPICards.tsx`, `Leaderboard.tsx`, `SalesTable.tsx`, `BonosChart.tsx`, 356 LOC removed). **Migration path:** Phase 9 (Vista Cliente v2 rewrite of `clientes/[empresaId]/page.tsx` + `EmpresaMiniCards.tsx`) replaces v1 `filterBonos + summarizeBonos + BonoSummary` consumers with v2 helpers (`filterBonosV2 + summarizeBonosV2 + BonoSummaryV2`), then completes the v1 prune in `bonos.ts`. Alternative: a small surgical follow-up plan `07-02b` does the migration before Phase 9. **No behavior change to Clientes page today** — kept-alive surface is byte-identical to v1.0.
- **Raw `<table>` markup over a shadcn `ui/table` primitive** (Plan 07-02 deviation, Rule 3 blocking) — repo has no `@/components/ui/table` primitive (only card / button / input / label / separator / skeleton / sonner / switch). Plan 07-02 instructed importing from one. Resolution: used raw `<table className="w-full text-sm">` markup with manual `<thead>` / `<tbody>` / `<tr>` / `<th>` / `<td>` styled via Tailwind, matching the existing codebase convention (v1 SalesTable.tsx already used the same idiom). Both `TopEmisores.tsx` and `TopReceptores.tsx` follow this pattern. Creating a new shadcn-style Table primitive for two ranking components is scope creep beyond Plan 07-02; if a future plan needs more table features (sortable headers, sticky thead, virtualization), it can refactor across all consumers in one diff.
- **Count-based share in `RechargeMethodSplit`** (Plan 08-03 REC-V2-04) — shares for the PSE vs TRANSFER cards are 0..1 BY COUNT, not by COP volume. Anchored to the PRD baseline reading "85% PSE / 15% Transfer" which describes the count split (PSE rows are typically smaller individual amounts but happen more often). Volume is RETURNED alongside count for pages that want to show COP, but `share` is the count-based metric. Returned as a literal `{ pse, transfer }` shape (NOT array) so the v2 page can bind two cards directly off `result.pse` / `result.transfer` without `.find()` or index lookup.
- **`aggregateRechargesByDateV2` is RICHER than v1 `aggregateRecargasByDate`** (Plan 08-03) — v1 emits `{ date, count, monto }` totals-only. v2 emits `{ date, pseCount, pseVolumen, transferCount, transferVolumen, totalCount, totalVolumen }` so the v2 stacked-trend chart (REC-V2-04 + REC-V2-08) can visualize PSE vs TRANSFER over time. v1 stays alive for clientes consumers that don't need the per-method split. Both functions coexist on the same module — the v1 surface is byte-identical (10 v1 exports preserved at lines 120, 128, 138, 184, 222, 252, 299, 352, 387, 420 — exact baseline match).
- **Amount distribution boundary inclusivity** (Plan 08-03 REC-V2-06) — `aggregateRechargeAmountDistribution` boundaries documented in JSDoc to prevent off-by-cent surprises: bottom bucket `<$100K` is strictly less-than (`amount < 100_000`); middle bucket `$100K-$1M` is INCLUSIVE on BOTH ends (`100_000 <= amount <= 1_000_000`) — exactly $100K and exactly $1M land in the middle; top bucket `>$1M` is strictly greater-than (`amount > 1_000_000`). Always returns all 3 buckets even when some are zero (chart x-axis stability across filter changes). Top rechargers grouped by `tikintag` (NOT `empresa_id`) — future-proofs against the 02-01 empresa-identity divergence path.
- **Partial v1 recargas prune — 10 symbols KEPT alive deferred to Phase 9** (Plan 08-03 mirrors the Plans 07-02 / 07-04 deferral pattern; third instance) — all 10 v1 exports kept byte-identical because `clientes/[empresaId]/page.tsx` + `EmpresaMiniCards.tsx` consume them: `filterRecargas`, `summarizeRecargas`, `aggregateRecargasByDate`, `aggregateRecargasByEmpresa`, `top10RecargasEmpresas`, `findTopEmpresaRecargadora`, `findRecargaMasGrande`, `RecargaSummary`, `RecargaByDate`, `RecargaByEmpresa`. **Migration path:** Phase 9 (Vista Cliente v2 rewrite of `clientes/[empresaId]/page.tsx` + `EmpresaMiniCards.tsx`) replaces v1 consumers with v2 helpers; final v1 prune of recargas.ts lands when Phase 9 ships. Joins the bonos and payouts deferred-prune lists already on the Phase 9 docket. **No behavior change to Clientes page today** — kept-alive surface is byte-identical to v1.0.
- **Field-name reconciliation when PLAN.md cites non-existent fields** (Plan 08-03 deviation, Rule 3 blocking) — PLAN.md `<action>` blocks referenced `tx.transactionAmount`, `tx.transferTikintag`, `tx.transferEmpresa` as if they existed on `Transaction`. Reading types.ts confirmed they do NOT. Resolution: used actual fields (`monto`, `tikintag`, `empresa_id` / `empresa_nombre`) and documented the mapping in the v2 section header comment at the top of the appended block. Pattern: when an executor sub-agent encounters this gap, do ONE pass of cross-referencing PLAN.md field names against the type module, apply the mapping inline, document under [Rule 3 - Blocking] deviations, and surface the reconciliation in the source file's section header so future readers can audit it without reading the SUMMARY. **Plan 08-01 hit the same field-name mismatch** independently (translation NOT a Rule deviation in 08-01's framing — types.ts is the authoritative @-context contract; pseudo-code is intent guidance). Two siblings reaching the same translation independently confirms: `tx.transferTikintag/Empresa/transactionAmount/date` and `filters.period.from/to` are recurring author-shorthand → executor-side translation pairs in v2.0 plan vocabulary.
- **From-scratch v2 domain pattern when section is brand-new in milestone** (Plan 08-01) — Uso Tarjeta has no v1 predecessor (NEW section in v2.0 per PRD), so `cardUsage.ts` was authored from scratch with NO coexistence layer needed. Compare to Phase 7 where `bonos.ts` and `payouts.ts` had to keep v1 fns alive while v2 fns appended below — Phase 8 Plan 08-01 just files a clean module. Pattern: when the milestone introduces a never-before-built section, the wave-1 domain plan stands up the entire surface in one file with no partial-prune debt incurred. Combined with sibling 08-03's append-to-existing pattern, Phase 8 Wave 1 demonstrates BOTH idioms in parallel: from-scratch (08-01) and v2-suffix-coexistence (08-03).
- **Two-arg adoption signature for ratio aggregations** (Plan 08-01 CARD-V2-04) — `aggregatePurchaseAdoption(allTx, purchaseRows)` accepts BOTH inputs and computes numerator/denominator from disjoint scopes (purchases for numerator, broader pool for denominator). Caller pre-filters `allTx` to the period ONCE; threads the result into both args. Same one-filter-pass-multiple-aggregations contract as Plan 07-03's `JoinedPayout` pipeline. Reusable shape for any future "X% of users do Y" KPI (e.g. retention, activation, cross-sell). Note: sibling Plan 08-03's `aggregateRechargeAdoption` independently established the SAME two-arg signature for the recargas adoption KPI — convention emerged in parallel without coordination.
- **PURCHASE direction=out is the canonical "card spend" filter** (Plan 08-01 CARD-V2-01) — the matching direction=in peer row (sale-in to receiving empresa) is INTENTIONALLY excluded so a single user's ticket is counted once, not twice. PURCHASE-out montos are negative in BD_Plataforma; `summarizePurchases` takes `Math.abs(monto)` so the headline volumen is gross spend (positive). Not just defensive — it's the correct semantic. `filters.tipo` ignored (Uso Tarjeta is PURCHASE-by-definition, same convention as `filterBonosV2` BONUS-by-definition / `filterRecargasV2` PAYIN-by-definition).
- **Inlined Bogotá date helper convention reaffirmed (third time)** (Plan 08-01) — `cardUsage.ts` defines a private 10-line `toBogotaISODate(d)` helper instead of importing `formatInTimeZone` from `date-fns-tz`. Bogotá is +/-5h from UTC with no DST → arithmetic is deterministic. Same convention as `recargas.ts` and `bonos.ts` keeping their own private `startOfDayBogotaTimestamp` helpers. The DRY-cost across modules (would require a new shared util imported by every domain module) exceeds the inline-cost of ~10 lines per module. Established convention since Phase 4; reaffirmed in Phase 8.
- **Project lint command is `npm run lint` (eslint), NOT `npx next lint`** (Plan 08-01 verification adjustment) — Next 16 dropped the `next lint` subcommand. The project's `package.json` declares `"lint": "eslint"`. Plans authored with the older `next lint` invocation should be honored against the actual lint runner; intent (0 errors, baseline warnings unchanged) is preserved. Update applies to all v2.0 plans authored before this discovery.
- **One-section-accent-per-page rule** (Plan 08-02 visual conventions, Wave 2 first composition lesson) — `text-section-tarjeta` (Amber) lands on EXACTLY ONE focal metric per `/uso-tarjeta` page: KPICardsCardUsage primary "Compras totales" card (text-4xl headline, eye-magnet first position). AdoptionCard intentionally uses `text-foreground` despite being the conversation-driver per CARD-V2-04 — two competing Amber zones diluted the read. Future v2 sections (Phase 9 Vista Cliente, Phase 10 Inicio v2) inherit this rule: section accent on ONE protagonist, not multiple. Establishes a v2.0-wide visual constraint distinct from Phase 7's per-section accent application (Phase 7 also applied accent to one card per page in practice; Plan 08-02 codifies it as a stated rule).
- **Adoption denominator = full transaction pool, NOT period-filtered** (Plan 08-02 page composition for `/uso-tarjeta`) — `aggregatePurchaseAdoption(allTx, purchaseRows)` is called with the FULL `getCachedTransactions().rows` as the denominator scope; only the numerator (`purchaseRows`) is period-filtered. Rationale: PRD baseline 40/235 ≈ 17% is a global-pool reading ("what fraction of our user base ever uses the tarjeta"), not a period-filtered reading ("what fraction of users active in the last 7 days made a card purchase"). Period-filtering the denominator would inflate adoption artificially in short windows. Phase 9 Vista Cliente may revisit if a per-period denominator becomes desirable for a per-empresa adoption mini-card. Reusable shape for any future "X% of users do Y" KPI where the denominator question is "of all known users" rather than "of users active in the period".
- **Soft-reset+restore-staged commit-time race recovery** (Plan 08-02 Task 1, NEW parallel-wave race mode) — sibling 08-04's executor created 5 NEW untracked files in `src/components/recargas/` during my Task-1 commit transaction window (between `git add -- <pathspec>` and `git commit -m`). STRONGER race mode than prior pathspec races (Plans 07-01, 07-03, 08-01, 08-03 — those involved active edits in disjoint dirs; this involved files appearing from nothing during the transaction window). Recovery: `git reset --soft HEAD~1` (preserves staged + working tree; rolls only the commit ref back) → `git restore --staged -- src/components/recargas/<five files>` (unstage sibling's files; they stay on disk untracked for sibling to commit). Then re-staged ONLY my 4 uso-tarjeta files + re-committed Task 1 cleanly. Sibling's worktree state survived; no rebase needed. Pattern: pathspec discipline catches 99% of races, but the commit transaction itself has a vulnerability window during which sibling-created untracked files can interleave. Soft-reset+restore-staged is non-destructive and idempotent — preserves both my work and sibling's, preserves history cleanly. Add to canonical recovery toolkit alongside `git stash --include-untracked` and explicit-pathspec-only commits.
- **Adoption-first cockpit composition pattern** (Plan 08-02, variant of Phase 7 time-first cockpit) — `/uso-tarjeta` uses single-column stack: KPI strip → AdoptionCard (conversation-driver headline) → trend chart Card → ranking table Card. Phase 7 Payouts established time-first cockpit (KPI header → AgingAlert conditional → StatusBreakdown → diagnostic row in `lg:grid-cols-2`). Plan 08-02 establishes adoption-first as a sibling pattern for sections where the headline metric is a ratio/adoption KPI rather than a latency/operational KPI. Plan 08-02 explicitly drops the `lg:grid-cols-2` diagnostic row (Uso Tarjeta has fewer protagonists; vertical reading flow reads cleaner). Convention: `lg:grid-cols-2` diagnostic row is OPTIONAL (use only when there are 2+ co-equal diagnostic widgets). Phase 9 Vista Cliente and Phase 10 Inicio v2 can pick whichever cockpit shape (time-first / adoption-first / ranking-first per Plan 07-02 Bonos) matches the section's protagonist metric.
- **Method-and-distribution-first cockpit composition pattern** (Plan 08-04, fourth cockpit shape in the v2.0 family alongside time-first 07-04, ranking-first 07-02, adoption-first 08-02) — `/recargas` uses: KPI strip (4 cards: Total recargas + Volumen + Adopción + Recarga promedio) → MethodSplitCard | AmountDistribution at `lg:grid-cols-2` (the two diagnostic protagonists answering "what method?" and "what size?") → TopRechargers full-width (ranking by `tikintag` answers "who?") → RecargasTrendChartV2 full-width (stacked PSE/TRANSFER timeline answers "when?"). Section accent (`text-section-recargas` Teal) on EXACTLY ONE focal metric (Total recargas primary card, text-4xl). Reusable for any future operational cockpit with primary KPI + two diagnostic protagonists side-by-side + ranking + temporal. Together the four cockpit shapes (time-first / ranking-first / adoption-first / method-and-distribution-first) establish a v2.0 composition vocabulary: each Wave-2 section picks the shape that matches its protagonist-metric category. Phase 9 Vista Cliente expected to inherit one of the four (likely a hybrid given empresa profile dimensionality).
- **Top-rankings grouping by `tikintag` (user) NOT by empresa — explicit v1→v2 semantic shift** (Plan 08-04 REC-V2-07 reaffirms the convention) — v1 RecargasTable ranked top empresas by `transferEmpresa`; v2 TopRechargers ranks top 10 USERS by `tikintag`. Joins the family of v2 user-lens rankings: Phase 7 Bonos TopEmisores/TopReceptores rank by `sourceTransferTikintag`/`destinationTransferTikintag`; Phase 8 Uso Tarjeta TopCardUsers ranks by `tikintag`; Phase 8 Recargas TopRechargers ranks by `tikintag`. **Tikintag is the canonical user identity at v2 ranking layer**; empresa-as-rank-key is a v1 idiom. Empresa column on each row is shown as a denormalized label (the user's first observed empresa_nombre), not the ranking key. Diagnostic value: who's the heaviest individual recharger/spender/sender, not which empresa aggregates the most volume.
- **Stacked-bar timeline for share-over-time stories** (Plan 08-04 RecargasTrendChartV2) — when the temporal story IS the count SHARE between two categories (PSE vs TRANSFER baseline 85/15 per PRD), Recharts `BarChart` with two `Bar stackId='same'` series beats a two-series LineChart for cognitive load. Stacked bars at each date make the share visually immediate; two crossing lines force eye-arithmetic. OKLCH-pinned fills (PSE = `oklch(0.62 0.10 200)`, TRANSFER = `oklch(0.42 0.10 200)`) for deterministic light/dark mode rendering. Tooltip shows PSE + TRANSFER + total. Reusable for any future binary/ternary share-over-time visualization (success/fail timelines, in/out flows, etc.).
- **Partial v1 recargas prune CONFIRMED — 10 symbols KEPT alive deferred to Phase 9** (Plan 08-04 ratifies the deferral decision Plan 08-03 set up; fourth instance of the deferral pattern in v2.0 — joins Plans 07-02, 07-04, 08-03). `recargas.ts` was NOT modified in Plan 08-04 (verified `git log d07ed47..HEAD -- src/lib/domain/recargas.ts` empty). All 10 v1 symbols remain byte-identical: `filterRecargas`, `summarizeRecargas`, `aggregateRecargasByDate`, `aggregateRecargasByEmpresa`, `top10RecargasEmpresas`, `findTopEmpresaRecargadora`, `findRecargaMasGrande`, `RecargaSummary`, `RecargaByDate`, `RecargaByEmpresa`. **Why all 10 (including 5 orphaned-after-this-plan):** Pruning the 5 orphans (no longer consumed after `/recargas/page.tsx` rewrite — `aggregateRecargasByEmpresa`, `top10RecargasEmpresas`, `findTopEmpresaRecargadora`, `findRecargaMasGrande`, `RecargaByEmpresa`) while keeping the 5 still-consumed (by `clientes/[empresaId]/page.tsx` + `EmpresaMiniCards.tsx` — `filterRecargas`, `summarizeRecargas`, `aggregateRecargasByDate`, `RecargaSummary`, `RecargaByDate`) would leave `recargas.ts` at a partial-v1 state confusing on its own. Cleaner: keep all 10 alive as a coherent v1 surface, prune as one cohesive diff when Phase 9 Vista Cliente migrates clientes consumers off v1. **Migration path:** Phase 9 swaps `clientes/[empresaId]/page.tsx` to v2 helpers (`filterRecargasV2 + summarizeRecargasV2 + aggregateRechargesByDateV2`) + `EmpresaMiniCards.tsx` prop type swap (`RecargaSummary` → `RecargaSummaryV2`, `RecargaByDate` → `RecargaByDateV2`); after both migrate, all 10 v1 symbols become orphans; final prune deletes the entire v1 block from `recargas.ts` in one cohesive diff. **Phase 9 deferred-prune docket totals 22 symbols across 3 modules** — bonos.ts (8 symbols, Plan 07-02 deferral) + payouts.ts (4 symbols, Plan 07-04 deferral) + recargas.ts (10 symbols, Plan 08-03/08-04 deferral). **No behavior change to Clientes page today** — kept-alive surface is byte-identical; `/clientes/[empresaId]` continues building and rendering identically.
- **Per-tikintag dossier domain module distinct from empresa-INDEX module** (Plan 09-01) — `src/lib/domain/cliente.ts` (singular) created from-scratch for the Vista Cliente v2 dossier, intentionally kept separate from `clientes.ts` (plural) which remains the empresa-INDEX module for the `/clientes` LIST page. Naming convention: SINGULAR file = per-entity dossier, PLURAL file = collection-index surface. The two have different scopes (dossier = CLI-V2-02..08 KPIs/benchmark/P2P/timeline for ONE tikintag; index = CLI-01..04 table of all empresas + monthly activity). Future v2+ sections needing a per-entity dossier surface should follow the same singular/plural split. Plan 09-01 ships 4 fns + 6 types in 678 LOC; consumed by Plans 09-02 (leaves) + 09-03 (page composition). **No deferred-prune debt incurred** — orthogonal to the Phase 7/8 22-symbol prune backlog (which Plan 09-03 finally clears).
- **Lint-clean inter-task commit window for multi-task domain modules** (Plan 09-01 deviation, Rule 3 blocking) — when a plan's `<action>` block prescribes imports/helpers in Task 1 that are only consumed by Task 2's appended functions, defer the imports to Task 2 to avoid `@typescript-eslint/no-unused-vars` warnings during the inter-task commit window. Plan 09-01 Task 1 was supposed to import `formatInTimeZone` + define `startOfDayBogotaTimestamp`/`endOfDayBogotaTimestamp` helpers, but neither Task-1 function (`findClienteSummary`, `aggregateClienteBenchmark`) consumes them — both are whole-history aggregations that don't need day-boundary filtering. Solution: deferred the imports to Task 2 where they're actually used. Inline comment in Task 1 documents the deferral. Plan's full import contract is fulfilled by Task 2's append. Pattern reusable for any multi-task domain module where Task N+1 introduces helpers Task N didn't need. **Distinct from "use eslint-disable":** disabling lint silences the warning but leaves dead code in the inter-task window; deferring the imports keeps the module strictly minimal at every commit boundary. Caveat: when Task N+1 is the SAME plan (atomic commits within a single plan execution), the deferral cost is minimal; when Task N+1 is a DIFFERENT plan (cross-plan helper introduction), the cost flips and inline-with-disable may be cleaner.
- **`BOGOTA_TZ` constant + `formatInTimeZone` import skippable when day-boundary helpers carry the offset inline** (Plan 09-01 simplification) — Plan prescribed `import { formatInTimeZone } from "date-fns-tz"; const BOGOTA_TZ = "America/Bogota";` but neither was needed: the `startOfDayBogotaTimestamp` / `endOfDayBogotaTimestamp` helpers carry the `-05:00` offset inline in `Date.parse(\`${s}T00:00:00-05:00\`)` (Bogotá is UTC-5 with no DST → arithmetic is deterministic). Same convention as `cardUsage.ts` which uses `toBogotaISODate` (a tighter local helper) instead of `formatInTimeZone`. Established convention: when a domain module operates on raw timestamps (not display-formatted strings), inline the offset; only import `formatInTimeZone` when the module needs to emit Bogotá-formatted strings (e.g. `'2026-04-29'` ISO dates in chart bucket keys, like `aggregateRecargasByDate`).
- **Counter-vs-row semantic split in dossier P2P aggregation** (Plan 09-01 CLI-V2-04) — `aggregateClienteP2P` returns BOTH a count/monto split (status-filtered, default `['completed']`) AND a rows array (ALL statuses, capped at 50). Rationale per Phase 9 CONTEXT essentials: KPI counters represent "actividad real" (rejected P2Ps shouldn't bloat the in/out count), but the dossier table is the operator's diagnostic view — they want to see attempted+failed transfers (operator value-prop = "see everything about this client"). Two different views of the same data within one return shape. Reusable pattern for any future dossier aggregation where one section's primary KPI and secondary table view differ on status semantics.
- **Timeline ignores `filters.status` and `filters.tipo`** (Plan 09-01 CLI-V2-05) — `aggregateClienteTimeline` deliberately doesn't filter by status or tipo even though those filters are honored elsewhere in the v2 surface. Rationale: the timeline is the operator's "show everything" view; filtering by status would defeat its purpose (operator opens timeline to investigate WHY a payout failed; hiding failed payouts hides the very thing they're investigating). Status badges are rendered per-row from `event.status` so the user can scan and find what they need. The Bogotá window from `filters.from`/`to` IS honored (it's a user-driven scope-narrowing operator, not a status-narrowing one). Pattern: timelines/event-streams should be status-agnostic; KPIs should be status-filtered.
- **Per-tikintag dossier leaf split — 7 leaves consume 4 domain types + 3 cross-section types** (Plan 09-02 pattern) — leaves are pure presentational (no domain logic, no Sheets imports, no format gates leaked) and consume `ClienteSummary`/`ClienteBenchmark`/`ClienteP2P`/`ClienteTimelineEvent` from cliente.ts (Plan 09-01) plus `BonoSummaryV2` (Plan 07-01) + `PurchaseSummary` (Plan 08-01) + `JoinedPayout` (Plan 06-02). Reusable shape for any future user-centric dossier (operator views, support tools, per-empresa profile pages). 7 leaves: TikintagSelector (Client Component, native `<select>` + `router.push("/clientes/{encodeURIComponent(id)}{?qs}")` preserving searchParams) + ClienteKPIHeader (6-KPI grid with benchmark accent) + RetirosBancoTable (raw `<table>` over `JoinedPayout[]` with `data-presenter-metric-hide` on failure-reason cells) + BonosClienteCards (2-card in/out split) + P2PCards (2 cards + raw `<table>` "Últimas P2P") + ComprasClienteCard (single card 3-stat) + TimelineActivity (Lucide-iconed feed). 1176 LOC across the 7 files; commits `4f60f44` (Task 1: 4 leaves +603 LOC) + `03793f7` (Task 2: 3 leaves +573 LOC).
- **One-section-accent-per-page rule held in dossier composition** (Plan 09-02 reaffirms Plan 08-02) — only ClienteKPIHeader carries `text-section-clientes` / `border-section-clientes` in JSX; the benchmark KPI (#6) gets the accent stripe (`border-l-4 border-section-clientes pl-3`) + `text-section-clientes` headline + delta-sign semáforo subtext. Other 6 leaves use `text-foreground` / `text-muted-foreground` for value classes. Status palette colors (verde/amber/rojo) are independent of section accent and used freely on status badges. This holds the rule strict: ONE section accent per page protagonist.
- **Section accents as taxonomic markers in event streams** (Plan 09-02 TimelineActivity new pattern) — TimelineActivity is the ONE place in the dossier where multiple section accents legitimately co-occur. The icons are colored per ClienteTimelineEventType (PURCHASE→text-section-tarjeta, RECHARGE_*→text-section-recargas, PAYOUT_BANK→text-section-payouts, BONUS_IN/P2P_IN→text-status-success, BONUS_OUT/P2P_OUT→text-foreground, OTRO→text-muted-foreground) — they read as a key/legend (each event "belongs" to a section), NOT as competing emphasis. Distinct from the page-level accent (one-per-protagonist). Future leaves with cross-section event streams can adopt this pattern. Lucide icon mapping: ArrowDownLeft (IN flows), ArrowUpRight (OUT flows), ShoppingCart (PURCHASE), Banknote (RECHARGE_PSE/TRANSFER), CreditCard (PAYOUT_BANK), Activity (OTRO fallback).
- **Counterparty special-case at leaf layer over domain layer** (Plan 09-02 TimelineActivity decision) — when a domain field's UX rendering is unsatisfying TODAY but the domain shape will improve in the future, prefer LEAF-layer override (single edit point) over DOMAIN-layer special-casing (which would propagate the cosmetic concern into the pure aggregation surface). TimelineActivity overrides domain's PURCHASE counterparty (today equals tikintag — would render "$mario → $mario") with literal "Compra tarjeta". Single-line revert when BD_Plataforma surfaces a true merchant column. Pattern reusable: any time a domain field's rendering is awkward but the underlying data shape is correct, the override goes in the leaf, not the aggregation.
- **Presenter-hide separation: leaf carries cell-level opt-outs, page wraps whole-component** (Plan 09-02 codifies the boundary) — leaves carry `data-presenter-metric-hide` ONLY on internal-only sub-elements (RetirosBancoTable failure-reason `<th>` + `<td>`). Whole-component presenter-hide is the PAGE's responsibility — Plan 09-03 wraps `<TimelineActivity>` in `<div data-presenter-hide>` at the page composition layer. Keeps the leaf reusable for future contexts where presenter mode shouldn't apply at all (e.g. internal-only debug panel where the timeline is always visible). Distinct from `KPICardsInicio` pattern where Comisión/Take-rate cards carry `data-presenter-hide` themselves (those cards are ALWAYS internal-only regardless of context).
- **Status-badge-per-leaf-context (no shared util)** (Plan 09-02 reaffirms inline-helper convention) — RetirosBancoTable handles `PayoutState` alphabet ("completed"/"in_progress"/"failed"/"OTRO_STATE"); P2PCards + TimelineActivity handle `Transaction.status` alphabet ("completed"/"rejected"/"failed"/"in_progress"/...). Each leaf carries its OWN `statusBadge()` helper rather than sharing a util — the alphabets are different and the labels are context-specific. Same precedent as AgingAlert's `displayBancoName` / TopBancos's helper. Plan 09-02 also catalogs the "completed badge: shown vs omitted" call: RetirosBancoTable + P2PCards include "Completado/Completada" badge (table contexts where badge presence is the column convention); TimelineActivity OMITS the badge for completed (the absence IS the green light, reduces visual noise in a 200-event feed).
- **Native `<select>` + path-based `router.push` for dossier switching (TikintagSelector)** (Plan 09-02 CLI-V2-01) — mirrors `EmpresaFilter` shape but writes to URL PATH (`/clientes/{id}`) instead of searchParam. `encodeURIComponent` on the tikintag for `$`-prefix safety in URL path segments (RFC 3986: `$mario` → `%24mario`). Native `<select>` over Combobox for the 235-tikintag list (Combobox swap is non-breaking when list grows past ~500 because URL contract is the only stable surface — same Phase 6 EmpresaFilter forward-plan). Imports `useRouter` + `useSearchParams` from `next/navigation` (NOT `usePathname` — path is hardcoded). Preserves all current `searchParams` (filters + presenter mode) across switches per CONTEXT.md essentials "Cambio de cliente reconfigura todo el dossier sin recarga".
- **Inline `Stat` helper component for KPI strip uniformity** (Plan 09-02 ClienteKPIHeader pattern) — when a card carries N visually-uniform sub-blocks with a small variant axis, an inline component beats N hand-rolled divs. ClienteKPIHeader's `Stat` primitive with a `variant: "default" | "benchmark"` prop threads the section accent without scattering Tailwind classes. 6 KPIs share the same shape; benchmark variant adds `border-l-4 border-section-clientes pl-3` + `text-section-clientes` headline. Reusable shape for any future N-KPI strip with one accent variant (Phase 10 Inicio v2 may adopt for hechos curados strip).
- **Per-direction ticket promedio derived locally in dossier leaves** (Plan 09-02 BonosClienteCards) — `BonoSummaryV2.ticketPromedio` is across BOTH directions combined; the dossier UI wants per-direction averages (the value-prop is "qué tan grandes son los bonos que ENVÍA vs los que RECIBE este usuario"). Two lines of arithmetic in the leaf (`ticketIn = countIn > 0 ? montoIn / countIn : 0; ticketOut = countOut > 0 ? montoOut / countOut : 0`) avoid forcing the domain to emit per-direction averages it doesn't need at the section-level page (where the combined ticketPromedio is the right number). Pattern: shape the domain output for the section-level cockpit; let dossier leaves derive their own variants on-the-fly.
- **Vista Cliente v2 page composition pattern: path-based subject + tikintagFilters helper + single-JOIN-per-request budget threaded into 3 consumers** (Plan 09-03) — `/clientes/[empresaId]/page.tsx` rewritten as Server Component dossier composition. The path's `empresaId` is the dossier subject; URL `?empresa=` searchParam is irrelevant on this route (path IS the picker). Built `tikintagFilters = { ...filters, empresa: empresaId }` to thread through v2 domain functions that honor `filters.empresa` (`filterBonosV2`, `filterPurchases`); date filters from URL pass through unchanged. `joinPayouts(allTx, allPayouts)` called ONCE per request — result threaded into THREE consumers: `aggregateClienteBenchmark` + `aggregateClienteTimeline` + narrowed `clientPayouts = joined.filter(p => p.transaction?.empresa_id === empresaId)` for RetirosBancoTable. Mounts 7 leaves in mixed-grid layout (KPI header full-width → RetirosBancoTable full-width → `lg:grid-cols-2` (Bonos | Compras) → P2PCards full-width → TimelineActivity in `<div data-presenter-hide>` → GenerarVistaClienteButton in `<div data-presenter-hide>`). Reusable shape for any future per-entity dossier page (operator views, support tools).
- **Cliente-foco share URL retargeted from /inicio to dossier-itself + presenter mode** (Plan 09-03 contract change) — v1 `GenerarVistaClienteButton` redirected to `/inicio?empresa=…&presenter=1` (cliente landed on Inicio with empresa-scoped filter applied). v2 redirects to `/clientes/{empresaId}?presenter=1` per CONTEXT.md "el URL persiste la selección para que un share-link te lleve al cliente exacto en el modo correcto." Cliente lands on the SAME view operator was looking at when they clicked Generate, in presenter mode. Reusable contract for any future shareable internal-mode page (operator views, support dashboards). Implementation switched from `parseFilters → buildUrl` round-trip (v1) to raw `URLSearchParams.set` (v2): destination URL is a different path that doesn't carry the same filter contract semantics; direct param-by-param copy is cleaner and forces `presenter=1` regardless of incoming `presenter` value.
- **Two-orchestrator presenter-hide model: page wraps whole-component, leaf carries cell-level opt-outs** (Plan 09-03 codifies the boundary established by Plan 09-02) — page composition layer is responsible for `<div data-presenter-hide>` wrappers around whole sections (Plan 09-03 wraps TimelineActivity + GenerarVistaClienteButton); leaves are responsible for `data-presenter-metric-hide` on internal-only sub-elements (Plan 09-02's RetirosBancoTable failure-reason th/td). Keeps leaves reusable for future internal-only contexts where presenter mode shouldn't apply at all (e.g. an internal-only debug panel where the timeline is always visible). Distinct from `KPICardsInicio` pattern where Comisión/Take-rate cards carry `data-presenter-hide` themselves (those cards are ALWAYS internal-only regardless of context). Convention: WHOLE-component presenter-hide is the page's responsibility; CELL-level presenter-metric-hide is the leaf's responsibility.
- **Cohesive multi-module v1 prune in one commit** (Plan 09-03 closes 4 deferred-prune dockets — Plans 07-02 / 07-04 / 08-03 / 08-04) — when a page rewrite frees v1 surfaces across multiple domain modules simultaneously, prune all in ONE `refactor` commit (not one-per-module). Plan 09-03 deleted 22 v1 symbols across `bonos.ts` (8) + `recargas.ts` (10) + `clientes.ts` (4 orphans) in commit `03efba3` (+109 / −901 LOC). Prevents intermediate states where one module is half-pruned. Audit gate (`grep -rE "<symbol>" src/` per symbol) MUST precede the deletion; only JSDoc/comment references are acceptable orphans (cross-module historical notes — e.g. recargas.ts JSDoc citing aggregateBonosByDate's convention — are fine, they don't break compilation). Pattern reusable for any future multi-module refactor where a single page-rewrite frees consumers across files. **payouts.ts v1 surface (4 symbols: filterPayouts, summarizePayouts, PayoutSummary, COMPLETED_PAYOUT_STATES) intentionally NOT touched** in this commit — inicio/page.tsx + HechosCurados.tsx still consume; Phase 10 (Inicio v2 rewrite) closes that loop.
- **404 fallback in-page Card pattern over Next.js notFound()** (Plan 09-03 reaffirms the schema-error Card convention) — when `findClienteSummary` returns null (unknown tikintag), page renders an in-page `<Card>` with "Empresa no encontrada" + decoded tikintag in `<span className="font-mono">` + back-link to `/clientes`. NOT a Next.js `notFound()` (which would invoke the route group's `not-found.tsx` boundary). Same in-page pattern as the Sheets-read error Card already established across all v2 pages. Rationale: keeps the failure mode visually consistent (operator sees a Card, not a generic 404 page) and shows EXACTLY what wasn't found so they can verify the typo. Reusable pattern for any future per-entity page where the entity may not exist.
- **Inicio v2 cross-cut filter — BOTH directions, ALL tipos by default, URL CSV narrows on demand** (Plan 10-01 INI-V2-04) — `filterInicioV2` is the one v2 filter that lets BOTH `direction === 'in'` AND `direction === 'out'` through (drops the v1 `direction !== 'in'` short-circuit) and honors `filters.tipo` CSV when present. Distinct from sibling v2 filters that hard-pin their tipo: `filterBonosV2` BONUS-by-definition / `filterPayoutsV2` PAYOUT-by-table-of-origin / `filterRecargasV2` PAYIN-by-definition / `filterPurchases` PURCHASE-by-definition. Inicio v2 is the cross-cut tab — the global `tipo` CSV multi-select is its native filter; operators want to see PURCHASE-out alongside BONUS-in alongside PAYOUT_BANK-out etc. on this single page. Defensively skips `direction === 'OTRO_DIRECTION'` only — keeps IN/OUT split sums clean if a future Sheet edit introduces a third direction value.
- **`successRate` denominator = `transactions.length`** (Plan 10-01 reaffirms `summarizePayoutsByState` convention) — `summarizeInicioV2.total` is the full row count (NOT `countCompleted + countFailed + countInProgress`). If a future state lands the three named counters underrepresent quietly, but `successRate` denominator stays correct (numerator = completed, denominator = total-attempted). Defensive-by-default; mirror of `payouts.ts:557`.
- **Activity time series: signed volumen + distinct tikintag per Bogotá bucket** (Plan 10-01 INI-V2-05) — `ActivityPointV2.volumen` is a SIGNED sum (no `Math.abs`); operator answer is "what's the net flow that day?" — the IN/OUT split lives on `summarizeInicioV2` KPIs, not the chart. Set-per-bucket dedup keyed by `tikintag` (Pitfall 11 closed at v2 granularity — same convention as v1 `aggregateActiveEmpresasByDate` but keyed by user identity instead of empresa). Single-shape `ActivityPointV2` carries BOTH `usuariosActivos` + `volumen` streams from one aggregation pass — chart leaf can render a line + area on the same x-axis. Both daily (`yyyy-MM-dd`) and weekly (`RRRR-'W'II` ISO week-numbering year) variants share the shape.
- **Top users by volumen NETO with deterministic tiebreak ladder** (Plan 10-01 INI-V2-06) — `aggregateTopUsersByVolume` ranks by `volumenNeto = volumenIn - volumenOut` DESC. Operator-relevant headline: positive = net receiver (more inflow than outflow), negative = net spender; both extremes are interesting. Ranking key is `tikintag` (canonical user identity at v2 ranking layer per STATE.md Plan 08-04 entry); `empresa` column is denormalized first-observed `empresa_nombre` label, NOT the ranking key. Deterministic tiebreak: `volumenNeto` DESC → `transacciones` DESC → `tikintag` lex ASC. Joins the v2 user-lens ranking family alongside Bonos TopEmisores/TopReceptores, Uso Tarjeta TopCardUsers, Recargas TopRechargers.
- **Top-N + Otros rollup over a typed enum widened to string at bucket level** (Plan 10-01 INI-V2-03) — `aggregateTransactionTypeDistribution` returns `TransactionTypeBucket[]` where `tipo: string` (NOT `TransactionType`) so the rolled-up `"Otros"` literal can sit alongside the typed enum values. Same pattern as `BancoStats.medium` from `payouts.ts` (medium union widened to allow rolled-up tail). Default `n=6`; ties broken by `tipo` lex ASC (deterministic across renders); Otros bucket appended after top-N when `groups.length > n`. `share = count / total` where `total = transactions.length` (denominator is FULL filtered input, not visible-N slice — visible shares always sum to ≤ 1.0; Otros makes them sum to exactly 1.0). Empty input → `[]` (no "Otros: 0" placeholder).
- **5th instance of v2-alongside-v1 coexistence pattern** (Plan 10-01 — joins bonos.ts / payouts.ts / recargas.ts / cardUsage.ts-from-scratch) — `inicio.ts` extended with v2 surface appended below v1 block; v1 lines 1-361 byte-identical (single diff hunk at line 362, 568 LOC appended). Section-header comment block separates v1 from v2; v2 reuses v1's `formatInTimeZone` import + `BOGOTA_TZ` constant + `startOfDayBogotaTimestamp`/`endOfDayBogotaTimestamp` helpers without duplication. Plan 10-02 will swap `inicio/page.tsx` imports + prune the v1 inicio block + the 4 v1 payouts.ts symbols + `inicio-hechos.ts` entirely in one cohesive `refactor(10-02)` commit — final v1 → v2 migration close-out.
- **Stream-idle timeout recovery via fresh-agent + filesystem-state inspection — canonical pattern for mid-task timeouts** (Plan 08-04 Generation 1 → Generation 2 handoff, NEW recovery mode in v2.0) — Generation 1 (original execute-plan agent) built all 5 v2 leaf components on disk (589 LOC) and was stream-idle-killed BEFORE the Task 1 commit. Filesystem state at termination: 5 untracked files in `src/components/recargas/`, tsc-clean, lint-clean. Generation 2 (recovery continuation agent) was spawned fresh; inspected filesystem via `git status --short` (5 untracked files matching plan spec); audited each file individually (read + tsc smoke + lint smoke + grep for surgical section accent); confirmed all 5 matched the plan's must_haves verbatim; committed verbatim as Task 1 (`0619d65`); continued cleanly to Task 2. **Distinct from checkpoint-resume:** checkpoint-resume carries explicit completed-task hashes in the prompt's `<completed_tasks>` table — fresh agent verifies hashes appear in `git log`, skips completed tasks, resumes from documented checkpoint. **Mid-task stream-idle recovery has NO commits to verify against** — previous agent's progress is on disk, NOT in git history. Fresh agent inspects filesystem state, validates against plan spec via tsc/lint/grep, commits verbatim, then resumes. **More flexible than checkpoint-resume** (works for any work-in-progress that's tsc-clean) but requires the recovery agent to do an audit pass before committing. **Catalogued as a canonical recovery pattern for future agents recovering from analogous timeouts**; complements the existing checkpoint-resume pattern (paused-agent handoff with explicit commit hashes) and the soft-reset+restore-staged pattern (Plan 08-02 commit-time race recovery). Three recovery modes now in the toolkit: pathspec-discipline-only races (silent, no recovery needed); commit-transaction races (soft-reset+restore-staged); mid-task timeouts with on-disk artifacts (audit + verbatim commit + resume).

### Pending Todos

Ninguno aún para v2.0. Carry-forwards de v1.0 deferreds NO se trasladan (decisión 2026-05-07 — quedan en `milestones/v1.0-REQUIREMENTS.md` para referencia histórica; surgen caso por caso vía `/gsd:add-todo`).

### Blockers/Concerns (carry-forward al milestone v2.0)

- **Sheet header H corregido por usuario 2026-05-07** — verificación end-to-end pendiente (no recargó browser después de "Ya"). Riesgo: cualquier edición upstream rompe el adapter; schema validation captura el error pero requiere intervención manual.
- **INFRA-04 (custom domain) deferido desde v1.0** — Plan 05-05 pospuesto. Decisión pendiente upfront: subdomain / apex / defer definitivo. Cliente-flow LIVE en `.vercel.app` con SSO de Vercel.
- **Vercel Deployment Protection ENABLED por defecto** — la URL `.vercel.app` requiere SSO. Mitigación: custom domain (INFRA-04) bypassa el SSO.
- **3 deudas de seguridad de v1.0** documentadas en `01-04-SUMMARY.md`: GCP key NO rotada, password 5-char, env vars solo Production target.
- **Vercel CLI fuera de PATH** — pre-condición: `export PATH="$HOME/.nvm/versions/node/v24.11.0/bin:$PATH"` para deploy tasks.
- **Parallel-wave git race** observado 3 veces en v1.0 — recovery: `git stash --include-untracked` o `git commit -- <pathspec>`.
- **dotenv-expand bug** — bcrypt hashes con salt empezando en letra/dígito se corrompen al ser leídos por `@next/env`. Mitigación: escape `\$` en `.env.local`.

## Session Continuity

Last session: 2026-05-08 — **Phase 10 Wave 1 SHIPPED (Plan 10-01 LANDED — Inicio v2 operative-lens domain surface)**. `src/lib/domain/inicio.ts` extended with 6 v2 functions + 4 v2 types appended below v1 block (361 → 929 LOC, +568 LOC append, v1 lines 1-361 byte-identical verified via single diff hunk at line 362). **6 v2 functions:** `filterInicioV2` (state-UNFILTERED CSV-status + CSV-tipo + Bogotá-anchored cross-cut filter; BOTH directions allowed, defensively skips OTRO_DIRECTION only) + `summarizeInicioV2` (single-pass reduce: usuariosActivos DISTINCT tikintag + volumenIn signed + volumenOut abs + countCompleted/Failed/InProgress + total + successRate zero-safe) + `aggregateTransactionTypeDistribution` (top-N + Otros rollup; ties broken by tipo lex ASC; default n=6) + `aggregateActivityByDateV2` (Bogotá daily buckets, distinct-tikintag per day, signed volumen) + `aggregateActivityByWeekV2` (ISO `RRRR-Www` weekly buckets) + `aggregateTopUsersByVolume` (top-N tikintag ranking by `volumenNeto = volumenIn - volumenOut` DESC; tiebreak ladder: transacciones DESC → tikintag lex ASC). **4 v2 types:** `InicioSummaryV2`, `TransactionTypeBucket` (tipo widened to string for "Otros" literal), `ActivityPointV2` (single-shape carries both usuariosActivos + signed volumen streams), `TopUserVolumeRow`. **Key decisions:** (a) cross-cut filter — BOTH directions + ALL tipos by default, URL CSV narrows on demand; distinct from sibling v2 filters that hard-pin their tipo (Inicio is the operative-health page, operators want PURCHASE-out alongside BONUS-in alongside PAYOUT_BANK-out on the same view); (b) successRate denominator = transactions.length (NOT count-sum) — mirrors `summarizePayoutsByState.total` convention; defensive against future state additions; (c) activity time series volumen is SIGNED (NOT abs) — operator answer is "net flow that day"; IN/OUT split lives on KPIs not chart; (d) top users grouped by tikintag (NOT empresa) — explicit v1→v2 user-lens shift catalogued in STATE.md Plan 08-04 entry; volumenNeto = volumenIn - volumenOut as signed ranking key (positive = net receiver, negative = net spender; both extremes interesting); (e) top-N + Otros rollup uses tipo: string (widened from TransactionType) so "Otros" literal sits alongside typed enum values — same pattern as `BancoStats.medium` in `payouts.ts`; (f) reused v1 imports (formatInTimeZone, DashboardFilters, Transaction, TransactionType) and v1 day-boundary helpers — no duplication, no new helpers; (g) section-header comment block separates v1 from v2 (mirror of bonos.ts / recargas.ts v2 idiom); fifth instance of v2-alongside-v1 coexistence pattern. **Field-name reconciliation: NONE** — fifth clean-break plan in a row (after Plans 09-01, 09-02, 09-03; plan vocabulary matched types.ts exactly, no PRD-vocabulary translation needed). **No Rule deviations** — plan executed exactly as written. **Minor count quibble (NOT a deviation):** plan's `<verify>` clause expected "16 exports total" (counted v1 as 4 fns + 4 interfaces, missing the 2 v1 weekly aggregations); actual surface is 20 exports (10 v1 + 10 v2 — 5 interfaces + 5 functions per side or, counted by v2-only delivery: 6 fns + 4 interfaces). Action specs in `<task>` blocks were honored 1-for-1; the count was a verify-clause undercount. tsc + lint (0 errors, 3 baseline warnings unchanged) + `npm run build` (still 13 routes — v1 still consumed by `inicio/page.tsx` + `HechosCurados.tsx` + `inicio-hechos.ts` until Plan 10-02 swaps imports) all green. Two atomic commits: Task 1 `8ea04b6` (KPI summary + tipo distribution: filter + summarize + tipo-distribution + 2 types, +321 LOC) + Task 2 `12d0710` (activity series + top users: 2 activity aggregations + top-users + 2 types, +247 LOC). Duration ~2 min implementation. **Plan 10-02 ready to consume the v2 surface** — page rewrite + cohesive prune of v1 inicio block + 4 v1 payouts.ts symbols (`filterPayouts`, `summarizePayouts`, `PayoutSummary`, `COMPLETED_PAYOUT_STATES`) + `inicio-hechos.ts` entirely in one `refactor(10-02)` commit closes the v1 → v2 migration end-to-end.

Earlier session: 2026-05-08 — **Phase 9 SHIPPED ✅ (Plan 09-03 LANDED — Vista Cliente v2 dossier LIVE + 22-symbol cohesive prune)**. `/clientes/[empresaId]/page.tsx` rewritten as Vista Cliente v2 dossier composition (229 → 289 LOC): mounts 7 leaves from Plan 09-02 (TikintagSelector + ClienteKPIHeader + RetirosBancoTable + BonosClienteCards + ComprasClienteCard + P2PCards + TimelineActivity) + GenerarVistaClienteButton. Single `joinPayouts(allTx, allPayouts)` call threaded into 3 consumers (`aggregateClienteBenchmark` + `aggregateClienteTimeline` + narrowed `clientPayouts` for RetirosBancoTable) — Plan 06-02 contract preserved. Path-based dossier subject (URL `?empresa=` ignored on this route — path IS the picker); `tikintagFilters = { ...filters, empresa: empresaId }` threaded to v2 domain functions that honor `filters.empresa`. 404 fallback in-page Card when `findClienteSummary === null` (NOT next.js `notFound()`); inline error Card on Sheets read failure. **Cliente-foco share URL retargeted** — `GenerarVistaClienteButton` now generates `/clientes/{empresaId}?presenter=1` (was `/inicio?empresa=…&presenter=1` in v1) per CONTEXT.md. **Two-orchestrator presenter-hide model codified**: page wraps whole-component (TimelineActivity + GenerarVistaClienteButton in `<div data-presenter-hide>`); leaf carries cell-level opt-outs (RetirosBancoTable failure-reason cells already had `data-presenter-metric-hide` from Plan 09-02). **3 v1 leaf component files DELETED** (orphaned after page rewrite): EmpresaProfileHeader.tsx + EmpresaMiniCards.tsx + EmpresaActivityChart.tsx (~530 LOC removed). **22 v1 domain symbols PRUNED in one cohesive `refactor` commit** across 3 modules: bonos.ts (8 symbols, 534 → 297 LOC) + recargas.ts (10 symbols, 895 → 569 LOC) + clientes.ts (4 orphans + helper subMonthsLabel + unused formatInTimeZone import + BOGOTA_TZ constant, 516 → 287 LOC). All v2 surfaces preserved byte-identical; empresas-INDEX surface in clientes.ts preserved for /clientes list page. **payouts.ts v1 surface (4 symbols) intentionally NOT touched** — inicio/page.tsx + HechosCurados.tsx still consume; Phase 10 closes that loop. **Audit gate before deletion**: `grep -rE "<symbol>" src/` per each of 22 symbols + 3 v1 leaves — confirmed only JSDoc/comment references survived (cross-module historical notes acceptable, they don't break compilation). Net Phase 9 closeout LOC: page (+211/−397) + 3 v1 leaves (~−530) + 3 modules (+109/−901) = **~−1500 LOC removed despite delivering richer 6-KPI cabecera + benchmark + 5 sections + presenter-hide system**. **Field-name reconciliation: NONE** (third clean-break plan in a row after 09-01 and 09-02). **No Rule deviations** — plan executed exactly as written. Two atomic commits: Task 1 `e8d3d09` (page rewrite + button retarget + 3 v1 leaves removed; +211/−397) + Task 2 `03efba3` (22-symbol prune; +109/−901). User typed "approved" at visual checkpoint after verifying: 6-KPI cabecera with benchmark Emerald accent + delta-sign semáforo subtext; TikintagSelector dropdown switches dossier preserving searchParams (`?from/&to` filter survives switching); all 5 sections render; presenter mode hides timeline + GenerarVistaClienteButton + RetirosBancoTable failure-reason column; cabecera + 4 other sections remain visible in presenter; cliente-foco share URL lands on `/clientes/{tikintag}?presenter=1`; 404 fallback works; dark mode preserves Emerald accent; no console errors. tsc + lint (0 errors, 3 baseline warnings unchanged) + `npm run build` (still 13 routes — `/clientes/[empresaId]` rebuilt in place; no new routes) all green. Duration 16 min. **All 8 CLI-V2-* requirements MET**. **Phase 9 deferred-prune docket CLOSED** (modulo payouts.ts v1 surface for Phase 10).

Previous session: 2026-05-08 — **Phase 9 Wave 2 leaves SHIPPED (Plan 09-02 LANDED)**. 7 leaf components in `src/components/clientes/` (1176 LOC across `TikintagSelector` 79 LOC + `ClienteKPIHeader` 235 LOC + `RetirosBancoTable` 192 LOC + `BonosClienteCards` 97 LOC + `P2PCards` 207 LOC + `ComprasClienteCard` 96 LOC + `TimelineActivity` 270 LOC). Among the 7 NEW files, only `TikintagSelector` is a Client Component (`"use client"` + `next/navigation` hooks); other 6 are Server Components. Open questions from 09-01 SUMMARY resolved at this leaf layer: (a) Lucide icon mapping — 9 ClienteTimelineEventType values map to 6 distinct Lucide icons (ArrowDownLeft for IN flows, ArrowUpRight for OUT, ShoppingCart for PURCHASE, Banknote for RECHARGE_*, CreditCard for PAYOUT_BANK, Activity fallback for OTRO); (b) Status palette per leaf — each leaf carries its own inline `statusBadge()` over local alphabet (RetirosBancoTable handles PayoutState, P2PCards + TimelineActivity handle Transaction.status); (c) PURCHASE counterparty visual special-case — overridden at leaf layer to literal "Compra tarjeta" (single edit point when BD_Plataforma surfaces true merchant column); (d) Empty-state UX — consistent muted-foreground voice ("Sin retiros para este tikintag en el período seleccionado.", etc.). One-section-accent-per-page rule held strict: only `ClienteKPIHeader` has `text-section-clientes`/`border-section-clientes` JSX (the benchmark KPI #6 with delta-sign semáforo subtext). TimelineActivity icons use multiple section accents as TAXONOMIC markers (each event "belongs" to a section), not page-level emphasis — orthogonal to the one-accent rule. Presenter-hide separation codified: leaves carry cell-level opt-outs (RetirosBancoTable failure-reason th/td have `data-presenter-metric-hide`); whole-component presenter-hide is page's responsibility (Plan 09-03 wraps TimelineActivity in `<div data-presenter-hide>`). NO field-name reconciliation needed (clean break, like 09-01). NO Rule deviations. tsc + lint (0 errors, 3 baseline warnings unchanged) + `npm run build` (still 13 routes; page composition lands in 09-03) all green. Two atomic commits: Task 1 `4f60f44` (4 leaves: TikintagSelector + ClienteKPIHeader + RetirosBancoTable + BonosClienteCards, +603 LOC) + Task 2 `03793f7` (3 leaves: P2PCards + ComprasClienteCard + TimelineActivity, +573 LOC). Duration 7 min.

Earlier session: 2026-05-08 — **Phase 9 Wave 1 SHIPPED (Plan 09-01 LANDED)**. `src/lib/domain/cliente.ts` from-scratch per-tikintag dossier domain module: 4 pure aggregations (`findClienteSummary` CLI-V2-02, `aggregateClienteBenchmark` CLI-V2-07, `aggregateClienteP2P` CLI-V2-04, `aggregateClienteTimeline` CLI-V2-05/V2-08) + 6 types (ClienteSummary, ClienteBenchmark, ClienteP2P, ClienteP2PRow, ClienteTimelineEvent, ClienteTimelineEventType 9-value union). 678 LOC (~291 code-only + ~387 comments), pure (no `next/`, `react`, `server-only`, `lib/sheets/`, `lib/format` imports), Bogotá-anchored. JoinedPayout consumed in two aggregations (benchmark + timeline) — confirms one-JOIN-per-request page-composition contract. Distinct from `clientes.ts` (plural, empresa-INDEX module). Two atomic commits (Task 1 `1e1fee7`: scaffold + summary + benchmark; Task 2 `a7742eb`: P2P + timeline). tsc + lint (0 errors, 3 baseline warnings unchanged) + `npm run build` (13 routes) all green. **Phase 9 deferred-prune backlog (22 v1 symbols across bonos.ts/payouts.ts/recargas.ts) UNTOUCHED in this plan** — Plan 09-03 (page composition) is the prune-completion plan. Plan 09-01 had ZERO field-name reconciliation deviations (plan vocabulary matches types.ts exactly — clean break from the 4× pattern observed in Plans 07-01/08-01/08-03/08-04). Two Rule-3 auto-fixes documented: (1) lint-clean inter-task commit window (deferred Task-1-unused imports/helpers to Task 2); (2) skipped unused `BOGOTA_TZ` constant + `formatInTimeZone` import (helpers carry the offset inline, same as `cardUsage.ts`).

**v2.0 actions tomadas hoy:**
- ✅ `/gsd:new-milestone` — milestone iniciado
- ✅ `/gsd:define-requirements` — 51 requirements en 8 categorías (REQUIREMENTS.md)
- ✅ `/gsd:create-roadmap` — 5 fases (6-10) con success criteria + research flags + traceability completo
- ✅ `/gsd:plan-phase 6` — Phase 6 plans creados (06-01 a 06-04)
- ✅ `/gsd:execute-plan 06-01` — `src/lib/domain/parsers.ts` creado; `schemas.ts` refactorizado a delegar; tsc + lint + build green
- ✅ `/gsd:execute-plan 06-03` (Wave 1) — `DashboardFilters.status`/`tipo` + `StatusFilter`/`TypeFilter` + header wired; tsc + lint + build green; zero deps añadidas
- ✅ `/gsd:execute-plan 06-02` (Wave 1) — `src/lib/domain/join.ts` (148 LOC) con `joinPayouts`/`joinIndex`/`joinMatchStats`/`JoinedPayout`; live match-rate 768/797 (96.36%, ≥96% threshold); tsc + lint + build green; zero deps añadidas
- ✅ `/gsd:execute-plan 06-04` (Wave 2) — next-themes ThemeProvider + ThemeToggle + 9 paleta CSS vars (light + dark) + `data-presenter-metric-hide` rule + header polish; tsc + lint + build green; zero deps añadidas; user-approved visual checkpoint

**Phase 7 Wave 1 actions (2026-05-07, parallel orchestration):**
- ✅ `/gsd:execute-plan 07-01` (Wave 1) — Bonos domain v2: `filterBonosV2` + `summarizeBonosV2` + `aggregateBonosByDateV2` + `aggregateTopEmisores` + `aggregateTopReceptores` + 3 v2 types (`BonoSummaryV2`, `BonoByDateV2`, `BonoTikintagRow`) + `Transaction.sourceTransferTikintag` / `destinationTransferTikintag` (pure-add 2-edit schema); v1 byte-identical; tsc + lint (0 errors) + build green; parallel-wave git race con sibling 07-03 manejado vía explicit `--` pathspec
- ✅ `/gsd:execute-plan 07-03` (Wave 1) — Payouts domain v2: `filterPayoutsV2` + `summarizePayoutsByState` + `aggregateAverageProcessingMinutes` + `aggregateAgingAlertPending` + `aggregateFailureReasons` + `aggregateThirdPartyPayouts` (first production consumer of Plan 06-02 `joinPayouts()`); 4 new types; v1 byte-identical; tsc + lint + build green; zero deps; no parallel-wave git race despite shared `src/lib/domain/` directory

**Phase 7 Wave 2 actions (2026-05-07, parallel orchestration):**
- ✅ `/gsd:execute-plan 07-04` (Wave 2) — Payouts page rebuild: 5 new v2 leaf components (PayoutsKPICardsV2, AgingAlert, StatusBreakdownCards, FailureReasons, ThirdPartyPayouts) + TopBancos restyle (section-payouts left-edge + per-row volume bars) + `formatMinutes` helper + page rewrite with TIME-FIRST cockpit (KPI header → AgingAlert conditional → StatusBreakdown → TopBancos → FailureReasons / ThirdParty in lg:grid-cols-2) + first production consumer of `joinPayouts()` chained into `aggregateThirdPartyPayouts`. **Partial v1 prune** (Rule 4 deviation): deleted 7 symbols whose only consumer was the rewritten page + 2 deleted v1 leaves (`PayoutsKPICards.tsx`, `LatencyHistogram.tsx`); KEPT alive 4 symbols (`filterPayouts`, `summarizePayouts`, `PayoutSummary`, `COMPLETED_PAYOUT_STATES`) still consumed by Inicio + Clientes pages — deferred to Phase 9 + Phase 10 page rewrites. tsc + lint (0 errors, 3 pre-existing warnings unchanged) + build all green; visual checkpoint approved by user.
- ✅ `/gsd:execute-plan 07-02` (Wave 2) — Bonos page rebuild: 4 new v2 leaf components (KPICardsV2, TopEmisores, TopReceptores, BonosFlowChart, 430 LOC added) + page rewrite with RANKING-FIRST cockpit (KPICardsV2 5-card header → TopEmisores | TopReceptores side-by-side at lg:grid-cols-2 → BonosFlowChart stacked Recharts timeline below) + 4 v1 component files deleted (KPICards, Leaderboard, SalesTable, BonosChart, 356 LOC removed; bonos/page was sole consumer). **Partial v1 prune** (Rule 4 deviation, mirrors 07-04 pattern): KEPT alive 8 v1 symbols in `bonos.ts` (`filterBonos`, `summarizeBonos`, `aggregateBonosByDate`, `aggregateBonosByEmpresa`, `top10Empresas` + interfaces `BonoSummary` / `BonoByDate` / `BonoByEmpresa`) because `clientes/[empresaId]/page.tsx` + `EmpresaMiniCards.tsx` still consume them — deferred to Phase 9 Vista Cliente rewrite or a small `07-02b` plan. Page commits `6c74f52` + `e6f9ab6`. tsc + lint (0 errors, 3 pre-existing warnings unchanged) + build all green; visual checkpoint approved by user. Parallel-wave git race with sibling 07-04 handled via `git stash --include-untracked --pathspec` for isolated tsc/lint/build verification + explicit `git add --` pathspec staging on every commit.

**Phase 8 Wave 1 actions (2026-05-07, parallel orchestration):**
- ✅ `/gsd:execute-plan 08-01` (Wave 1) — Uso Tarjeta domain v2: `src/lib/domain/cardUsage.ts` from-scratch (NEW file, 380 LOC, no v1 predecessor — Uso Tarjeta is a brand-new section in v2.0 per PRD): 5 functions (`filterPurchases` + `summarizePurchases` + `aggregatePurchaseAdoption` + `aggregatePurchasesByDate` + `aggregateTopCardUsers`) + 4 interfaces (`PurchaseSummary`, `PurchaseAdoption`, `PurchaseByDate`, `TopCardUser`) — 9 exports total per must_haves. PURCHASE direction=out filter contract; CSV `filters.status` honored with default `["completed"]`; `filters.tipo` ignored (PURCHASE-by-definition). **Key decisions:** two-arg adoption signature `(allTx, purchaseRows)` for one-filter-pass-multiple-aggregations budget; `Math.abs(monto)` because PURCHASE-out montos are negative in BD_Plataforma; default `limit=10` for top users with deterministic tiebreak (volumenCOP DESC, then compras DESC); inlined Bogotá date helper (10 lines, deterministic +/-5h shift, no DST) over importing `formatInTimeZone`; no granularity arg in by-date (UI re-buckets; same convention as `aggregateRecargasByDate`); no zero-fill (chart leaf handles empty axis); `TopCardUser.empresa: string | undefined` defensive for future schema evolution. **From-scratch pattern:** no v1 to coexist with, no partial-prune debt incurred — clean greenfield module. **Field-name translation:** plan pseudo-code referenced `tx.transferTikintag` / `transferEmpresa` / `transactionAmount` / `tx.date` / `filters.period.from`; actual `Transaction` uses `tikintag` / `empresa_nombre` / `monto` / `fecha`, and `DashboardFilters` is flat. Translated to actual fields per @-context contract; documented in SUMMARY (NOT counted as Rule deviation since types.ts is authoritative). **Verification adjustment:** project lint is `npm run lint` (eslint), NOT `npx next lint` (Next 16 dropped the subcommand). tsc + eslint (0 errors, 3 pre-existing warnings unchanged from baseline) + `npm run build` all green; commits `0292834` (scaffold) + `7bf2063` (3 aggregations). **Parallel-wave race observation:** sibling 08-03 had +265 LOC of uncommitted `recargas.ts` edits in worktree at my Task-2 commit time; explicit `git add -- src/lib/domain/cardUsage.ts` pathspec staging dodged the race; sibling's worktree state survived untouched; sibling's commits (`af8d2bb`, `d07ed47`) interleaved naturally with mine in chronological log. NEW observation: when both racers are append-only on disjoint files, tsc/lint/build pass cleanly mid-flight — silent race, no recovery needed beyond pathspec discipline.
- ✅ `/gsd:execute-plan 08-03` (Wave 1) — Recargas domain v2: 7 v2 functions (`filterRecargasV2` + `summarizeRecargasV2` + `aggregateRechargesByDateV2` + `aggregateRechargeAdoption` + `aggregateRechargeMethodSplit` + `aggregateRechargeAmountDistribution` + `aggregateTopRechargers`) + 6 v2 types (`RecargaSummaryV2`, `RecargaByDateV2`, `RechargeAdoption`, `RechargeMethodSplit`, `RechargeAmountBucket`, `TopRecharger`) appended below v1 in `src/lib/domain/recargas.ts` (433 → 895 lines, +462 net append, 0 deletions). v1 surface 100% byte-identical (10 v1 exports preserved at original line numbers — `filterRecargas`, `summarizeRecargas`, `aggregateRecargasByDate`, `aggregateRecargasByEmpresa`, `top10RecargasEmpresas`, `findTopEmpresaRecargadora`, `findRecargaMasGrande`, `RecargaSummary`, `RecargaByDate`, `RecargaByEmpresa`) — still consumed by `clientes/[empresaId]/page.tsx` + `EmpresaMiniCards.tsx`; deferred-prune to Phase 9 Vista Cliente rewrite (third instance of the deferral pattern from Plans 07-02 / 07-04). **Key decisions:** count-based share in `RechargeMethodSplit` (NOT volume; matches PRD "85% PSE / 15% Transfer" reading); literal `{pse, transfer}` shape NOT array; `aggregateRechargesByDateV2` RICHER than v1 (per-method PSE/TRANSFER split vs totals-only); amount distribution boundary inclusivity unambiguous (bottom strict, middle inclusive both ends, top strict); always returns all 3 buckets for stable axis; top rechargers grouped by tikintag (NOT empresa_id) for future-proofing; reused module-private `RECHARGE_TIPOS` constant — did NOT redeclare. **One auto-fix [Rule 3 - Blocking]:** PLAN.md cited fields not on Transaction (`transactionAmount`, `transferTikintag`, `transferEmpresa`) → mapped to actual fields per types.ts (`monto`, `tikintag`, `empresa_id`/`empresa_nombre`); reconciliation documented in v2 section header comment. tsc + lint (0 errors, 3 pre-existing warnings unchanged) + build all green; commits `af8d2bb` + `d07ed47`. **Parallel-wave race observation (third in v2.0):** sibling 08-01 created/edited NEW file `cardUsage.ts` while I was modifying `recargas.ts`; explicit `git add -- src/lib/domain/recargas.ts` pathspec at every commit dodged the race cleanly — no merge conflicts, no rebase, interleaved commit history (mine + sibling's commits sequenced naturally). Confirms STATE.md guidance: parallel-wave race is a NON-EVENT when (a) explicit pathspec only, (b) disjoint files.

**Phase 8 Wave 2 actions (2026-05-07, parallel orchestration):**
- ✅ `/gsd:execute-plan 08-02` (Wave 2) — Uso Tarjeta page rebuild: 4 new v2 leaf components (KPICardsCardUsage 91 LOC, AdoptionCard 66 LOC, PurchaseTrendChart 146 LOC, TopCardUsers 107 LOC ≈ 410 LOC) + page composition (`src/app/(protected)/uso-tarjeta/page.tsx`, 166 LOC) + TabNav grown from 5 to 6 entries (single-line TABS-array surgical edit; PRD v2 reading order Inicio · Bonos · Payouts · Uso Tarjeta · Clientes · Recargas; Recargas reordered LAST). First brand-new v2.0 protected route to land in production composition (every other tab is a v1 carryforward). Page pipeline: parseFilters → getCachedTransactions (try/catch with inline error Card) → filterPurchases ONCE → 4 aggregations chained (summary + adoption + by-date + top-users). Layout: ADOPTION-FIRST cockpit (KPI strip → AdoptionCard → trend chart Card → top users Card; single-column stack, no `lg:grid-cols-2` diagnostic row). Server Components everywhere except `PurchaseTrendChart.tsx` (Recharts requires DOM). **Key decisions:** section accent (Amber `text-section-tarjeta`) on EXACTLY ONE focal metric — KPICardsCardUsage primary "Compras totales" card text-4xl; AdoptionCard intentionally uses `text-foreground` (one-accent-per-page rule established for v2.0); **adoption denominator = FULL `getCachedTransactions().rows` (NOT period-filtered)** — preserves PRD baseline 40/235 ≈ 17% global-pool reading; numerator IS period-filtered; PurchaseTrendChart single-series LineChart of compras (count) with COP volume in tooltip only (dual-axis dropped); hard-coded OKLCH stroke `oklch(0.65 0.18 75)` (Amber, section-tarjeta hue, same idiom as BonosFlowChart violet shades); raw `<table>` markup for TopCardUsers (Plan 07-02 deviation pattern reaffirmed — no shadcn `ui/table` primitive in repo); `dynamic = "force-dynamic"`; `metadata.title = "Uso Tarjeta · Tikin Dashboard"`. **Three [Rule 3 - Blocking] auto-fixes:** (1) field-name reconciliation — plan pseudo-code used PRD-vocabulary names (`tx.transferTikintag`, `transactionAmount`, `transferEmpresa`, `tx.date`); actual fields are `tikintag`, `monto`, `empresa_nombre`/`empresa_id`, `fecha`. `DashboardFilters` is FLAT (`from`/`to`), NOT nested under `period`. Translated inline (same pattern hit by every Wave 1 + Wave 2 executor in Phase 8 — recurring author-shorthand vs executor-translation gap in v2.0 plan vocabulary). (2) **Parallel-wave commit-time race [NEW MODE]** — during Task 1 commit transaction (between `git add -- <pathspec>` and `git commit -m`), sibling 08-04 created 5 NEW untracked files in `src/components/recargas/`; they leaked into my explicit-pathspec commit because they appeared from nothing during the transaction window. Recovery: `git reset --soft HEAD~1 + git restore --staged -- src/components/recargas/<five files>` (unstage sibling's files; they stay on disk untracked for sibling to commit), then re-staged + re-committed Task 1 cleanly. Sibling's worktree state survived; no rebase needed. Pattern documented as canonical recovery for the "untracked-file appearance during commit transaction" race mode (stronger than the disjoint-dir pure-pathspec races documented in earlier phases). (3) Project lint runner — `npm run lint` (eslint), NOT `npx next lint` (Next 16 dropped the subcommand; `package.json` declares `"lint": "eslint"`). Visual checkpoint user-approved without issues (TabNav 6-tab order verified; KPI section accent visible on "Compras totales"; AdoptionCard percentage + subtext visible; Recharts trend chart no console errors with locale-aware tooltip; TopCardUsers ≤10 rows right-aligned tabular-nums; dark mode + Modo Presentación + cliente-foco URL all confirmed). tsc + lint (0 errors, 3 pre-existing warnings unchanged) + `npm run build` all green (13 routes — was 12 before this plan; `/uso-tarjeta` is the new addition). Commits `27e0ac5` (Task 1: 4 leaves, +410 LOC) + `3ec9a8d` (Task 2: page + TabNav, +168 −1 LOC).
- ✅ `/gsd:execute-plan 08-04` (Wave 2) — Recargas page rebuild: 5 new v2 leaf components in `src/components/recargas/` (RecargasKPICardsV2 125 LOC, MethodSplitCard 110 LOC, AmountDistribution 98 LOC, TopRechargers 102 LOC, RecargasTrendChartV2 154 LOC = 589 LOC total) + page rewrite (`src/app/(protected)/recargas/page.tsx`, 185 LOC final, +108 −98 over v1) with **METHOD-AND-DISTRIBUTION-FIRST cockpit** (KPICardsV2 4-card strip → MethodSplitCard | AmountDistribution at `lg:grid-cols-2` → TopRechargers full-width Card → RecargasTrendChartV2 stacked PSE/TRANSFER timeline full-width Card) + 4 v1 leaves DELETED audit-clean (`RecargasKPICards.tsx` 75 LOC + `RecargasTable.tsx` 94 LOC + `HechosCuradosRecargas.tsx` 104 LOC + `RecargasTrendChart.tsx` 94 LOC = 367 LOC removed; `/recargas/page.tsx` was sole consumer for all 4 — audit gate clean, no surprise consumers). **`recargas.ts` UNTOUCHED in this plan** (verified `git log d07ed47..HEAD -- src/lib/domain/recargas.ts` empty across all 3 commits) — 10 v1 domain symbols KEPT alive (mirror of Plans 07-02 / 07-04 / 08-03 deferral pattern, fourth instance) deferred to Phase 9 Vista Cliente rewrite. Server Components everywhere except `RecargasTrendChartV2.tsx` (Recharts requires DOM). Page pipeline: parseFilters → getCachedTransactions (try/catch with inline error Card) → filterRecargasV2 ONCE → 6 aggregations chained (summarize + adoption + methodSplit + amountDistribution + topRechargers + byDateV2). **Key decisions:** section accent `text-section-recargas` (Teal) on EXACTLY ONE focal metric — RecargasKPICardsV2 primary "Total recargas" card text-4xl (verified `grep -rE` exactly 1 match across 5 leaves); **adoption denominator = full `txResult.rows` pool (NOT period-filtered)** — same convention as Plan 08-02; method-and-distribution-first layout adds the FOURTH cockpit shape to the v2.0 vocabulary (joins time-first 07-04, ranking-first 07-02, adoption-first 08-02); RecargasTrendChartV2 chose **stacked BarChart over two-series LineChart** — PRD baseline is a count SHARE story (85% PSE / 15% TRANSFER), stacked bars at each date make share visually immediate; OKLCH-pinned fills (PSE = `oklch(0.62 0.10 200)`, TRANSFER = `oklch(0.42 0.10 200)`); Tooltip shows PSE + TRANSFER + total; raw `<table>` markup for TopRechargers (Plan 07-02 deviation pattern reaffirmed); top rechargers grouped by `tikintag` NOT `empresa` — explicit v1→v2 semantic shift per PRD REC-V2-07; **prior-period KPI badges DROPPED** (PRD lens shift: REC-V2-* requirements describe absolute counts/volumes/shares, not deltas); no `data-presenter-*` attributes (conservative-default per CROSS-V2-07); `dynamic = "force-dynamic"`. **Three [Rule 3 - Blocking] auto-fixes + 1 recovery context:** (1) field-name reconciliation — plan pseudo-code used PRD-vocabulary names (`tx.transferTikintag`, `transactionAmount`, `transferEmpresa`, `tx.date`); actual fields are `tikintag`, `monto`, `empresa_nombre`/`empresa_id`, `fecha`. `DashboardFilters` is FLAT (`from`/`to`), NOT nested under `period`. Translated inline at every call site (same pattern every Phase 8 executor hit independently — recurring author-shorthand vs executor-translation pair in v2.0 plan vocabulary). (2) AdapterResult shape — pre-checkpoint prose said `txResult.data`; actual return type from `src/lib/sheets/transactions.ts` exposes `rows`. Used `txResult.rows`; tsc 0 errors confirms. (3) **Stream-idle timeout recovery [NEW MODE]** — original execute-plan agent built all 5 v2 leaves on disk (589 LOC) and was stream-idle-killed BEFORE the Task 1 commit; filesystem state at termination: 5 untracked files, tsc-clean, lint-clean. Recovery agent (Generation 2, fresh spawn) inspected filesystem via `git status --short`, audited each of 5 files individually (read + tsc smoke + lint smoke + grep for surgical section accent), confirmed match against plan must_haves, committed verbatim as Task 1 (`0619d65`), continued cleanly to Task 2 (`2a82a58`) → reached visual checkpoint → returned structured checkpoint message → user typed "approved." Generation 3 (this docs commit). Pattern catalogued as canonical recovery for mid-task stream-idle timeouts where work-in-progress is recoverable via filesystem inspection — distinct from checkpoint-resume (which carries explicit completed-task hashes) and from soft-reset+restore-staged (which recovers from commit-transaction races); third recovery mode in the v2.0 toolkit. Visual checkpoint user-approved without per-issue feedback (cockpit landed visually correct on first render). tsc + lint (0 errors, 3 pre-existing warnings unchanged) + `npm run build` all green (13 routes — `/recargas` continues to compile alongside the new `/uso-tarjeta`). No git race observed during this continuation phase (sibling 08-02 had committed cleanly before this plan resumed; disjoint paths). Commits `0619d65` (Task 1: 5 leaves +589 LOC) + `2a82a58` (Task 2: page rewrite + 4 v1 leaf deletions, +108/-465 net).

**Phase 9 Wave 3 actions (2026-05-08):**
- ✅ `/gsd:execute-plan 09-03` (Wave 3) — Vista Cliente v2 page composition + 22-symbol cohesive prune. **Page rewrite**: `src/app/(protected)/clientes/[empresaId]/page.tsx` rebuilt as Server Component dossier composition (229 → 289 LOC; +211/−397 LOC over v1) — replaces v1 (header + 12-month chart + 3 mini-cards) with v2 dossier (TikintagSelector + ClienteKPIHeader 6 KPIs incl. benchmark + RetirosBancoTable + BonosClienteCards + ComprasClienteCard + P2PCards + TimelineActivity in `<div data-presenter-hide>` + GenerarVistaClienteButton in `<div data-presenter-hide>`). Path-based dossier subject (URL `?empresa=` ignored on this route — path IS the picker); `tikintagFilters = { ...filters, empresa: empresaId }` threaded to v2 domain functions that honor `filters.empresa` (`filterBonosV2`, `filterPurchases`); date filters from URL pass through unchanged. **Single JOIN per request**: `joinPayouts(allTx, allPayouts)` called ONCE — result threaded into THREE consumers (`aggregateClienteBenchmark` + `aggregateClienteTimeline` + narrowed `clientPayouts = joined.filter(p => p.transaction?.empresa_id === empresaId)` for RetirosBancoTable). **404 fallback in-page Card** when `findClienteSummary === null` (NOT next.js `notFound()`); inline error Card on Sheets read failure. **Cliente-foco share URL retargeted** — GenerarVistaClienteButton.tsx rewritten: now generates `/clientes/{empresaId}?presenter=1` (was `/inicio?empresa=…&presenter=1` in v1). Implementation switched from `parseFilters → buildUrl` round-trip to raw `URLSearchParams.set` (forces `presenter=1` regardless of incoming value; preserves all other searchParams param-by-param). **3 v1 leaf component files DELETED** (orphaned after page rewrite): EmpresaProfileHeader.tsx + EmpresaMiniCards.tsx + EmpresaActivityChart.tsx (~530 LOC removed; sole consumer was the v1 page). **22 v1 domain symbols PRUNED in one cohesive `refactor(09-03):` commit** across 3 modules (closes 4 deferred-prune dockets from Plans 07-02 / 07-04 / 08-03 / 08-04): bonos.ts (8 symbols deleted: filterBonos, summarizeBonos, aggregateBonosByDate, aggregateBonosByEmpresa, top10Empresas, BonoSummary, BonoByDate, BonoByEmpresa; 534 → 297 LOC; v2 surface preserved byte-identical) + recargas.ts (10 symbols deleted: filterRecargas, summarizeRecargas, aggregateRecargasByDate, aggregateRecargasByEmpresa, top10RecargasEmpresas, findTopEmpresaRecargadora, findRecargaMasGrande, RecargaSummary, RecargaByDate, RecargaByEmpresa; 895 → 569 LOC; v2 surface preserved byte-identical) + clientes.ts (4 orphans deleted: findEmpresa, EmpresaProfileSummary, aggregateMonthlyActivity, MonthlyActivity, plus internal helper subMonthsLabel + unused formatInTimeZone import + BOGOTA_TZ constant; 516 → 287 LOC; empresas-INDEX surface preserved for /clientes list page — deriveEmpresasIndex, summarizeEmpresasIndex, EmpresaListRow, EmpresasIndexSummary, EmpresaStatus). **payouts.ts v1 surface (4 symbols: filterPayouts, summarizePayouts, PayoutSummary, COMPLETED_PAYOUT_STATES) intentionally NOT touched** — inicio/page.tsx + HechosCurados.tsx still consume; Phase 10 (Inicio v2 rewrite) closes that loop. **Audit gate before deletion**: `grep -rE "<symbol>" src/` per each of 22 symbols + 3 v1 leaves — confirmed only JSDoc/comment references survived (cross-module historical notes acceptable, they don't break compilation). **Key decisions:** (a) cohesive multi-module v1 prune in ONE commit (not one-per-module) avoids intermediate states where a module is half-pruned; (b) two-orchestrator presenter-hide model codified — page wraps whole-component (`<div data-presenter-hide>` around TimelineActivity + GenerarVistaClienteButton); leaf carries cell-level opt-outs (RetirosBancoTable failure-reason cells already had `data-presenter-metric-hide` from Plan 09-02); (c) 404 fallback uses in-page Card (NOT `notFound()`) — same convention as schema-error Card across all v2 pages, shows decoded tikintag in `<span className="font-mono">` so user sees what they typed; (d) `URLSearchParams` builder over `parseFilters → buildUrl` round-trip in GenerarVistaClienteButton — destination is a different path that doesn't carry the same filter contract semantics; direct param-by-param copy is cleaner. **Field-name reconciliation: NONE** — third clean-break plan in a row after 09-01 and 09-02 (no PRD-vocabulary translation needed; plan vocabulary matches types.ts + Plan 09-01/09-02 exports exactly). **No Rule deviations** — plan executed exactly as written. tsc + lint (0 errors, 3 baseline warnings unchanged) + `npm run build` (still 13 routes — `/clientes/[empresaId]` rebuilt in place; no new routes) all green. Two atomic commits: Task 1 `e8d3d09` (page rewrite + button retarget + 3 v1 leaves removed; +211/−397) + Task 2 `03efba3` (22-symbol prune; +109/−901). User typed "approved" at visual checkpoint after verifying: 6-KPI cabecera with benchmark Emerald accent + delta-sign semáforo subtext; TikintagSelector dropdown switches dossier preserving searchParams; all 5 sections render; presenter mode hides timeline + GenerarVistaClienteButton + RetirosBancoTable failure-reason column; cabecera + 4 other sections remain visible in presenter; cliente-foco share URL lands on `/clientes/{tikintag}?presenter=1`; 404 fallback works; dark mode preserves Emerald accent; no console errors. Duration 16 min. **Net Phase 9 closeout LOC: ~−1500 LOC removed** (page rewrite −186 + 3 v1 leaves ~−530 + 3 modules −792) despite delivering richer 6-KPI cabecera + benchmark + 5 sections + presenter-hide system. **All 8 CLI-V2-* requirements MET**. **Phase 9 SHIPPED ✅** — only payouts.ts v1 surface (4 symbols) carries forward to Phase 10.

**Phase 9 Wave 2 actions (2026-05-08):**
- ✅ `/gsd:execute-plan 09-02` (Wave 2) — Vista Cliente v2 leaves: 7 new component files in `src/components/clientes/` (1176 LOC; +603 Task 1, +573 Task 2). **TikintagSelector** (79 LOC, Client Component): native `<select>` of 235 tikintag options + path-based `router.push("/clientes/{encodeURIComponent(id)}{?qs}")` preserving `searchParams` (filters + presenter mode); `useRouter` + `useSearchParams` from `next/navigation` (NOT `usePathname` — path is hardcoded); `font-mono` styling on select + options. **ClienteKPIHeader** (235 LOC, Server Component): 6-KPI grid (Balance · Primera tx · Última actividad · Total tx · Pocket activo · Tiempo vs benchmark) inside a single Card; benchmark KPI carries `border-l-4 border-section-clientes pl-3` + `text-section-clientes` headline + delta-sign semáforo subtext (faster=green, slower=red, equal=muted) + sample-size footer (`N vs M payouts`); inline `Stat` helper with `variant="default"|"benchmark"` prop for grid uniformity; uses `formatMinutes`/`formatBogotaDate`/`formatCOP`/`formatInteger` from `@/lib/format` (skipped the plan's prescribed inline formatMinutes — canonical helper already exists from Phase 7-04). **RetirosBancoTable** (192 LOC, Server Component): raw `<table>` over `JoinedPayout[]` with 7 columns (Fecha, Holder, Banco, Monto, Tiempo, Estado, Razón de fallo); failure-reason `<th>` + `<td>` carry `data-presenter-metric-hide` (3 attribute occurrences) so the column collapses in presenter; status badges per `PayoutState`; inline `displayBanco()` Title-case helper. **BonosClienteCards** (97 LOC, Server Component): 2-card `md:grid-cols-2` grid (Recibidos / Enviados) over `BonoSummaryV2`; per-direction ticket promedio derived locally (since `BonoSummaryV2.ticketPromedio` is across both directions). **P2PCards** (207 LOC, Server Component): 2-card header (Recibidas/Enviadas) + raw `<table>` "Últimas P2P" with 5 columns (Fecha, Dirección, Contraparte, Monto, Estado); direction column color-asymmetric (in=text-status-success, out=text-foreground); `font-mono` on Contraparte; counter-vs-row semantic split honored (counters honor `filters.status`, rows include all statuses); inline `P2PRow` + `statusBadge` helpers. **ComprasClienteCard** (96 LOC, Server Component): single Card with Compras totales headline + 2-col inline grid (Volumen / Ticket promedio); recent-list deferred to 09-03 visual checkpoint per YAGNI. **TimelineActivity** (270 LOC, Server Component): vertical `<ul>` of `ClienteTimelineEvent[]` with 4 helper functions (`iconAndColor`, `typeLabel`, `statusBadge`, `looksLikeTikintag` + `counterpartyLabel`); Lucide icon map: ArrowDownLeft (BONUS_IN/P2P_IN, text-status-success), ArrowUpRight (BONUS_OUT/P2P_OUT, text-foreground), ShoppingCart (PURCHASE, text-section-tarjeta), Banknote (RECHARGE_PSE/TRANSFER, text-section-recargas), CreditCard (PAYOUT_BANK, text-section-payouts), Activity (OTRO, text-muted-foreground); Spanish type labels + non-completed status badges only (the absence IS the green light); PURCHASE counterparty hardcoded to "Compra tarjeta" (overrides domain's `empresa_nombre` fallback that today equals tikintag); NO `data-presenter-*` JSX attributes — Plan 09-03 wraps in `<div data-presenter-hide>` at the page composition layer. **Key decisions:** one-section-accent-per-page rule held strict (only ClienteKPIHeader carries `text-section-clientes`/`border-section-clientes` JSX); TimelineActivity icons use multiple section accents as TAXONOMIC markers (NOT competing emphasis); presenter-hide separation codified (leaf cell-level vs page whole-component); native `<select>` + path-based router.push pattern for dossier switching; per-direction ticket promedio derived locally in BonosClienteCards (avoid forcing domain to emit per-direction averages). **Field-name reconciliation: NONE** — plan vocabulary clean (matches BonoSummaryV2/PurchaseSummary/JoinedPayout exactly; no PRD-vocabulary translation needed; second clean-break plan in a row after 09-01). **Sub-threshold notes (NOT Rule deviations):** (1) `formatMinutes` already exists in `@/lib/format` from Phase 7-04 — reused canonical helper instead of duplicating per the plan's prescription; (2) TikintagSelector skipped `usePathname` per plan's `<action>` instruction (path hardcoded); (3) added `font-mono` to `<select>` className (not in EmpresaFilter pattern but appropriate for `$`-prefixed tikintags). tsc + lint (0 errors, 3 pre-existing warnings unchanged) + `npm run build` (still 13 routes — no new routes; page composition lands in 09-03) all green. Two atomic commits: `4f60f44` (Task 1: 4 leaves +603 LOC) + `03793f7` (Task 2: 3 leaves +573 LOC). Duration 7 min.

**Phase 9 Wave 1 actions (2026-05-08):**
- ✅ `/gsd:execute-plan 09-01` (Wave 1) — Cliente domain v2: `src/lib/domain/cliente.ts` from-scratch (NEW file, 678 LOC, no v1 predecessor — per-tikintag dossier surface is brand-new in v2 PRD): 4 functions (`findClienteSummary` CLI-V2-02 + `aggregateClienteBenchmark` CLI-V2-07 + `aggregateClienteP2P` CLI-V2-04 + `aggregateClienteTimeline` CLI-V2-05/V2-08) + 6 types (`ClienteSummary`, `ClienteBenchmark`, `ClienteP2P`, `ClienteP2PRow`, `ClienteTimelineEvent`, `ClienteTimelineEventType` 9-value union: BONUS_IN/OUT, P2P_IN/OUT, PURCHASE, RECHARGE_PSE, RECHARGE_TRANSFER, PAYOUT_BANK, OTRO) — 10 exports total per must_haves. Module distinct from `clientes.ts` (plural empresa-INDEX) — singular/plural naming convention codifies dossier-vs-list-surface separation. **Key decisions:** header KPIs computed over WHOLE history (balance/primeraTx/totalTx/ultimaActividad immune to date filters; only `pocketActivo` honors `filters.to` via 30-day Bogotá-noon window) so adjusting filters doesn't shift cabecera totals; benchmark `aggregateClienteBenchmark(joinedPayouts, tikintag)` accepts `JoinedPayout[]` for one-JOIN-per-request budget (second proven shape after Plan 07-04 third-party detection); defensive `state==='completed'` filter inside benchmark mean (re-asserts `aggregateAverageProcessingMinutes` payouts.ts:582 contract — Aging-fallback contamination guard); P2P **counter-vs-row semantic split** (counters status-filtered default `['completed']` for "actividad real" KPIs; rows array carries ALL statuses for operator-dossier diagnostic table); P2P table cap = 50 rows, timeline cap = 200 events (defensive against pathological tikintags); timeline IGNORES `filters.status`/`filters.tipo` intentionally (operator opens timeline to investigate WHY a payout failed; hiding failed payouts hides the diagnostic target); `findClienteSummary` returns `null` for unknown tikintag (caller renders 404 fallback); internal helpers `classifyTransactionEvent` + `counterpartyForTransaction` extracted to keep aggregation main loops flat. **Two [Rule 3 - Blocking] auto-fixes:** (1) lint-clean inter-task commit window — plan prescribed Task 1 to import `formatInTimeZone` + define day-boundary helpers, but neither Task-1 function consumed them (whole-history aggregations); deferred to Task 2 where they're consumed (avoids `@typescript-eslint/no-unused-vars` warnings during inter-task window). (2) skipped unused `BOGOTA_TZ` constant + `formatInTimeZone` import — the day-boundary helpers carry the `-05:00` offset inline (Bogotá UTC-5 no DST → deterministic), same convention as `cardUsage.ts`. **Field-name reconciliation: NONE** — plan vocabulary matches types.ts exactly (clean break from the 4× pattern observed in Plans 07-01/08-01/08-03/08-04 where PRD-vocabulary fields had to be translated to actual `Transaction`/`Payout` fields). tsc + lint (0 errors, 3 pre-existing warnings unchanged) + `npm run build` (13 routes) all green. Commits `1e1fee7` (Task 1: scaffold + summary + benchmark, +310 LOC) + `a7742eb` (Task 2: P2P + timeline, +375/-7 LOC). **Phase 9 deferred-prune backlog (22 v1 symbols across bonos.ts/payouts.ts/recargas.ts) NOT touched in this plan** — Plan 09-03 (page composition rewrite of `clientes/[empresaId]/page.tsx` + `EmpresaMiniCards.tsx`) is the prune-completion plan; Plan 09-01 is orthogonal greenfield.

**Next:**
1. **Phase 10 (Inicio v2 + payouts.ts v1 final close)** — last v2.0 phase. Inicio page rewrite is the natural closing milestone: replace the Latencia destacada hecho in `inicio/page.tsx` with v2 helpers (`summarizePayoutsByState` + `aggregateAverageProcessingMinutes`); restructure `HechosCurados.tsx` props (PayoutSummary → its v2 equivalent or restructured). After that swap lands, the 4 v1 payouts.ts symbols (`filterPayouts`, `summarizePayouts`, `PayoutSummary`, `COMPLETED_PAYOUT_STATES`) become orphans → final prune deletes them in the same Phase 10 commit, closing the v1 → v2 migration end-to-end.
2. **Phase 10 may also consume `aggregatePurchaseAdoption(allTx, purchaseRows)`** as a global "estado del negocio" KPI in the Inicio hero strip — adoption is the canonical conversation-driver per PRD; strong candidate for inclusion. Decision deferred to Phase 10 planning.
3. **Open question carry-forward — ComprasClienteCard recent-purchases mini-list** still deferred per YAGNI (Plan 09-02 + 09-03 visual checkpoints did not surface a need; the dossier already houses 5 other sections and vertical space is at a premium). Add a `purchases?: Transaction[]` prop and inline recent-list inside the existing card if a future iteration wants it; no new leaf needed.

Stopped at: Completed 10-01-PLAN.md (Phase 10 Wave 1 — Inicio v2 operative-lens domain surface; 6 v2 fns + 4 v2 types appended below v1 byte-identical block in `src/lib/domain/inicio.ts`). tsc + lint + build clean. Plan 10-02 (page rewrite + cohesive prune of v1 inicio + 4 v1 payouts.ts symbols + inicio-hechos.ts) is the natural next plan — closes v1 → v2 migration end-to-end.
Resume file: None
