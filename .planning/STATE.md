# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-27)

**Core value:** Una sola URL donde el equipo de Tikin ve métricas frescas del negocio sin abrir Sheets, presentable cuando se proyecta a clientes.
**Current focus:** Phase 5 — Clientes + Domain

## Current Position

Phase: 4 of 5 (Inicio + Recargas) — ✅ COMPLETE. Wave 1 (4 of 4 plans) + Wave 2 (4 of 4 plans). Phase 4 SHIPS.
Plan: 08 of 8 (Recargas page composition) ✅ COMPLETE — checkpoint approved. Both `/inicio` and `/recargas` LIVE in production at `https://project-dashboard-4b4fxxmdr.vercel.app` (deployment `dpl_8GprZ3cAoemQCczDTRRLeqy4WNqS`).
Status: **Phase 4 SHIPS.** Both hero tabs (`/inicio` Plan 04-07 + `/recargas` Plan 04-08) live in production with cliente-foco contract end-to-end. Plan 04-07 ships — `/inicio` is LIVE end-to-end on BD_Plataforma + BD_Payouts data: 5 KPIs with deltas (GMV / Comisión / Take rate / Empresas activas / Bonos vendidos), GMV trend chart + Empresas activas trend chart (bucket-aware at 60-day threshold; subtitle reflects daily/semanal), 3 hechos curados (top empresa GMV / latencia destacada with prior-period delta / empresas nuevas activadas with "+N más" overflow). page.tsx grew from ~37 LOC placeholder to 255 LOC full composition (commit 26c8432, +246/-18). Pipeline: parseFilters → computePriorPeriod → Promise.all parallel Sheets fetch (BD_Plataforma + BD_Payouts) → filterCompletedIn TWICE (current + prior windows over the SAME allTx — single Sheets fetch, second window derived in-memory) → summarizeInicio twice → bucket switch (length > 60 days ? week : day) → 4 chart aggregations → 3 hechos curados (findTopEmpresaByGMV + findEmpresasNuevasActivadas with allTx FULL dataset per Pitfall 5 + summarizePayouts on filtered payouts for latencia delta) → JSX render. Cliente-foco contract verified end-to-end across all 4 URL states (user response: "approved"): no-filters → 5 KPIs + 2 charts + 3 hechos curados; presenter=1 → 3 KPIs (Comisión + Take rate hidden); empresa=$X → 5 KPIs filtered + chart shows flat y=1 (visible because not cliente-foco); presenter=1&empresa=$X (CRITICAL) → 3 KPIs + GMV chart visible, EmpresasActivasChart Card HIDDEN, HechosCurados ALL HIDDEN. Page-level Card wraps EmpresasActivasChart so data-presenter-empresa-hide attaches to the Card not the inner chart (whole editorial block hides cleanly; chart degenerates to flat line at y=1 when empresa-filtered per RESEARCH Edge case 2). Bucket subtitle: small muted span inside CardTitle "(diario)"/"(semanal)" — same pattern reusable for Plan 04-08. Empty state implicit: summarizeInicio([]) zero-safe → zero-value cards, <2 points charts → friendly Spanish empty-state copy, HechosCurados handles per-card empty states. Inline Card error fallback on Sheets failure (mirror of bonos/payouts pages). NO verifySession (route group layout already guards). tsc --noEmit clean. npm run build ✓ Compiled successfully in 9.2s (Next 16.2.4 Turbopack), all routes emitted, /inicio now ƒ (Dynamic) with full composition. ZERO plan-spec deviations on Task 1 — literal code blocks from <action> compiled clean on first build, 8 verify checks all green, 4-URL cliente-foco contract held on first manual verification. Operational deviation: Task 3 (Vercel --prod deploy) intentionally deferred to batch with Plan 04-08 (ship /inicio + /recargas together as the Phase 4 user-facing pair). Phase 5 CLI-08 ("Generar vista para cliente") is now UNBLOCKED — its precondition (cliente-foco contract impecable on the hero tab) is verified end-to-end. PRIOR Plan 04-05 status preserved below: 5 visual leaves for /inicio in HEAD: DeltaBadge atom (78 LOC, Server, with `inverted` prop for latency direction), KPICardsInicio (130 LOC, Server, 5-card grid with delta sub-component, Comisión + Take rate carry data-presenter-hide), GMVTrendChart (109 LOC, Client, Recharts BarChart 320px height, currentColor strokes, bucket-aware tickFormatter), EmpresasActivasChart (113 LOC, Client, Recharts LineChart, `dataKey="count"`, `formatInteger` YAxis), HechosCurados (124 LOC, Server, 3-card editorial reel — top empresa GMV / latencia destacada with INVERTED DeltaBadge / empresas nuevas with overflow "+N más" — outer wrapper carries data-presenter-empresa-hide for cliente-foco hide). All 5 components compile clean (`tsc --noEmit` silent, `npm run build` ✓ 13.1s, 11 routes emitted). 0 plan-spec deviations. Operational note: gsd-executor agent hit 3 consecutive SDK stream timeouts during Wave 2 parallel attempt — Tasks 1+2 (commits b442ddc, d68f4aa) landed clean under the agent; Task 3 (HechosCurados, commit d5c6eed) + SUMMARY.md + this STATE update + metadata commit closed orchestrator-direct after the agent failed to recover. No file-state divergence; each commit is atomic. DeltaBadge `inverted` prop bundled into Task 1 commit (rather than retrofitted in Task 3) avoiding churn. Wave 2 plan 04-06 (Recargas visual leaves) NEXT — runs sequentially since the parallel-spawn approach has been failing repeatedly. PRIOR Wave 1 status preserved below. `src/lib/domain/inicio-hechos.ts` created (180 LOC, 5 exports: 3 interfaces + 2 functions). `findTopEmpresaByGMV(filteredTransactions): TopEmpresaResult | null` is single-pass O(N) Map reduction; null on empty input. `findEmpresasNuevasActivadas(allTransactions, filters): EmpresasNuevasResult` finds empresas with first-EVER tx in window — Pitfall 5 hardened. Top-5 cap with overflowCount; sorted asc by firstTx. Module verified PURE (only Transaction + DashboardFilters type-only imports). Stable contracts (TopEmpresaResult, EmpresaNueva, EmpresasNuevasResult) ready for Plan 04-05 + 04-07. ZERO plan-spec deviations. SEVENTH consecutive technical-zero-deviation plan (after 02-04, 03-02, 03-03, 03-04, 04-03, 04-01). Same parallel-wave git race already documented from BOTH sides (entries 134 + 150): commit `28d709c` carries inicio-hechos.ts (mine, sha256-verified) under feat(04-01); commit `76052a1` carries period.ts under feat(04-04). Cosmetic only. Task 2 commit `3a502a3` correctly attributed feat(04-02). Wave 2 (04-05/04-06/04-07/04-08) NOW UNBLOCKED — all four upstream domain libraries (period.ts, inicio.ts, inicio-hechos.ts, recargas.ts) ship with stable type contracts; cliente-foco CSS gate primitives ready. PRIOR Plan 04-01 status (preserved): `src/lib/domain/period.ts` (108 LOC, 2 functions: computePriorPeriod + pctChange) and `src/lib/domain/inicio.ts` (361 LOC, 6 functions + 4 types) created. `src/lib/domain/period.ts` (108 LOC, 2 functions: computePriorPeriod + pctChange) and `src/lib/domain/inicio.ts` (361 LOC, 6 functions + 4 types) created — both pure modules, no next/react/server-only/lib/sheets/lib/format imports. computePriorPeriod returns same-length immediately-prior window in Bogotá (April 2026 → March 2-31, NOT March 1-31; 30 days each); null when filters lack from/to or are malformed. pctChange returns `number | null` (zero-safe: prior=0/NaN/non-finite → null). Inicio default filter contract is broader than Bonos: `direction='in' + status='completed'` (NO tipo filter — Inicio aggregates ALL completed inflows; bonosVendidos KPI computed inside summarizeInicio over the full filtered set). summarizeInicio returns 5 KPIs (gmv, comision, takeRate, empresasActivas, bonosVendidos) in single-pass reduce with empty-input safety + zero-divisor safety on takeRate. aggregateGMVByDate/Week and aggregateActiveEmpresasByDate/Week cover both granularities; active-empresa variants use Map<bucket, Set<empresa_id>> to close Pitfall 11 (1 empresa with N tx in one bucket counts as 1, not N). Week bucket uses `RRRR-'W'II` (ISO week-numbering year, e.g. 2026-W18) not `yyyy-'W'II` to correctly bucket year-boundary days. Inline date helpers verbatim from bonos.ts:53-74 (fourth domain module to make this DRY-vs-cohesion call). All 7 must_have truths fixture-verified live via tsx + npm run build (Next 16.2.4 Turbopack ✓ Compiled successfully in 8.7s, TS 7.2s, all 11 routes). Per-task atomic-commit pattern: 28d709c (period.ts task — see deviation note below), 4d7c9ee (inicio.ts task — clean). One operational deviation: parallel-wave git race produced a misfiled commit message (28d709c says "feat(04-01): create period.ts" but actually contains only inicio-hechos.ts; the actual period.ts content was co-shipped under sibling commit 76052a1's `feat(04-04)` umbrella). Code state correct (period.ts in HEAD is byte-identical to spec); only git-message-to-content labeling is off — same parallel-wave race the sibling documented in 04-04-SUMMARY.md (mitigation: future parallel waves use `git stash --include-untracked` or `git commit -- <pathspec>`). All 7 fixture verifications green. Stable contracts now available for Plan 04-02 (filterCompletedIn for hecho-curado scoping if not already adopted), Plan 04-05 (KPICardsInicio consumes InicioDeltaSummary), Plan 04-07 (page composition wires filterCompletedIn + 4 aggregations + computePriorPeriod). Wave 1 now 3-of-4 complete (04-01 + 04-03 + 04-04 done; 04-02 still in flight per sibling).
Last activity: 2026-05-06 — Completed Plan 04-08 (Recargas page composition), checkpoint approved by user. /recargas LIVE end-to-end with 2 KPIs + deltas, trend bar chart, top-10 empresas table, 2 hechos curados; cliente-foco contract verified across all 4 URL states. Single code commit 559bc9b (175 LOC page.tsx) shipped on 2026-05-05; ~24h calendar pause for human-verify; orchestrator-direct close on 2026-05-06 with Vercel `--prod` deploy + this metadata commit. **Phase 4 SHIPS.** Both `/inicio` (Plan 04-07) + `/recargas` (Plan 04-08) live at `https://project-dashboard-4b4fxxmdr.vercel.app` (deployment `dpl_8GprZ3cAoemQCczDTRRLeqy4WNqS`, alias `https://project-kr6et.vercel.app`, region iad1). Production smoke green: `/login` HTTP 200, `/recargas` HTTP 307 → /login (proxy gate intact). Deploy supersedes Phase 3 production at `https://project-dashboard-allec5r4i.vercel.app` (which kept /inicio + /recargas as Phase 1 placeholders). 0 plan-spec deviations on Task 1 — literal code blocks from `<action>` compiled clean on first build, 6 verify checks all green, 4-URL cliente-foco contract held on first manual verification. NINTH consecutive technical-zero-deviation plan (after 02-04, 03-02, 03-03, 03-04, 04-03, 04-01, 04-02, 04-07). Phase 5 (Clientes + Domain) is the next milestone — CLI-07 mini-resumen 3 cards now points to a populated /recargas (no longer a Phase 1 placeholder), CLI-08 cliente-foco share-URL lands cleanly on both /inicio and /recargas.

Progress (all plans, total 5+4+...): ████████░░ 20/? (Phase 1 done 4/4; Phase 2 done 4/4; Phase 3 done 4/4 — SHIPS; Phase 4 done 8/8 — SHIPS; Phase 5 next)

## Performance Metrics

**Velocity:**
- Total plans completed: 20
- Average duration: ~9.4m
- Total execution time: ~219m (over 6 calendar days due to user_setup + checkpoint gates + parallel wave 1 in Phase 4 + ~24h human-verify pause on Plan 04-08)

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 01-foundation | 4/4 | ~57m | ~14m |
| 02-bonos | 4/4 | ~28m | ~7m |
| 03-payouts | 4/4 | ~38m | ~10m |
| 04-inicio-recargas | 8/8 ✅ SHIPS | ~96m | ~12m |

**Recent Trend:**
- 01-01 (11m 24s), 01-02 (7m 50s), 01-03 (13m 14s), 01-04 (~25m active + several gate-waits), 02-01 (11m 22s), 02-02 (8m 35s), 02-03 (3m 14s), 02-04 (5m 12s), 03-01 (8m 19s), 03-02 (10m 16s), 03-03 (6m 8s), 03-04 (~14m active + ~4h human-verify pause), 04-04 (~3m active in parallel wave), 04-03 (3m 54s in parallel wave), 04-01 (6m 9s in parallel wave)
- Trend: Phase 4 Wave 1 parallel execution validated three concurrent plans (04-01, 04-03, 04-04) with aggregate wall-clock savings vs sequential. Plan 04-01 closed at 6m 9s — slightly longer than 04-03 (3m 54s) because it shipped TWO modules (period.ts + inicio.ts) vs one, and because it had to navigate a git index race when sibling commits landed mid-execution. ZERO TECHNICAL deviations (the misfiled commit message is operational, not technical — code in HEAD is correct). SIXTH consecutive zero-technical-deviation plan after 02-04, 03-02, 03-03, 03-04, 04-03. Domain library plans hit the FAST end of the velocity spectrum (~3-6m) when they're mirrors of a proven reference module (here: bonos.ts → period.ts/inicio.ts) and the must_haves carry literal-code-block fidelity. Parallel-wave hazard now documented in TWO independent SUMMARYs (04-04, 04-01): the git index is a shared resource across concurrent agents, and the "stage files individually" rule does NOT prevent foreign untracked files from entering a co-existing commit if they get tracked between `git add` and `git commit`. Forward mitigations: (a) `git stash --include-untracked` foreign untracked before staging; (b) `git commit -m "..." -- <explicit-pathspec>` to limit commits to specific paths; (c) orchestrator-level serialization for the commit step within parallel waves.

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- Roadmap (2026-04-27): 5-phase compression — Foundation, Bonos, Payouts, Inicio+Recargas, Clientes+Domain. Inicio antes de Clientes para que CLI-08 ("Generar vista para cliente") aterrice en Inicio populado.
- Requirements (2026-04-27): Success rate (Recargas + Payouts) y failure breakdowns diferidos a v2 por data assumptions; `destination_type` confirmado, habilita PAY-04.
- Plan 01-01 (2026-04-27): Session JWT 30-day expiry, bcrypt cost 10, cookie SameSite=lax, rate-limit 5/5min/IP via Upstash sliding window, two-layer auth (proxy gate + DAL re-verify), timing-safe bcrypt always runs.
- Plan 01-01 (2026-04-27): `proxy.ts` lives at `src/proxy.ts` (NOT project root) because Next 16 looks for the proxy file at the same level as `app/`, which is `src/` when `--src-dir` is enabled. Documented as Rule-1 deviation in 01-01-SUMMARY.md.
- Plan 01-02 (2026-04-27): Sheets adapter is the SINGLE place googleapis is imported. `'server-only'` enforced empirically (zero hits in `.next/static/*.js`). Future adapters (payouts.ts, etc.) follow the same shape: AdapterResult<T> + Expected{X}Headers + per-row Zod safeParse with skip-and-count semantics.
- Plan 01-02 (2026-04-27): Sheets client uses LAZY JWT singleton — module imports never throw, missing-creds error raised inside `getSheetsClient()` on first call. Lets parallel plans import the adapter without GCP creds set.
- Plan 01-02 (2026-04-27): Header names ('fecha','monto','tipo','empresa_id','empresa_nombre','destination_type') and tab names ('Transacciones','Payouts') are TENTATIVE. Plan 04 production smoke confirms vs live Sheet; mismatch surfaces as a clear schema error naming missing columns. Adjustment touches only `src/lib/domain/schemas.ts` and (if tab name differs) `src/lib/sheets/config.ts`.
- Plan 01-02 (2026-04-27): Smoke endpoint lives at `/api/smoke`, NOT `/api/_smoke` — Next App Router treats `_`-prefixed folders as PRIVATE (non-routable). Documented as Rule-1 deviation in 01-02-SUMMARY.md.
- Plan 01-02 (2026-04-27): `withRetry` retries ONLY on 429 (default 3 retries, base 250ms, cap 4s, jitter). Non-429 errors re-thrown immediately so schema mismatches and auth failures aren't masked by retry backoff.
- Plan 01-03 (2026-04-27): URL-as-state for ALL dashboard filters (from, to, empresa, presenter). `parseFilters(searchParams)` on read, `router.push(buildUrl(pathname, filters))` on write. Sticky across navigation, shareable, server-renderable. Future filter dimensions extend `DashboardFilters` in `src/lib/url-state.ts`; TabNav and PresenterToggle inherit them automatically via spread.
- Plan 01-03 (2026-04-27): `src/lib/format.ts` is the single Intl gate of the project. Verified by grep on every commit — no other file in `src/**/*.{ts,tsx}` calls `Intl.NumberFormat` or `toLocaleString`. Pitfall 9 (currency drift) and Pitfall 10 (timezone drift) closed at the codebase level.
- Plan 01-03 (2026-04-27): Layouts cannot read `searchParams` in App Router (don't re-render on soft nav). Documented as Rule-1 deviation in 01-03-SUMMARY.md. PresenterFrame and LastRefresh are `'use client'` wrappers reading `useSearchParams`. Future plans: any UI that must react to URL changes lives in Client Components, NOT in Server layouts.
- Plan 01-03 (2026-04-27): `data-presenter='on'|'off'` + `data-presenter-hide` is the visual contract for presenter mode. CSS does the hiding/typography bump declaratively. JS only flips the URL.
- Plan 01-03 (2026-04-27): EmpresaFilter accepts `empresas: EmpresaOption[]` and renders gracefully on `[]`. Phase 2+ feeds it real data without component changes.
- Plan 01-04 (2026-04-29): Vercel project `project-dashboard` linked under scope `alejandro-almeidas-projects-5f343d98`. Region pinned to `iad1`. Production URL: `https://project-dashboard-bkwmin189.vercel.app`.
- Plan 01-04 (2026-04-29): Upstash Redis provisioned via Vercel Marketplace integration (NOT direct Upstash account). Marketplace injects `KV_REST_API_*` env vars; we manually aliased `UPSTASH_REDIS_REST_URL`/`_TOKEN` so existing code works unchanged.
- Plan 01-04 (2026-04-29): Sheets config tab names corrected from tentative `Transacciones`/`Payouts` to real `BD_Plataforma`/`BD_Payouts`. Both data sources live in ONE Sheet ID (`1X0o...QObA`), separated by tabs.
- Plan 01-04 (2026-04-29): Real BD_Plataforma headers (23 cols) and BD_Payouts headers (15 cols) captured live. Documented verbatim in 01-04-SUMMARY.md. Schema in `src/lib/domain/schemas.ts` is now KNOWN-MISMATCHED — Phase 2's first task is rewriting it.
- Plan 01-04 (2026-04-29): Vercel CLI's `vercel integration add` overwrote `.env.local` mid-flow, deleting 8 secrets. Recovery via Anthropic chat history (orchestrator only — agents don't have this fallback). Forward guidance: any future plan that runs Marketplace integrations should pull or back up `.env.local` first.
- Plan 02-01 (2026-04-29): Domain `Transaction` interface rewritten to match real BD_Plataforma. Fields: id (from transaction_id), fecha (from created_at), monto, grossAmount, comision (= total_transaction_fee), fixedFee, variableFeePct (0..1 fraction), tipo, direction, status, empresa_id, empresa_nombre, tikintag, accountId, reference?, destination_type?. `transactions.ts` adapter unchanged — Phase 1's pipeline is genuinely Sheet-shape-independent.
- Plan 02-01 (2026-04-29): TransactionType enum derived from live data (11 values + OTRO fallback): BONUS, CREDIT_ADJUSTMENT, FEE, P2P, PAYIN_PSE, PAYIN_TRANSFER, PAYOUT_BANK, PURCHASE, REFUND, TREASURY, UKNOWN. "UKNOWN" preserved verbatim (sic — typo in production data). TransactionDirection: in/out. TransactionStatus: completed/rejected.
- Plan 02-01 (2026-04-29): empresa_id default = tikintag (e.g. `$mario`, `$tikincol`, `$liftit-app`-shaped handles). Override to account_id is a 2-line edit confined to `src/lib/domain/schemas.ts` transform. tikintag chosen over account_id because it's human-readable and aligns with Tikin's existing addressing scheme; account_id (UUID) would force a separate display lookup that doesn't exist in BD_Plataforma yet. Phase 5 (Clientes) is the natural place to add a tikintag → empresa display-name mapping if needed.
- Plan 02-01 (2026-04-29): variable_fee_percentage stored in Sheet as whole percent (0..100), confirmed live via /api/diagnose: range 0..4.76, samples [0, 3.5, 3.99, 4.56, 4.76]. Schema accepts up to 100 and divides by 100 in transform. The `Transaction.variableFeePct` contract is fraction-only (0..1) so consumers do `monto * variableFeePct` directly. Caught as Rule-1 bug after first smoke run showed 45% skip rate; fix dropped skip rate to 1.36%.
- Plan 02-01 (2026-04-29): Diagnostic-then-cleanup pattern. Temporary `_diagnose.ts` + `/api/diagnose` route created mid-plan to capture distinct enum values from production before writing schema; deleted before final commit. Production never carried inspection-only code. Single commit shows only the rewrite + cleanup state.
- Plan 02-01 (2026-04-29): /api/smoke verified green against production (count=3188, skipped=44, 1.36%). 3188 + 44 = 3232 matches the row count from 01-04-SUMMARY.md exactly. Remaining 44 skips are rows with empty transaction_id (genuinely malformed).
- Plan 02-02 (2026-04-29): `bonos.ts` is a PURE module — no `next/`, no `server-only`, no `react`. Imports limited to `./types` and `@/lib/url-state` (type-only). Functions: `filterBonos` + `summarizeBonos` + `aggregateBonosByDate` + `aggregateBonosByEmpresa` + `top10Empresas`. Output types `BonoSummary` / `BonoByDate` / `BonoByEmpresa` are stable contracts for Plans 03 and 04 UI consumers.
- Plan 02-02 (2026-04-29): Default Bonos filter contract = `tipo='BONUS' + direction='in' + status='completed'` + Bogota-anchored from/to + optional empresa. Documented inline in jsdoc and in 02-02-SUMMARY.md "Bonos Filter Contract" so override is explicit. `pending` (if Tikin adds it) falls through to `OTRO_STATUS` and is auto-excluded; types.ts grows when Tikin officially supports the value.
- Plan 02-02 (2026-04-29): `aggregateBonosByDate` does NOT zero-fill missing days. Bono density is high (~3000 BD_Plataforma rows over ~90 days, BONUS being the largest single tipo) so the chart lib in Plan 03 handles continuous-axis spacing. Zero-fill would also make the dashboard look like the source of truth on "no-sale days" — which it isn't.
- Plan 02-02 (2026-04-29): `pctDelTotal` is computed against the input passed to `aggregateBonosByEmpresa` (already filtered), not the universe of transactions. Matches "% of bonos in the current view" reading.
- Plan 02-02 (2026-04-29): Date filter uses literal `${date}T00:00:00-05:00` / `T23:59:59.999-05:00` offsets instead of `new Date(date)`. Closes the silent off-by-one where naked `new Date('2026-04-01')` parses to UTC midnight = 19:00 prev day in Bogotá. Same convention as `url-state.ts`.
- Plan 02-02 (2026-04-29): `getCachedTransactions = cache(getTransactions)` from React. Same-request memoization (DashboardHeader + page share one Sheets fetch); zero cross-request caching so "lectura en vivo" per PROJECT.md is preserved. Use `getCachedTransactions` from any Server Component in the `(protected)/layout.tsx` render tree; use `getTransactions` from route handlers, server actions, scripts.
- Plan 02-02 (2026-04-29): `DashboardHeader` is now async. Reads transactions, computes empresa registry via `getEmpresaRegistry`, passes real `EmpresaOption[]` to `<EmpresaFilter>`. Try/catch around the read → on Sheet failure the dropdown renders empty (Phase 1 behavior) and the error surfaces on the data-bearing page; chrome never breaks.
- Plan 02-02 (2026-04-29): Empresa registry sort uses `Intl.Collator('es', { sensitivity: 'base', numeric: true })`. The `numeric: true` is key for tikintags shaped like `$1anderson`/`$11john` — orders numeric segments as numbers, not lexically. Plan said `localeCompare(b.nombre, 'es')`; Rule-1 refinement because plain localeCompare would order `$1` < `$11` < `$2`. Documented as deviation in 02-02-SUMMARY.md.
- Plan 02-02 (2026-04-29): Production verification — /inicio HTTP 200, dropdown shows 234 options (1 default + 233 real empresas, alphabetical es-collated), `?empresa=$mario` correctly renders selected. /bonos same 234 options (header is shared). /api/smoke still ok=true count=3188 skipped=44.
- Plan 02-03 (2026-04-29): Chart library = recharts ^2.15.4. Selected over nivo (loads d3 entirely → bundle inflation), visx (primitives kit, not chart lib → time cost), raw SVG (no axes/tooltip/responsive for free). React 19 compatible cleanly. Override path documented: BonosChart.tsx is a self-contained leaf — swap recharts→nivo or recharts→visx by editing only that file. No override happened during execution; recharts compiled clean on first try.
- Plan 02-03 (2026-04-29): BonosChart is the ONLY 'use client' component of the Bonos UI. Recharts ResponsiveContainer needs DOM (window resize listener). Leaderboard, KPICards, SalesTable are pure Server Components — zero hydration cost, static HTML on those leaves. Pattern reusable for Phase 3+ (Recargas trend, Payouts trend, Inicio mini-charts).
- Plan 02-03 (2026-04-29): Chart line uses `stroke="currentColor"` not a hardcoded hex. The wrapping page sets `text-{token}` and the line follows. Theme switches don't touch the chart; light/dark works without conditional logic. Same pattern reusable for future trend charts.
- Plan 02-03 (2026-04-29): BonosChart does NOT carry `data-presenter-hide`. The chart is the heroína — visible in BOTH internal AND presenter views. Modo Presentación + filtro empresa transforms the tab into a "vista 1-cliente" by reshaping the data feed, NOT by hiding the chart. The "cliente sees only their data" contract comes from the URL filter (Plan 04 wires it), not from visibility toggles here.
- Plan 02-03 (2026-04-29): KPI Ticket promedio is ALWAYS visible (both internal + presenter). Only Comisión total carries `data-presenter-hide`. Matches roadmap Success Criteria 4 and 02-CONTEXT.md vision (cliente needs to see "what's our average sale" of their own data; revenue/comisión stays internal-only).
- Plan 02-03 (2026-04-29): SalesTable hides th + every td of the last 2 cols ($ comisión, % del total). CSS `display: none` on `display:table-cell` works per-cell — browser table layout absorbs freed space across remaining cols. No explicit width recalculation needed in the component. Plan 04 will visually confirm the table doesn't shift jaggedly during the mode transition; if it does, the fix is `<col>` widths inside SalesTable (5-line addition).
- Plan 02-03 (2026-04-29): Two Rule-1 micro-fixes during Task 3 — (a) removed redundant `$` prefix in `Sobre {formatCOP(...)} vendidos` (formatCOP already prepends '$ ', double-dollar would have rendered); (b) replaced bare `${summary.count}` with `formatInteger(summary.count)` (3000+ counts need thousands grouping per Colombian convention). Both documented in 02-03-SUMMARY.md "Deviations" so the patterns don't recur in future plans.
- Plan 02-04 (2026-04-29): `searchParams: Promise<Record<string, string | string[] | undefined>>` is the canonical Server Component page signature (Next 16). `await searchParams` once at top, then `parseFilters(params)`. Confirmed against `next 16.2.4` in node_modules. All future page.tsx files in this project follow this shape.
- Plan 02-04 (2026-04-29): `export const dynamic = "force-dynamic"` on data-bearing pages. URL state would explode the cache key across filter combinations and PROJECT.md mandates per-request fresh Sheets reads. ISR / RSC caching is opt-out at the page level. Build output confirms: `ƒ /bonos` (Dynamic). Future Payouts/Recargas/Inicio pages follow the same directive.
- Plan 02-04 (2026-04-29): Inline error fallback over `error.tsx` boundary for the Sheets fetch failure case. Try/catch around `getCachedTransactions()` returns a `<Card>` with the underlying `err.message` (e.g. "Sheet schema mismatch — columnas faltantes en transactions Sheet: created_at"). The route-group's `error.tsx` stays free for genuine runtime errors.
- Plan 02-04 (2026-04-29): Empty-state still renders KPICards (zero values). `summarizeBonos([])` returns `{count:0, ticketPromedio:0, comisionTotal:0, montoTotal:0}` (zero-safe by Plan 02-02 contract). Showing `$ 0` cards + a friendly "Sin bonos en el período seleccionado" Card preserves layout consistency and tells the user "your filter is the cause" rather than "something is broken".
- Plan 02-04 (2026-04-29): Aggregations run AFTER the empty-state guard, not before. `aggregateBonosByDate`, `aggregateBonosByEmpresa`, `top10Empresas` are skipped when the filter excluded everything — saves CPU on the empty-page case. Behavior identical for non-empty cases.
- Plan 02-04 (2026-04-29): Page does NOT re-fetch the empresa registry. DashboardHeader from Plan 02-02 already calls `getCachedTransactions()` and computes `getEmpresaRegistry`. The page's own `getCachedTransactions()` returns the SAME memoized result via React `cache()` per-request dedup. Verified empirically: page-size progression in production (51k placeholder < 57k empty < 65k 1-empresa < 155k 7d < 264k full) is monotonic with content actually rendered — no double-fetch artifacts.
- Plan 02-04 (2026-04-29): No `verifySession()` call in the page. The `(protected)/layout.tsx` from Plan 01-04 already guards the entire subtree (and the proxy gate from 01-01 catches it earlier). Re-calling here would be redundant. Future protected pages follow this pattern — auth lives in the layout, never per-page.
- Plan 02-04 (2026-04-29): Layout grid `lg:grid-cols-[1fr_2fr]` for Leaderboard | SalesTable. The table has 5 columns and visually weighs more; 1:2 ratio prevents the table from looking cramped while keeping the leaderboard readable. Mobile (default) stacks them vertically with `space-y-6`. Pattern reusable for Phases 3-5 wherever a list and a table coexist.
- Plan 02-04 (2026-04-29): No deviations during execution. Plan's literal code block compiled clean on first build, production deployed clean on first try, all 16 verification checks green. Zero Rule-1/2/3 fixes needed. Phase 2 ships unblocked.
- Plan 03-01 (2026-05-04): BD_Payouts wire format is FAR more exotic than research speculated. Live capture (798 rows) revealed: `value`/`transaction cost` ship as `"COP 200,000.00"` strings (not numbers); `aging`/`total time` ship as PostgreSQL interval strings `"0 years 0 mons N days HH hours MM mins SS.fff secs"` (not numbers, not seconds, not "HH:MM:SS"); `date`/`state timestamp` ship as English-locale strings `"April 27, 2026, 9:48 AM"`. Two new Zod helpers (`MoneyFromCOP`, `parsePgInterval`) absorb the parsing in schemas.ts; downstream `Payout` interface stays clean (numbers + Dates).
- Plan 03-01 (2026-05-04): `Holder` is a CARDHOLDER FULL NAME, NOT a tikintag. The empresa filter (CROSS-02) cannot match `holder === '$mario'`. Plan 03-02 (page composition) MUST build a `Map<transactionId, empresaId>` from BD_Plataforma rows and join via `Payout.transactionId` to populate `Payout.empresa_id` when the URL filter is active. Default Payout.empresa_id is undefined; the join is the override path. No-empresa-filter case can skip the join entirely.
- Plan 03-01 (2026-05-04): `Destination Medium` is a BANK CODE (12 distinct values: bancolombia, nequi, daviplata, nubank, banco_de_bogota, banco_davivienda, banco_caja_social_bcsc, banco_av_villas, banco_falabella, banco_bbva_colombia, banco_mundo_mujer, davibank), NOT 'tarjeta'/'cuenta_bancaria' as 03-RESEARCH.md speculated. All 798 production rows are bank payouts — there are NO card payouts in BD_Payouts. PAY-04's "split tarjeta vs banco" needs reinterpretation by Plan 03-02/03: either (a) split by bank with top-N + "otros" bucket, or (b) drop the split entirely as a degenerate one-category KPI. PayoutMedium typed as open `string` (with OTRO_MEDIUM constant) so Tikin onboarding new banks doesn't churn the schema.
- Plan 03-01 (2026-05-04): `latencySeconds` = Total Time when present (state_timestamp − date, populated for completed payouts), Aging fallback otherwise. Total Time is the canonical end-to-end "time-to-payout"; Aging is the row's age (now − date) and is the wrong metric for percentile/histogram. Plan 03-02 percentile/histogram MUST filter to `state==completed` BEFORE reading latencySeconds — per 03-CONTEXT.md essentials ("solo payouts que efectivamente se completaron"). The fallback is purely defensive; success-rate KPI (PAY-V2-01) is the only consumer that touches non-completed rows.
- Plan 03-01 (2026-05-04): Adapter uses `.get()` (one range) NOT `batchGet` (two ranges). 03-RESEARCH.md preferred batchGet for one-quota-unit savings. Divergence rationale: transactions.ts already ships its own `.get()`; React `cache()` already dedupes per-render; two cached fetches = 2 quota units = same as 1 batchGet. Switching transactions.ts to share a batchGet wrapper is a non-trivial refactor of working code. Future optimization path documented inline in `payouts.ts` JSDoc: introduce `getCachedSheetsBundle()` that batchGets both ranges if 429s start appearing.
- Plan 03-01 (2026-05-04): `Payout.transactionId` (from BD_Payouts.Transaction ID) is the join key to BD_Plataforma; `Payout.internalId` (from BD_Payouts.ID, a 64-char hash) is the row's primary key for dedup. They are DISTINCT — one transaction may produce multiple payout rows (refund+retry per 03-RESEARCH.md Pitfall 9). The histogram in Plan 03-02 counts rows by `internalId`; the join uses `transactionId`.
- Plan 03-01 (2026-05-04): Diagnostic-then-cleanup pattern confirmed working a SECOND time (after Plan 02-01). 30-second live capture surfaced 4 schema-shape surprises that would have caused 100% skip rate or wrong domain modeling if assumed from research. Pattern is now established as the FIRST step of any plan that introduces a new Sheet tab. Future Phase 4 (Recargas) plans should mirror this exactly when reading any new tab.
- Plan 03-01 (2026-05-04): Single-commit-per-plan pattern preserved (mirror of 02-01). Diagnostic route created in Task 1, used to capture findings, deleted in Task 2 before commit. Git never tracked the diagnostic route. Single feat commit `94ccc9a` shows only the rewrite + cleanup state. Production HEAD never carried inspection-only code. This deviates from the orchestrator's generic "per-task atomic commit" rule — documented explicitly in 03-01-SUMMARY.md "Single-Commit Pattern".
- Plan 03-02 (2026-05-04): `payouts.ts` is a PURE module (mirror of `bonos.ts`) — no `next/`, `react`, `server-only`, `lib/sheets/` imports. Type-only imports for `Payout, PayoutState` from `./types` and `DashboardFilters` from `@/lib/url-state`. 609 lines, 13 exports (4 functions for Plan 03-04 page composition + 1 percentile primitive + 1 sibling helper + 5 output type interfaces + 1 union type + 1 const). Inline date helpers (`startOfDayBogotaTimestamp`/`endOfDayBogotaTimestamp`) are verbatim copies from `bonos.ts` — DRY-ing across modules costs more than the inline ~22 lines.
- Plan 03-02 (2026-05-04): Default Payouts filter contract = `state=='completed' + Bogotá-anchored from/to + p.empresa_id===filter.empresa`. Empresa filter MATCHES `empresa_id`, NOT `holder` (Plan 03-01 confirmed Holder is cardholder name). When `filter.empresa` is set without the Plan 03-04 join, `filterPayouts` returns `[]` — better empty than wrong-empresa data. Plan 03-04 page composition MUST run a `transactionId → empresa_id` join from BD_Plataforma BEFORE calling `filterPayouts` if it intends to honor an empresa filter.
- Plan 03-02 (2026-05-04): Two filter functions, not one. `filterPayouts` (state-filtered) for headline metrics (summarize, histogram, topBancos); `filterPayoutsByPeriodOnly` (state-UNFILTERED) for `aggregateSuccessRate` denominator. Sibling helpers + pairing JSDoc make the contract explicit: if you used filterPayouts for success rate, the rate would always be 100%. Pattern reusable for Phase 4 Recargas success rate (REC-V2-01).
- Plan 03-02 (2026-05-04): Percentile algorithm = R-7 linear interpolation (NumPy/R/Excel `PERCENTILE.INC` default). Cited inline with formula + 5 fixture-verified examples. Median of [1..5]=3 (exact, odd-length); median of [1..4]=2.5 (interpolated, even-length); p95 of [1..10]=9.55 (h=8.55, interpolated 9 + 0.55*1). Zero-safe edges: empty=0, singleton returns the value, p<=0 → first, p>=1 → last. INPUT MUST BE SORTED ASCENDING — callers sort once and reuse for both p50 and p95.
- Plan 03-02 (2026-05-04): Histogram bucket boundaries are HALF-OPEN `[lower, upper)`. A boundary value (3600s, 21600s, 86400s) lands in the UPPER bucket, fixture-verified at exact edges. The half-open convention matches R/NumPy/Excel binning. `aggregateLatencyHistogram` always emits 4 rows in fixed order (`<1h`, `1-6h`, `6-24h`, `>24h`) even on empty input — Recharts BarChart in Plan 03-03 needs deterministic categories across renders to avoid shape jumps on filter swap.
- Plan 03-02 (2026-05-04): Histogram has NO medium stack. All 798 production payouts are bank (Plan 03-01); a stack would always show one color and one zero, visually misleading. Single-series shape `{bucket, count}` replaces the originally planned stack-by-medium. `LatencyBucket` is `{ bucket: LatencyBucketLabel, count: number }`.
- Plan 03-02 (2026-05-04): `aggregateTopBancos` REPLACES the original `aggregateByDestination`. Originally PAY-04 split tarjeta vs banco; reality is zero card payouts. Top N banks (default 5) by `montoTotal` + literal Spanish "Otros bancos" rollup. Each `BancoStats` carries `{medium, count, montoTotal, p50Seconds, p95Seconds}`. The Otros tail RE-AGGREGATES underlying Payout rows rather than summing per-bank stats — percentiles do NOT compose by addition (p95 of A∪B is generally NOT a function of p95(A) and p95(B)). Fixture-verified: 3-bank Otros tail with latencies [50,75,100] yields p50=75 exact, p95=97.5 interpolated.
- Plan 03-02 (2026-05-04): Output types (`PayoutSummary`, `LatencyBucket`, `BancoStats`, `TopBancos`, `SuccessRate`) are stable contracts for Plans 03-03 (visual components) and 03-04 (page composition). Co-located with the function that produces them, mirror of `bonos.ts` `BonoSummary`/`BonoByDate`/`BonoByEmpresa` pattern.
- Plan 03-02 (2026-05-04): Per-task atomic-commit pattern preserved (NOT the single-commit pattern from 03-01). Two feat commits: `b3eaea2` (filter+summarize+quantileSorted), `7741f0f` (histogram+topBancos+successRate). Plan 03-01's single-commit was justified by diagnostic-then-cleanup; no transient code here, no justification to merge commits. Future plans default to per-task atomic commits unless a diagnostic-then-cleanup pattern applies.
- Plan 03-02 (2026-05-04): Plan executed exactly as written — ZERO deviations. The plan's literal `<action>` code blocks compiled clean on first build; every `<verify>` fixture matched expected values to the digit. Heavy upfront pre-work in plan-author phase (literal code, fixture expected outputs cited, scope adjustment pre-resolved) → deterministic execute pass. Pattern reusable: domain library plans can be near-deterministic when the data shape is already stable.
- Plan 03-03 (2026-05-04): `formatDuration(seconds) → 'H:MM:SS'` is pure JS (no Intl, no `Intl.DurationFormat`/`Intl.RelativeTimeFormat`) and lives in `src/lib/format.ts` alongside formatCOP/formatInteger/formatPercent. Locale-invariant by design — colon convention is universal. No day rollover (`90061s → '25:01:01'`, NOT `'1d 1:01:01'`) for ops-friendly continuous-hours reading. Edge cases: NaN/null/undefined/Infinity/<0 → '—' (em dash, our standard "no data" marker). Single Intl gate intact — zero new `Intl.NumberFormat` callsites. Future Phase 4 Recargas time-to-credit KPIs (if added) reuse this helper verbatim.
- Plan 03-03 (2026-05-04): Hero technical KPIs (P50/P95) use `font-mono text-3xl tabular-nums` for digit alignment across cards. The font-mono ensures `0:12:04` and `2:31:18` colons line up; tabular-nums is paranoid double-belt-and-suspenders for proportional fonts. Convention: any future "incuestionable" technical reading (latencies, durations, time-to-X) follows this style. Revenue/$ amounts continue to use `font-heading` for the "loud headline" reading (Bonos KPI Ticket promedio convention from Plan 02-03).
- Plan 03-03 (2026-05-04): Visibility contract — only Tikin-internal quality/revenue KPIs carry `data-presenter-hide`. Confirmed for: (a) Bonos's Comisión total card (Plan 02-03), (b) Bonos's Leaderboard (Plan 02-03 — top10empresas is a competitive ranking), (c) Payouts's Tasa de éxito card (Plan 03-03 — Tikin's reliability metric). NOT presenter-hidden: hero KPIs (count, $, P50, P95), TopBancos widget (per user 2026-05-04 — at cliente-foco view URL filter narrows to that empresa), LatencyHistogram (the "la mayoría son inmediatos" story IS the cliente-facing narrative).
- Plan 03-03 (2026-05-04): TopBancos widget has `displayBancoName` private helper that title-cases bank codes (`bancolombia → "Bancolombia"`, `banco_de_bogota → "Banco De Bogota"`). Edge cases: `"OTRO_MEDIUM"` (Plan 03-01 fallback constant) → `"Sin medio"`; `"Otros bancos"` (Plan 03-02 literal Spanish rollup label) → unchanged. Future per-bank display-name overrides extend this map; for now title-case is human-readable enough.
- Plan 03-03 (2026-05-04): LatencyHistogram is single-series (no medium stack) — Plan 03-02 already emits `LatencyBucket[]` shaped `{bucket, count}` (no medium dimension). `minPointSize={2}` keeps zero-count buckets visible (Recharts hides zero-height bars by default — research Pitfall #5). `stroke="currentColor"` + `fill="currentColor"` for theme-aware via parent's `text-foreground` token (mirror of BonosChart from Plan 02-03). NO `<Card>` wrapper inside the chart file — Plan 03-04 page provides the chrome. NO `<Legend>` (single series; legend is visual noise). `allowDecimals={false}` on YAxis prevents `1.5` ticks on small datasets.
- Plan 03-03 (2026-05-04): DestinationSplit.tsx is INTENTIONALLY NOT created (replaced by TopBancos at real granularity per Plan 03-01 finding "all 798 production payouts are bank, zero card"). The originally-planned tarjeta/banco binary would have been a degenerate one-category widget. TopBancos honors PAY-04's "split por destino" intent at 12-bank granularity. Files in src/components/payouts/ verified to be EXACTLY 3: PayoutsKPICards.tsx, LatencyHistogram.tsx, TopBancos.tsx.
- Plan 03-03 (2026-05-04): Per-task atomic-commit pattern preserved (mirror of 02-03 and 03-02 — NOT the single-commit pattern from 02-01 / 03-01 which was justified by diagnostic-then-cleanup). Two feat commits: `fb7c04a` (formatDuration helper + PayoutsKPICards), `974ca32` (TopBancos + LatencyHistogram). No transient/diagnostic code in this plan.
- Plan 03-03 (2026-05-04): Plan executed exactly as written — ZERO deviations (THIRD consecutive zero-deviation plan after 02-04 and 03-02). UI-leaf plans benefit even more from heavy upfront pre-work than domain-library plans because the leaf shapes are constrained by stable upstream type contracts (Plan 03-02's output types). Pattern: when output type interfaces are stable + literal code blocks exist + grep-style verification passes are pre-cited, execution becomes mechanical. Plan 03-04 (page composition) is the next test of whether composition-pattern plans can also achieve deterministic-execute when the leaf components are already typed.
- Plan 03-04 (2026-05-04): `/payouts` page composition mirrors `bonos/page.tsx` line-for-line. Differences are confined to: imports (payouts adapter + leaves + domain functions), the conditional second fetch for the Transaction ID join, the post-fetch `Payout.empresa_id` patch step, and the rendered leaves (PayoutsKPICards / LatencyHistogram-in-Card / TopBancos). The plan-author's "mirror-the-proven-pattern" approach (Phase 2-04 was production-validated) → fewer architectural choices means fewer ways to get this wrong.
- Plan 03-04 (2026-05-04): Conditional second fetch via `filters.empresa ? getCachedTransactions() : Promise.resolve(null)`. Optimization rationale: React `cache()` would dedupe the BD_Plataforma fetch with DashboardHeader's call anyway, but returning `null` from the second branch on the no-filter path lets the page skip the Map construction entirely — saves CPU + clearer code. The conditional is purely an optimization, not a correctness requirement. Pattern reusable for Phase 4 Recargas / Inicio wherever cross-tab enrichment is conditional.
- Plan 03-04 (2026-05-04): Empresa join uses `Map<transactionId, empresa_id>` built from `txResult.rows` then `Payout.empresa_id ?? map.get(p.transactionId)` patches each row in O(n+m). The `??` fallback is forward-compatible: if Tikin later adds a holder→empresa mapping directly to BD_Payouts and Plan 03-01's adapter starts populating `Payout.empresa_id`, the fallback automatically prefers the adapter value over the lookup. No future code change needed in this page. Domain library (`filterPayouts`) stays pure — page handles the schema-shape mismatch.
- Plan 03-04 (2026-05-04): Both `filterPayouts` (state-filtered) and `filterPayoutsByPeriodOnly` (state-UNFILTERED for success-rate denominator) derive from `enrichedPayouts`, NOT from `payoutsResult.rows`. The join MUST apply BEFORE either filter — otherwise empresa narrowing breaks (filter would match against a still-undefined `empresa_id` and return `[]`). Filter propagation invariant documented in the page's JSDoc.
- Plan 03-04 (2026-05-04): Success rate is computed even when `completed.length === 0`. Rationale: `periodOnly` may have failed/in-progress payouts in the date+empresa window — '100 attempts, 0 completed → 0% success rate' is meaningful information, not an empty state. Inline JSDoc documents the apparent inconsistency for future reviewers.
- Plan 03-04 (2026-05-04): `LatencyHistogram` is wrapped by the page in a Card with title 'Latencia de payouts' (no description — the title carries the story). `TopBancos` is rendered as-is because Plan 03-03 ships it with its own Card chrome. Different chrome ownership matches each leaf's design intent: histogram is a generic chart (page provides chrome), TopBancos is a self-contained widget (provides its own chrome). Mirror of `BonosChart` page-provides-chrome pattern from Plan 02-04.
- Plan 03-04 (2026-05-04): Production deploy `https://project-dashboard-allec5r4i.vercel.app` (id `dpl_2VyRRm4VrLvXJFjNch4qyujsZkMF`, alias `https://project-kr6et.vercel.app`, region iad1) is the new canonical production for Phase 3. Previous Phase 2 production deploy (`project-dashboard-qhrwwpfuw`) is superseded but not deleted (Vercel preserves history).
- Plan 03-04 (2026-05-04): Live production data on the default `/payouts` view: 727 payouts completados, $ 653.315.014 volumen, P50 = `7:55:16`, P95 = `116:02:30`, Tasa de éxito 91.2%. The `font-mono tabular-nums` styling on P50/P95 makes the colons line up across cards — that's the "incuestionable" technical reading 03-CONTEXT.md asked for. With `?empresa=$1anderson` the page narrows to 3 payouts ($ 800K, P50 0:33:31, P95 3:13:24, 100% success) confirming the Transaction ID join works end-to-end.
- Plan 03-04 (2026-05-04): Plan executed exactly as written — ZERO deviations (FOURTH consecutive zero-deviation plan after 02-04, 03-02, 03-03). Page-composition plans CAN match domain-library and UI-leaf plans on the deterministic-execute spectrum when the reference page is mirror-able + novel logic is shipped as literal code + scope decisions are pre-resolved + upstream type contracts are stable. The plan-author phase doing the "all four conditions in place" upfront work is the high-leverage step.
- Plan 04-04 (2026-05-04): Cliente-foco visibility gate is JS-free. PresenterFrame writes TWO data-attributes (`data-presenter` + `data-empresa-filter`) on the outer wrapper from URL state; CSS does the hiding via attribute combination `[data-presenter="on"][data-empresa-filter="active"] [data-presenter-empresa-hide]`. No React conditionals, no useEffect, no hydration mismatch. Pattern reusable for any future "hide ONLY when X AND Y" visibility flag — add a new attribute on the wrapper, add a new CSS rule combining qualifiers, tag elements with the new descendant-attribute. Future contributors should NOT introduce JS-driven visibility flips for new presenter/cliente-foco gates.
- Plan 04-04 (2026-05-04): Two distinct visibility attributes coexist on the same data-presenter contract: `data-presenter-hide` (plain Modo Presentación — hides Comisión, Take rate, Tasa de éxito; Tikin-internal revenue/quality KPIs) vs `data-presenter-empresa-hide` (cliente-foco only — hides top-empresa rankings, empresas-nuevas, EmpresasActivasChart that degenerates to flat line at y=1). Naming is deliberately verbose so source-grep finds the exact use case. Plans 04-05 and 04-07 inherit the contract by tagging container-level elements (not individual children) — minimizes attribute soup, keeps visibility at the editorial-block level.
- Plan 04-04 (2026-05-04): The "all empresas" sentinel is the EMPTY STRING (or absent URL param), NOT `__all__`. Verified at execution time in `src/lib/url-state.ts` (no constant) and `src/components/filters/empresa-filter.tsx` (option `value=""`, onChange clears via `e.target.value || undefined`). Plan's reference to `__all__` was tentative per Plan 02-02 STATE entry; not adopted. Defensive `__all__` check kept in PresenterFrame logic for forward-compat if a sentinel is introduced later (Phase 5 may add an explicit "all" representation).
- Plan 04-04 (2026-05-04): Parallel-wave git index race documented. Phase 4's wave 1 ran plans 04-01, 04-02, 04-03, 04-04 concurrently. Between Plan 04-04's `git add presenter-frame.tsx` and `git commit`, Plan 04-01 staged `period.ts` (untracked at that moment), so commit `76052a1` carries both files under the 04-04 message and commit `28d709c` (labeled "feat(04-01): create period.ts") actually contains only `inicio-hechos.ts`. Code state is correct; only git-message-to-content labeling is off. Mitigation for future parallel waves: each agent should `git stash --include-untracked` foreign untracked files before staging, OR use `git commit -- <pathspec>` to limit the commit to specific paths. The "stage files individually, never `git add .`" rule was followed — the race is intrinsic to a shared git index across concurrent processes.
- Plan 04-03 (2026-05-05): `recargas.ts` is a PURE module (mirror of `bonos.ts`/`payouts.ts`) — no `next/`, `react`, `server-only`, `lib/sheets/`, or `lib/format` imports. Type-only imports for `Transaction, TransactionType` from `./types` and `DashboardFilters` from `@/lib/url-state`; runtime `formatInTimeZone` from `date-fns-tz`. 433 lines, 10 exports (5 functions for Plan 04-08 page composition + 2 hechos curados for Plan 04-06 UI + 3 output type interfaces). Inline date helpers (`startOfDayBogotaTimestamp`/`endOfDayBogotaTimestamp`) are verbatim copies from `bonos.ts` — third domain module to establish this DRY-vs-cohesion call (the shared util would require all three domain modules to import a fourth file; inline ~22 lines is the cheaper choice when only three modules need it).
- Plan 04-03 (2026-05-05): RECHARGE_TIPOS = `['PAYIN_PSE', 'PAYIN_TRANSFER']`. Cited from 02-01-SUMMARY.md production capture (these are the "user puts money in" tipos per types.ts JSDoc). PURCHASE/BONUS deliberately excluded — PURCHASE has direction='in' for the receiving side but is a sale TO the user from another empresa (different intent); BONUS is a promotion (different intent, owned by Bonos tab). Conflating them with PAYIN_* would make the "X recargas, $Y montoTotal" highlight-reel narrative misleading. If Tikin adds variants (PAYIN_NEQUI, PAYIN_DAVIPLATA), append to the constant — single-edit-point migration via Set membership check.
- Plan 04-03 (2026-05-05): `aggregateRecargasByDate` uses `formatInTimeZone(t.fecha, "America/Bogota", "yyyy-MM-dd")` directly from `date-fns-tz`, NOT `toBogotaISODate` from `@/lib/format` (which is what bonos.ts uses). Deliberate divergence enforced by an explicit verify check in PLAN.md ("imports show ONLY type-only imports + date-fns-tz. No ... lib/format"). The recargas module stays decoupled from the project's Intl gate so it remains importable from any context (including hypothetical Edge Functions / non-Server contexts) without pulling the format utility tree.
- Plan 04-03 (2026-05-05): `RecargaSummary` shape is narrower than `BonoSummary` — only `{count, montoTotal}`, NOT `{count, ticketPromedio, comisionTotal, montoTotal}`. Recargas highlight-reel doesn't need ticketPromedio (recargas have no "ticket" concept; users decide their own amount) or comisionTotal (recargas are typically free for the user; comisión goes to PSE/bank, not Tikin). Plan-author scoped the type to what the UI actually displays — fewer fields = fewer chances for downstream confusion.
- Plan 04-03 (2026-05-05): Hechos curados as pure `T | null` functions. `findTopEmpresaRecargadora` is trivial first-of-sorted-rows pick (since `aggregateRecargasByEmpresa` already sorts desc by monto); module stays composable: caller chains aggregate → findTop without re-sorting. `findRecargaMasGrande` single-pass reduce O(n) with deterministic tie-break: on equal monto, prefer the chronologically-earlier `fecha`. JSDoc documents the contract so UI consumers in Plan 04-06 don't accidentally rely on tie-order. Pattern reusable in Plan 04-04 (inicio-hechos.ts, parallel wave 1) for the 3 inicio hechos curados.
- Plan 04-03 (2026-05-05): Per-task atomic-commit pattern preserved (mirror of 02-02 / 02-04 / 03-02 / 03-03 / 03-04 — NOT the single-commit pattern from 02-01 / 03-01 which was justified by diagnostic-then-cleanup). Two feat commits: `51cd230` (filter+summary+3 aggregations), `25ef95a` (2 hechos curados). No transient/diagnostic code.
- Plan 04-03 (2026-05-05): Plan executed exactly as written — ZERO deviations (FIFTH consecutive zero-deviation plan after 02-04, 03-02, 03-03, 03-04). Pattern reinforces: domain library plans achieve near-deterministic execution when (a) literal code blocks ship in `<action>`, (b) verify steps cite expected outputs to the digit, (c) reference module is mirror-able line-for-line, (d) divergences from the reference (here: no `@/lib/format` import) are explicit in `<verify>`, (e) tipo set membership cited from production capture (Plan 02-01) so no research speculation needed.
- Plan 04-01 (2026-05-05): `period.ts` is a foundational pure module — more foundational than `format.ts` (could in theory be imported FROM format.ts). Inlines the literal Bogotá `T00:00:00-05:00` offset construction (mirror of `bonos.ts:53-62`) and imports `formatInTimeZone` from `date-fns-tz` directly to format prior-period dates as YYYY-MM-DD; NO `@/lib/format` import. Avoids any circular-dependency risk and keeps the module independent of the project's Intl gate. Two exports: `computePriorPeriod` (filters → {from, to} | null) and `pctChange` (current, prior → number | null). 108 LOC.
- Plan 04-01 (2026-05-05): `computePriorPeriod` returns the IMMEDIATELY-PRIOR window of the SAME LENGTH IN DAYS (not the prior calendar period). April 2026 (30 days) → March 2-31 (30 days), NOT March 1-31. Validated by fixture: `lengthDays = differenceInCalendarDays(toDate, fromDate) + 1` (inclusive both ends, mirror of filterBonos), `priorTo = subDays(fromDate, 1)`, `priorFrom = subDays(priorTo, lengthDays - 1)`. Returns null on missing/malformed from/to (defensive shape check + finite-timestamp check + monotonic check `toMs >= fromMs`). The user's CONTEXT.md spec is explicit: "filtro = abril 2026 → compara contra marzo 2026 (ventana del mismo tamaño, inmediatamente previa)".
- Plan 04-01 (2026-05-05): `pctChange` returns `number | null` (NOT NaN/Infinity on edge cases). Three null cases collapse to one return site: `prior === 0`, `prior` non-finite, `current` non-finite. Caller renders em-dash via `formatPercent(null) → '—'` from format.ts. Mirror of `bonos.ts:286` `pctDelTotal` zero-safety pattern. Reusable beyond Inicio if Recargas adds delta support in v2 (REC-V2-XX).
- Plan 04-01 (2026-05-05): `inicio.ts` is a PURE module (mirror of `bonos.ts`/`payouts.ts`/`recargas.ts`) — no `next/`, `react`, `server-only`, `lib/sheets/`, or `lib/format` imports. Type-only `Transaction, TransactionType` from `./types` and `DashboardFilters` from `@/lib/url-state`; runtime `formatInTimeZone` from `date-fns-tz`. 361 LOC, 10 exports (6 functions for Plans 04-05/04-07 page composition + 4 output type interfaces). Inline date helpers verbatim from bonos.ts:53-74 (FOURTH domain module to make this DRY-vs-cohesion call).
- Plan 04-01 (2026-05-05): Inicio default filter contract is BROADER than Bonos — `direction='in' + status='completed'` + Bogotá-anchored from/to + optional empresa. NO `tipo === 'BONUS'` filter. CONTEXT.md vision: GMV is total volume across ALL completed inflows (BONUS, PAYIN_*, P2P, etc.), not just bonos. The `bonosVendidos` KPI is computed INSIDE `summarizeInicio` over the full filtered set. `filterCompletedIn` is the function name (vs `filterBonos`/`filterRecargas`/`filterPayouts`) to advertise the broader scope.
- Plan 04-01 (2026-05-05): `summarizeInicio` is single-pass: tracks `gmv`, `comision`, `bonosVendidos`, `Set<empresa_id>` in one loop. `takeRate` computed AFTER as `gmv > 0 ? comision/gmv : 0` (zero-safe, never NaN). 5 KPIs returned: gmv, comision, takeRate, empresasActivas, bonosVendidos. Empty-input safe across all aggregators. `InicioDeltaSummary { current, prior }` is the stable contract for Plan 04-05 KPICardsInicio: `summarizeInicio(filterCompletedIn(tx, filters))` for current + same chained call with prior-period filters for `prior`, then per-card render `{value, pctChange(current.x, prior?.x ?? 0)}`.
- Plan 04-01 (2026-05-05): Active-empresa aggregators (`aggregateActiveEmpresasByDate/Week`) use `Map<bucket, Set<empresa_id>>` shape — `Set.add` on existing id is no-op, `set.size` is the distinct count. Pitfall 11 (RESEARCH.md) explicitly closed: 1 empresa with 10 transactions in one day counts as 1 active for that day, NOT 10. Fixture verified: 3 tx empresa A on Apr 15 + 2 tx empresa B on Apr 16 → `[{04-15, count:1}, {04-16, count:1}]` (NOT count:3 / count:2). Same dedup contract at week granularity.
- Plan 04-01 (2026-05-05): Week bucket format = `RRRR-'W'II` (ISO week-numbering year + ISO week-of-year), NOT `yyyy-'W'II` (calendar year). Rationale: yyyy is the calendar year, RRRR is the ISO week-numbering year — they differ for ~5 days/year at boundaries (e.g. 2024-12-30 belongs to ISO week 2025-W01). Using yyyy would mis-bucket those days. RRRR-Www is also lexicographically chronological (string sort works as date sort, so the aggregation output sorted asc by bucket is also sorted asc by time). Fixture verified: 2026-04-29T15:00 Bogotá → `2026-W18`.
- Plan 04-01 (2026-05-05): Parallel-wave git index race (SECOND occurrence in Phase 4). Concurrent agent (in `--teammate-mode tmux`) executing Phase 4 Plans 04-02/04-03/04-04 was committing to the same repo while Plan 04-01 was in-flight. Sibling's commit `76052a1` (`feat(04-04)`) co-shipped period.ts in its diff (matching content byte-for-byte to what Plan 04-01 wrote). Between `git add src/lib/domain/period.ts` (no-op since HEAD already had identical content) and `git commit`, sibling tracked `inicio-hechos.ts`, which got captured under Plan 04-01's commit message in `28d709c`. Code state correct (period.ts in HEAD is byte-identical to spec; all 7 must_have truths verified live), but commit-message-to-content labeling is off. NO rework needed — destructive history rewrite would impact sibling's downstream commits. Pattern is now documented twice (04-04 first, 04-01 confirming) — orchestrator should serialize the commit step across parallel-wave agents going forward, OR each agent should `git stash --include-untracked` foreign files before staging.
- Plan 04-01 (2026-05-05): Per-task atomic-commit pattern preserved (mirror of 02-02 / 02-04 / 03-02 / 03-03 / 03-04 / 04-03). Two feat commits: 28d709c (Task 1 — period.ts; misfiled per above note), 4d7c9ee (Task 2 — inicio.ts; clean). No transient/diagnostic code. Plan executed with ZERO TECHNICAL deviations (sixth consecutive technical-zero-deviation plan after 02-04, 03-02, 03-03, 03-04, 04-03); the only deviation is the operational git-race labeling drift, which has no correctness impact.
- Plan 04-02 (2026-05-05): `inicio-hechos.ts` is a PURE module (mirror of bonos.ts / empresas.ts / payouts.ts / recargas.ts / inicio.ts) — no next/, react, server-only, lib/sheets/, lib/format imports. Type-only `Transaction` from `./types` and `DashboardFilters` from `@/lib/url-state`; ZERO runtime imports (Task 2 doesn't need date-fns helpers — uses inline literal `T00:00:00-05:00` / `T23:59:59.999-05:00` Bogotá offset construction, mirror of bonos.ts:53-74). 180 LOC, 5 exports (3 interfaces + 2 functions). SIXTH domain module to establish the inline-Bogotá-offset DRY-vs-cohesion call.
- Plan 04-02 (2026-05-05): `findTopEmpresaByGMV` decision — "top" is by ABSOLUTE GMV, NOT growth (RESEARCH.md Open Question 1). Rationale: removes prior-period dependency, simpler hecho copy ("$ X facturados" vs "+Y% vs marzo"). Single-pass O(N) Map reduction with first-occurrence-wins for empresa_nombre, deterministic lexicographic tie-break (rare in practice given continuous monto values). Empty input → null; all-zero GMV → returns lexicographically-first empresa with `gmv: 0` (page renders empty-state copy when gmv === 0).
- Plan 04-02 (2026-05-05): `findEmpresasNuevasActivadas` Pitfall 5 hardening at THREE layers: (1) JSDoc IMPORTANT paragraph cites "Pitfall 5" by name and quotes failure mode; (2) function name implies period-relative narrowing ("Activadas" reads as "in this period"); (3) Plan 04-07 page composition responsible for passing `txResult.rows` (unfiltered dataset), NOT `filterBonos(...)` (filtered subset). If a caller accidentally passes filtered subset, every empresa in subset appears "new" by definition (subset already excludes everything outside window). Function does NOT validate input is unfiltered — same trust contract as bonos.ts filter functions.
- Plan 04-02 (2026-05-05): Empresas-nuevas top-5 cap with overflowCount for "+N más" UI display. Sorted ascending by firstTx (earliest activation = longest-tenured of new cohort, reads as most "interesting" in editorial copy). Window guards: missing/unparseable filters.from/to → returns `{shown: [], overflowCount: 0}` (degrades gracefully rather than guess). Empresa-filter belt-and-suspenders: if filters.empresa active, narrows results to single empresa BEFORE the cap (CONTEXT.md cliente-foco contract hides hechos curados via CSS, but this filter ensures sane shape if ever rendered). Honors `__all__` sentinel forward-compat (per Plan 04-04 entry 133, current sentinel is empty string).
- Plan 04-02 (2026-05-05): NO `findLatenciaDestacada` extracted in this plan. Plan 04-07 page composition reuses Phase 3's `summarizePayouts` from `./payouts` directly — that function already returns `{count, p50Seconds, p95Seconds, ...}` and the "P50 vs período anterior" delta is computed by composing `summarizePayouts(currentPeriodPayouts)` with `summarizePayouts(priorPeriodPayouts)` and feeding both into Plan 04-01's `pctChange`. No new domain function justified — existing percentile primitive is exactly what's needed.
- Plan 04-02 (2026-05-05): Output types `TopEmpresaResult`, `EmpresaNueva`, `EmpresasNuevasResult` are stable contracts for Plan 04-05 (HechosCurados component) and Plan 04-07 (page composition). Co-located with the function that produces them, mirror of bonos.ts BonoSummary/BonoByDate/BonoByEmpresa pattern. EMPRESAS_NUEVAS_CAP = 5 named constant inside module (NOT exported); future tuning is a single-line edit.
- Plan 04-02 (2026-05-05): Per-task atomic-commit pattern preserved (mirror of 02-02 / 02-04 / 03-02 / 03-03 / 03-04 / 04-01 / 04-03). Two feat commits: 28d709c (Task 1 — inicio-hechos.ts; misfiled-message per Plans 04-01 and 04-04 git-race entries 134/150), 3a502a3 (Task 2 — findEmpresasNuevasActivadas appended; correctly attributed `feat(04-02)`). The Task 1 race is now confirmed from the 04-02-side (sha256 verified): commit 28d709c content is byte-identical to my Task 1 Write output (`ab7db587c69712d107870b3f766eda8d50467f4845c6bd3a0426ca942d519dd6`). No correctness impact; consumers import by path.
- Plan 04-02 (2026-05-05): Plan executed with ZERO TECHNICAL deviations (SEVENTH consecutive technical-zero-deviation plan after 02-04, 03-02, 03-03, 03-04, 04-03, 04-01); the only deviation is the operational git-race labeling drift, which has no correctness impact and was already documented from two sides (04-01 + 04-04). Phase 4 Wave 1 (4 plans, 4 zero-deviation passes) closed.
- Plan 04-07 (2026-05-05): `/inicio` page composition mirrors `bonos/page.tsx` + `payouts/page.tsx` line-for-line. Differences confined to: dual-period `filterCompletedIn` calls (current + prior windows over the SAME `allTx` payload), `Promise.all` parallel BD_Plataforma + BD_Payouts fetch, bucket-granularity switch at 60-day threshold, page-level Card wrap on EmpresasActivasChart, and the 3 hechos-curados domain calls. 255 LOC final (vs ~37 LOC placeholder); commit 26c8432 +246/-18. Top-level JSDoc (~70 lines) documents the 7-step pipeline + dual-period pattern + bucket threshold + cliente-foco delegation + empty-state convention + error handling — replaces what would otherwise be tribal knowledge in future Phase 5 work.
- Plan 04-07 (2026-05-05): Dual-period filter pattern. `currentTx = filterCompletedIn(allTx, filters)` + `priorTx = filterCompletedIn(allTx, {...filters, from: priorWindow.from, to: priorWindow.to})` over the SAME unfiltered `allTx` array — exactly ONE Sheets read per request, second window derived in-memory. `computePriorPeriod` returns null when filters lack from/to → priorTx is null → KPI deltas render em-dash via `pctChange(current, null)`. Reusable for any future delta-bearing page (Phase 5 if Clientes adds period comparisons). The pattern requires summary-shape consistency (same `summarizeInicio` invariant for both windows), which Plan 04-01's contract guarantees.
- Plan 04-07 (2026-05-05): Promise.all parallel Sheets fetch (NOT sequential await pair). The two reads are independent (BD_Plataforma + BD_Payouts) — kicking them in parallel saves one network round-trip on cache-miss requests. React `cache()` still dedupes the BD_Plataforma fetch with DashboardHeader's identical call on the same render — verified via no double-fetch artifacts in production page sizes. Pattern reusable for any future page that needs both tabs (Phase 5 Clientes if it joins payouts to clientes).
- Plan 04-07 (2026-05-05): Page-level Card wraps EmpresasActivasChart so `data-presenter-empresa-hide` attaches to the Card (whole editorial block hides cleanly under cliente-foco) NOT to the inner chart (which would leave empty Card chrome visible). Different chrome ownership than HechosCurados (which carries its own attribute internally per Plan 04-05) and TopBancos (Plan 03-04, self-contained widget). Net: exactly ONE page-level grep hit for the attribute. Pattern reusable for Plan 04-08 if RecargasTrendChart degenerates to flat line under empresa filter (likely — one empresa's recargas timeline is the same shape as N empresas' if N=1).
- Plan 04-07 (2026-05-05): Bucket-granularity decision lives in the PAGE, not in the leaf. Page computes `length` from filters.from/to (default 30 when unbounded), picks `granularity: 'day' | 'week'` at the 60-day threshold, calls the matching aggregator pair (`aggregateGMVByDate` vs `aggregateGMVByWeek` etc.), and threads `granularity` to GMVTrendChart for tickFormatter switching. Charts stay bucket-agnostic; the page is the orchestrator. Card subtitle hint ("(diario)"/"(semanal)") rendered as small muted span inside CardTitle so the reader knows what each bar represents.
- Plan 04-07 (2026-05-05): `findEmpresasNuevasActivadas(allTx, filters)` MUST receive the unfiltered `allTx` (NOT `currentTx`). RESEARCH Pitfall 5: a filtered subset would mark every empresa in subset as "new" by definition (subset already excludes everything outside window). The page is the trust boundary — domain function does not validate input. Inline comment in page.tsx (lines 182-186) cites Pitfall 5 by name to lock the contract for future maintainers.
- Plan 04-07 (2026-05-05): Empty-state branch is implicit (no short-circuit Card replacing the layout). `summarizeInicio([])` is zero-safe → zero-value KPI cards still render. `<2 points` chart guard renders friendly Spanish empty-state copy. HechosCurados handles per-card empty states (Plan 04-05 contract). The user reads "your filter excluded everything" via empty-state copy across the layout, NOT "the dashboard is broken" via a single replacement Card. Same convention as `bonos/page.tsx`.
- Plan 04-07 (2026-05-05): Cliente-foco contract verified end-to-end across all 4 URL states (user response: "approved"). The CRITICAL state — `?presenter=1&empresa=$X` — correctly hides Comisión + Take rate cards (Plan 04-05 attributes), HIDES the EmpresasActivasChart Card (Plan 04-07 page-level attribute), and HIDES the entire HechosCurados block (Plan 04-05 wrapper attribute). What remains visible is exactly the editorial reading 04-CONTEXT.md asked for: GMV + 3 visible KPIs + GMV-over-time chart for the cliente. Phase 5 CLI-08 ("Generar vista para cliente") precondition is now SATISFIED — the share-URL can land on a populated /inicio without any Tikin-internal noise.
- Plan 04-07 (2026-05-05): Task 3 (Vercel --prod deploy) intentionally deferred to batch with Plan 04-08 (Recargas page composition). Rationale: Phase 4 ships /inicio + /recargas together as the user-facing pair; deploying /inicio alone would put a half-shipped Phase 4 in production. This is an operational deviation (not a technical one) — the plan as written calls for an immediate deploy after approval, but the orchestrator chose to batch. No correctness impact; production still on the Plan 03-04 deploy until 04-08 ships.
- Plan 04-07 (2026-05-05): Single code commit (26c8432) covers the full Task 1 rewrite — placeholder removed and full composition added in one diff (+246/-18). NOT the per-task atomic-commit pattern from prior plans because Task 1 IS the only code-bearing task (Task 2 is the human-verify checkpoint, Task 3 is deploy). Plus a metadata commit for SUMMARY + STATE.md. Plan executed with ZERO plan-spec deviations on Task 1 — literal code blocks from `<action>` compiled clean on first build, 8 verify checks all green, 4-URL cliente-foco contract held on first manual verification. EIGHTH consecutive technical-zero-deviation plan (after 02-04, 03-02, 03-03, 03-04, 04-03, 04-01, 04-02). Page-composition plans CAN match domain-library and UI-leaf plans on the deterministic-execute spectrum when the reference page is mirror-able + novel logic ships as literal code + scope decisions are pre-resolved + upstream type contracts are stable.
- Plan 04-08 (2026-05-06): `/recargas` page composition mirrors `bonos/page.tsx` + `/inicio` page (Plan 04-07) line-for-line, minus complications. Removed: payouts cross-fetch (no latencia hecho on Recargas), bucket-granularity switch (RecargasTrendChart from Plan 04-06 is daily-only by current spec), 3rd hecho curado, presenter-only KPIs. Kept: dual-period filter pattern (current + prior windows over same `allTx`), inline error fallback, empty-state delegation to leaves, NO verifySession, `dynamic = 'force-dynamic'`. 175 LOC final (vs ~37 LOC placeholder); commit 559bc9b on 2026-05-05. Top-level JSDoc (~60 lines) documents the pipeline + dual-period reuse + cliente-foco delegation + empty-state convention + error handling + why simpler than `/inicio`.
- Plan 04-08 (2026-05-06): `?presenter=1` (presenter alone, no empresa) behaves IDENTICAL to default on `/recargas`. Recargas has only 2 KPIs (`Total $ recargado`, `# transacciones`) — both pure volume metrics, neither Tikin-internal-only. There's no Comisión/Take rate equivalent to hide. Page emits zero `data-presenter-hide` attributes; leaves emit zero too. Modo Presentación is a typography/chrome bump only (handled by global PresenterFrame), not a visibility flip. Cliente-foco state (`?presenter=1&empresa=$X`) hides ONLY HechosCuradosRecargas (which carries `data-presenter-empresa-hide` on its outer wrapper per Plan 04-06). The page-side `grep` on visibility attributes returns 0 hits in JSX (the only match is in JSDoc). Different chrome ownership pattern than `/inicio` (which page-wraps EmpresasActivasChart with presenter-empresa-hide).
- Plan 04-08 (2026-05-06): Cliente sees their own table row on `/recargas` (no presenter-empresa-hide on RecargasTable). Per CONTEXT.md cliente-foco definition: "show the cliente what's about THEM". Empresa-filtered top-10 table shows cliente's row only (or empty-state if no recargas). Hiding it would deprive the cliente of their own number — defeats the share-URL story. Same rationale as TopBancos staying visible on `/payouts` in cliente-foco (Plan 03-03 STATE entry 116).
- Plan 04-08 (2026-05-06): RecargasTrendChart does NOT degenerate to flat line under empresa filter. Unlike EmpresasActivasChart on `/inicio` (which becomes y=1 horizontal line when filtered to one empresa per Plan 04-07 STATE entry 163), the recargas trend keeps bar-height variability — each day has a count of recargas FOR that empresa, not a "distinct empresas active that day" count. NO page-level Card wrap with `data-presenter-empresa-hide` needed. Chart stays visible in cliente-foco — desired.
- Plan 04-08 (2026-05-06): Single sequential `await getCachedTransactions()` (no Promise.all). Recargas only reads BD_Plataforma — no BD_Payouts join needed. React `cache()` already dedupes the call with DashboardHeader. Sequential await is the simplest correct shape. Pattern reusable for any future single-tab page in Phase 5+.
- Plan 04-08 (2026-05-06): 2 KPIs (not 3 or 4). Plan-author scoped tightly to REC-01 (Total $ recargado) + REC-02 (# transacciones). REC-V2-01 (success rate) is v2 because the `status='completed'` filter is already inside `filterRecargas`'s default contract — adding a denominator KPI requires a `filterRecargasByPeriodOnly` (state-unfiltered) sibling helper, not v1 scope. Decision deferred consistently with Phase 3 PAY-V2-01 (Payouts success rate) v2 deferral.
- Plan 04-08 (2026-05-06): Vercel `--prod` deploy (Task 3) executed at plan close on 2026-05-06 — supersedes Phase 3 production at `https://project-dashboard-allec5r4i.vercel.app` (which kept /inicio + /recargas as Phase 1 placeholders). New canonical production: `https://project-dashboard-4b4fxxmdr.vercel.app` (deployment `dpl_8GprZ3cAoemQCczDTRRLeqy4WNqS`, alias `https://project-kr6et.vercel.app`, region iad1). Deploy ran clean in ~2 min via the global Vercel CLI 52.0.0 at `/Users/alejoalmeida/.nvm/versions/node/v24.11.0/bin/vercel` (not on default PATH; required explicit PATH export). Auth as `alejandro-9264` carried over from previous Phase 3 deploy session. Production smoke: `/login` HTTP 200, `/recargas` HTTP 307 → /login (proxy gate intact). Phase 4 SHIPS.
- Plan 04-08 (2026-05-06): Plan executed exactly as written — ZERO plan-spec deviations on Task 1 (NINTH consecutive technical-zero-deviation plan after 02-04, 03-02, 03-03, 03-04, 04-03, 04-01, 04-02, 04-07). Single code commit 559bc9b. Page-composition plans now have a 3-data-point track record of deterministic execution (03-04 + 04-07 + 04-08). Final tally for Phase 4: 8 plans, 7 zero-technical-deviation passes, 1 with operational git-race-only deviation (04-04 — code state correct, only commit-message-to-content labeling drift).

### Pending Todos

None yet.

### Blockers/Concerns

- **Vercel Deployment Protection ENABLED by default** — production URL requires Vercel SSO. For client demos (per PROJECT.md primary use case) USER must disable: https://vercel.com/alejandro-almeidas-projects-5f343d98/project-dashboard/settings/deployment-protection → Vercel Authentication → Disabled. Or wait for Phase 5 (custom domain `dashboard.tikin.co`) which can route public.
- **3 v2 features now v1-eligible** — REC-V2-01 (Recargas success rate), PAY-V2-01 (Payouts success rate), PAY-V2-02 (Payouts failure breakdown). The data exists today (`status` in BD_Plataforma confirmed `completed`/`rejected` live; `State` + `Failure Reason` in BD_Payouts captured by 01-04). REQUIREMENTS.md should be updated by Phase 2 or a small docs phase.
- **GCP service account key NOT rotated** — `private_key_id 71dd502c55f4859096a2a5073dd23bdceecc4459` was leaked in chat history during Plan 04 setup. SA scope: Viewer on one Sheet only. User accepted to ship Phase 1; rotation procedure documented in 01-04-SUMMARY.md → Security Debt #1.
- **Password is `T1k1N` (5 chars)** — user-accepted. Mitigations: bcrypt cost 10 + Upstash sliding-window rate limit (5/5min/IP active in production). Rotation procedure documented in 01-04-SUMMARY.md → Security Debt #2.
- **Env vars only in Vercel `Production` target** — preview + development environments lack the 8 user vars (auth + GCP + Sheets). Future preview deploys will fail. Resolution procedure (loop over vars adding to preview + development) documented in 01-04-SUMMARY.md → Security Debt #3.
- **TransactionType.UKNOWN is a real value in production data** (sic — typo). Schema preserves it verbatim rather than silently mapping to OTRO so the data-quality issue stays visible in dashboards. User owns source-side cleanup at the Sheet.
- **Same tikintag may map to multiple wallets per empresa** — e.g. Liftit might have a corporate `$liftit-app` wallet and individual employee wallets with different tikintags. Today they appear as separate "empresas" in the dashboard (233 unique tikintags surfaced in production). Phase 5 (Clientes/Domain) is the natural place to introduce a many-to-one tikintag → empresa display-name mapping if Tikin confirms that's needed.
- **Bonos default filter excludes `direction=out` and `status=rejected` silently.** This is intentional — refunds are recorded as separate `REFUND` tipo rows so excluding `out` here doesn't double-count, and rejected transactions never carried money. But if Tikin asks "why does the count differ from my Sheet pivot?", the gap is here. Documented inline in 02-02-SUMMARY.md "Bonos Filter Contract".
- ~~**No card payouts in BD_Payouts.**~~ — ABSORBED by Plan 03-02 design. `aggregateLatencyHistogram` has no medium stack (single-series shape); `aggregateTopBancos` replaces the originally planned tarjeta/banco split with top N banks + "Otros bancos" rollup. PAY-04's intent ("split por destino") is honored using real granularity (bank codes) rather than the speculative tarjeta/banco binary. Original blocker dissolved.
- ~~**BD_Payouts.Holder ≠ tikintag** — empresa filter requires Transaction ID join in Plan 03-02.~~ — RELOCATED to Plan 03-04. Plan 03-02's `filterPayouts` correctly matches `p.empresa_id === filter.empresa`; `Payout.empresa_id` is undefined until Plan 03-04 page composition runs the `transactionId → empresa_id` join from BD_Plataforma. The filter naturally returns `[]` when join hasn't been performed (better empty than wrong-empresa data). Plan 03-04 has the new work scope to build a `Map<transactionId, empresaId>` (3188 entries, O(n) linear) ONLY when `filters.empresa` is active.
- **One BD_Payouts row had a numeric `Holder` cell** (Row 188 in current data). Skip rate 0.13% is acceptable but worth monitoring as data grows. If skip rate climbs, widening the `holder` schema to `z.union([z.string(), z.number()]).transform(s => String(s).trim())` is a 1-line fix in `src/lib/domain/schemas.ts`.

**Resolved this session:**
- ~~Schema mismatch in `src/lib/domain/schemas.ts`~~ — closed by Plan 02-01. /api/smoke green: count=3188, skipped=44.
- ~~Empresa identity column ambiguous~~ — decided in Plan 02-01: default = tikintag (override is 2-line edit in schemas.ts).
- ~~EmpresaFilter list is empty~~ — closed by Plan 02-02. Production verification: 233 unique empresas in dropdown, Spanish-collated, URL filter works (`?empresa=$mario` selects correctly).
- ~~/bonos placeholder page~~ — closed by Plan 02-04. /bonos now renders live BD_Plataforma data end-to-end with all filters working, presenter mode visually correct, empty/error states with Spanish copy, no regression on the other 4 tabs.
- ~~3 unknowns from 03-RESEARCH.md (Aging/TotalTime units, Destination Medium values, Holder ↔ tikintag)~~ — closed by Plan 03-01 via diagnostic-then-cleanup. All three answered with concrete production data; downstream decisions documented inline in `Payout` JSDoc and in 03-01-SUMMARY.md.
- ~~PAY-04 reinterpretation needed (no card payouts)~~ — closed by Plan 03-02. `aggregateTopBancos(rows, n=5)` returns `{top: BancoStats[], otros: BancoStats}` honoring PAY-04's "split por destino" intent at real granularity. `aggregateLatencyHistogram` has no medium stack — single-series shape avoids visually-misleading single-color stacks.
- ~~Empresa filter join location~~ — clarified by Plan 03-02. `filterPayouts` matches `p.empresa_id`; Plan 03-04 page composition is responsible for populating `empresa_id` via Transaction ID join when `filters.empresa` is set (skip the join cost on no-filter case).
- ~~/payouts placeholder page~~ — closed by Plan 03-04. /payouts now renders live BD_Payouts data end-to-end (727 payouts, $ 653.315.014, P50 7:55:16, P95 116:02:30, Tasa de éxito 91.2%) with conditional Transaction ID join from BD_Plataforma when empresa filter is active, presenter mode hides only Tasa de éxito (KPIs + LatencyHistogram + TopBancos all stay visible per user 2026-05-04 scope decision), empty state with friendly Spanish copy, no regression on the other 4 tabs. Production verified at `https://project-dashboard-allec5r4i.vercel.app`.
- ~~/inicio placeholder page~~ — closed by Plan 04-07. /inicio now renders live data end-to-end (5 KPIs with deltas, 2 bucket-aware charts, 3 hechos curados); cliente-foco contract verified across all 4 URL states. Production verified at `https://project-dashboard-4b4fxxmdr.vercel.app` (Plan 04-08 deploy).
- ~~/recargas placeholder page~~ — closed by Plan 04-08. /recargas now renders live BD_Plataforma data filtered to RECHARGE_TIPOS end-to-end (2 KPIs with deltas, trend bar chart, top-10 empresas table, 2 hechos curados); cliente-foco hides only HechosCuradosRecargas. Production verified at `https://project-dashboard-4b4fxxmdr.vercel.app` (deployment `dpl_8GprZ3cAoemQCczDTRRLeqy4WNqS`). Phase 4 SHIPS.

## Session Continuity

Last session: 2026-05-06 (post-checkpoint approval close + Vercel prod deploy)
Stopped at: ✅ Plan 04-08 (Recargas page composition) COMPLETE — checkpoint approved, Vercel `--prod` deploy shipped. **Phase 4 SHIPS.** Both `/inicio` and `/recargas` LIVE in production at `https://project-dashboard-4b4fxxmdr.vercel.app` (deployment `dpl_8GprZ3cAoemQCczDTRRLeqy4WNqS`, alias `https://project-kr6et.vercel.app`, region iad1) — supersedes Phase 3 prod which kept both as Phase 1 placeholders. /recargas pipeline: parseFilters → computePriorPeriod → single sequential `await getCachedTransactions()` (no Promise.all — single-tab data) → `filterRecargas` twice (current + prior over same allTx, dual-period filter pattern reused from Plan 04-07) → `summarizeRecargas` twice → `aggregateRecargasByDate` → `aggregateRecargasByEmpresa` + `top10RecargasEmpresas` → 2 hechos curados (`findTopEmpresaRecargadora`, `findRecargaMasGrande`) → JSX render. 175 LOC final (single code commit 559bc9b on 2026-05-05). Cliente-foco contract verified end-to-end across all 4 URL states (user response: "approved"): no-filters → 2 KPIs + chart + table + 2 hechos curados; presenter=1 → IDENTICAL to default (Recargas has no Tikin-internal-only KPI to hide); empresa=$X → 2 KPIs filtered, chart filtered, table shows just that empresa, hechos show empresa-related; presenter=1&empresa=$X (CLIENTE-FOCO) → 2 KPIs visible, chart visible, table visible (cliente sees their own row — desired), 2 hechos curados HIDDEN (HechosCuradosRecargas wrapper carries data-presenter-empresa-hide). Page emits ZERO data-presenter-*hide attributes — visibility delegation 100% to leaves (the only page-side grep hit is in JSDoc, which is a comment). Different chrome ownership than /inicio. RecargasTrendChart does NOT degenerate to flat line under empresa filter (recargas-by-date is bar-height-variable per empresa, unlike empresas-active-by-date which becomes y=1 for one empresa) → no page-level Card wrap with presenter-empresa-hide needed. Empty state (`?from=2026-12-01&to=2026-12-31`): KPIs render `$ 0` / `0`, chart shows "Sin datos suficientes…", table empty-state, hechos render their per-card empty messages. Inline Card error fallback on Sheets failure. NO verifySession (route group layout already guards). tsc clean. npm run build ✓ all routes emitted including /recargas ƒ (Dynamic). NINTH consecutive technical-zero-deviation plan (after 02-04, 03-02, 03-03, 03-04, 04-03, 04-01, 04-02, 04-07). Vercel CLI 52.0.0 found at `/Users/alejoalmeida/.nvm/versions/node/v24.11.0/bin/vercel` (not on default PATH; required explicit PATH export); auth as `alejandro-9264` carried over from previous Phase 3 deploy session; deploy ran ~2 min, build 1m, status `● Ready Production`. Smoke: `/login` HTTP 200, `/recargas` HTTP 307 → /login (proxy gate intact). Phase 4 final tally: 8 plans, 7 zero-technical-deviation passes, 1 with operational git-race-only deviation (04-04 — code state correct, only commit-message-to-content labeling drift). Phase 5 (Clientes + Domain) is the next milestone — CLI-07 (mini-resumen 3 cards Bonos/Recargas/Payouts) now points to a populated /recargas (Phase 1 placeholder dissolved); CLI-08 ("Generar vista para cliente") cliente-foco share-URL lands cleanly on both /inicio and /recargas; the orchestrator should next run gsd-verifier on Phase 4 and update ROADMAP.md to mark Phase 4 ✅ Complete. PRIOR Plan 04-07 close preserved below for context. ✅ Plan 04-07 (Inicio page composition) COMPLETE — checkpoint approved by user across all 4 cliente-foco URL states. /inicio is LIVE end-to-end on BD_Plataforma + BD_Payouts data: 5 KPIs with deltas (GMV, Comisión, Take rate, Empresas activas, Bonos vendidos), GMV trend chart + Empresas activas trend chart (bucket-aware at 60-day threshold; subtitle reflects daily/semanal), 3 hechos curados (top empresa GMV / latencia destacada with prior-period delta / empresas nuevas activadas with "+N más" overflow). page.tsx grew from ~37 LOC placeholder to 255 LOC full composition (commit 26c8432, +246/-18 in one diff). Pipeline: parseFilters → computePriorPeriod → Promise.all parallel Sheets fetch → filterCompletedIn TWICE (current + prior windows over the SAME allTx — single Sheets fetch) → summarizeInicio twice → bucket switch → 4 chart aggregations → 3 hechos curados (findTopEmpresaByGMV + findEmpresasNuevasActivadas with FULL allTx per Pitfall 5 + summarizePayouts on filtered payouts for latencia delta) → JSX render. Top-level JSDoc (~70 lines) documents the 7-step pipeline + dual-period pattern + bucket threshold + cliente-foco delegation + empty-state convention + error handling. Cliente-foco contract verified end-to-end across all 4 URL states (user response: "approved"): the CRITICAL state ?presenter=1&empresa=$X correctly hides Comisión + Take rate (Plan 04-05 attributes), the EmpresasActivasChart Card (Plan 04-07 page-level attribute), and the entire HechosCurados block (Plan 04-05 wrapper attribute). What remains visible is exactly the editorial reading 04-CONTEXT.md asked for. tsc --noEmit clean. npm run build ✓ in 9.2s (Next 16.2.4 Turbopack), all routes emitted, /inicio now ƒ (Dynamic) with full composition. ZERO plan-spec deviations on Task 1 — literal code blocks compiled clean on first build, 8 verify checks all green, 4-URL contract held on first manual verification. EIGHTH consecutive technical-zero-deviation plan (after 02-04, 03-02, 03-03, 03-04, 04-03, 04-01, 04-02). Operational deviation: Task 3 (Vercel --prod deploy) intentionally deferred to batch with Plan 04-08 — Phase 4 ships /inicio + /recargas together as the user-facing pair. Phase 5 CLI-08 ("Generar vista para cliente") is now UNBLOCKED — its precondition (cliente-foco contract impecable on the hero tab) is verified end-to-end. Plan 04-08 (Recargas page composition, the second human-verify checkpoint plan, mirrors this 04-07 skeleton minus hechos-curados-3 + payouts join) NEXT — closes Phase 4. PRIOR Wave 1 close preserved below for context. Plan 04-02 (Inicio hechos curados domain) closes the wave: `src/lib/domain/inicio-hechos.ts` created (180 LOC, 5 exports). `findTopEmpresaByGMV(filteredTransactions): TopEmpresaResult | null` is single-pass O(N) Map reduction with first-occurrence-wins for empresa_nombre, lexicographic tie-break, null on empty input. `findEmpresasNuevasActivadas(allTransactions, filters): EmpresasNuevasResult` finds empresas whose first-EVER tx falls in the active window — Pitfall 5 hardened in JSDoc + impl + function naming (callers MUST pass FULL allTx, NOT filtered subset). Top-5 cap with overflowCount for "+N más" UI; sorted asc by firstTx. Window guards (missing/unparseable from/to → empty result) and empresa-filter belt-and-suspenders (cliente-foco). Module verified PURE — only Transaction + DashboardFilters type-only imports, zero runtime imports of next/react/server-only/lib/sheets/ and zero `Intl.*`/`toLocaleString`. Decision: top empresa = absolute GMV, NOT growth (no prior-period dependency, easier hecho copy). Decision: NO `findLatenciaDestacada` extracted — Plan 04-07 reuses Phase 3's `summarizePayouts` directly. Stable contracts (`TopEmpresaResult`, `EmpresaNueva`, `EmpresasNuevasResult`) ready for Plan 04-05 (HechosCurados component) and Plan 04-07 (page composition). Per-task atomic-commit pattern: 28d709c (Task 1 inicio-hechos.ts — sha256 byte-identical to my Write output; misfiled-message under feat(04-01) per the parallel-wave git race already documented from BOTH sides in entries 134 + 150) + 3a502a3 (Task 2 — findEmpresasNuevasActivadas appended; correctly attributed feat(04-02)) + this docs commit. ZERO TECHNICAL deviations (SEVENTH consecutive after 02-04, 03-02, 03-03, 03-04, 04-03, 04-01). Phase 4 Wave 1 final tally: 4 plans, 4 zero-deviation passes, ~5-15 min wall-clock per plan in concurrent execution. Wave 2 (04-05 / 04-06 / 04-07 / 04-08) NOW UNBLOCKED — all four upstream domain libraries (period.ts, inicio.ts, inicio-hechos.ts, recargas.ts) ship with stable type contracts; cliente-foco CSS gate primitives (`data-presenter-empresa-hide` attribute) ready. Wave 2 plans: 04-05 (HechosCurados / InicioCharts / KPICardsInicio components), 04-06 (RecargasUI components), 04-07 (Inicio page composition wiring everything together), 04-08 (Recargas page composition). Mitigation guidance for future parallel waves established by 04-01 + 04-04 + 04-02 SUMMARY entries: use `git stash --include-untracked` of foreign untracked files before staging, OR `git commit -- <pathspec>` to limit commit scope.
Resume file: None
